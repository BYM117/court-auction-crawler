# 웹 공개 데이터의 개인정보 마스킹 — 인계 문서

작성일 2026-08-20. 수집기(맥) 쪽 점검 중 확인한 내용을 웹(꽁지맵) 작업자에게 넘기기 위한 메모다.

## 한 줄 요약

경매 물건에 딸린 **현황조사서·매각물건명세서 본문에 세입자 이름이 들어 있고, 그 본문이 웹 공개 payload에 그대로 실려 R2로 올라가고 있다.** 수집기는 원본을 그대로 보관하기로 했으니, **웹에서 보여줄 때 `김○○` 형태로 가리는 처리**가 필요하다.

## 왜 하는가

- 물건 기본정보(주소·감정가·매각기일)나 사진은 문제없다. 사실 정보이고 법원이 공공저작물로 자유이용을 허용한다.
- 문제는 **문서 본문**이다. 현황조사서에는 그 집에 사는 사람의 이름, 전입신고일, 임대차 관계가 적혀 있다.
- "공개된 곳에서 가져왔다"는 개인정보 면책 사유가 되지 않는다. 법원 사이트는 입찰 참고용으로 제한 제공하는 것이지 재배포를 전제로 공개한 게 아니다.
- 저작권 문제는 최악이라도 "쓰지 마세요"로 끝나지만, 개인정보는 당사자가 직접 문제 삼을 수 있다.

## 확인된 사실

### 어디에 들어 있나

웹 공개 payload(`public_auction_detail`)의 이 경로다.

```
documents[].metadata.text      ← 문서 본문 텍스트 (대부분 여기)
documents[].metadata.tables    ← 표로 정리된 같은 내용
```

`documents[]`의 나머지 필드(`id`, `document_type`, `status`, `title`, `file_size`, `sha256`, `url` 등)에는 개인정보가 없다.

`metadata`의 키는 `capture_method`, `text`, `tables` 세 개다.

### 문서 종류별 위험도

표본 600건을 훑어 이름으로 보이는 값이 있는지 본 결과다. 러프한 패턴 매칭이라 오탐이 섞여 있으니 **정확한 수치가 아니라 상대적 위험도로만 보면 된다.**

| 문서 종류 | 이름 포함 추정 | 비고 |
|---|---:|---|
| 현황조사서 | 약 47% | **주범.** 임대차관계 항목에 점유자 이름이 직접 나온다 |
| 매각물건명세서 | 약 25% | 점유자·임차인 항목 |
| 감정평가서 | 0% | 안전. 물건 가치 평가라 사람 이름이 없다 |

DB 전체 기준 본문을 보유한 문서는 89,886건이고, 문서 종류별 총량은 각각 4.3만 건 안팎이다.

### R2 접근 구조 (중요)

- 업로드 시 공개 ACL을 지정하지 않으므로 **버킷은 비공개**다. 주소만 안다고 열리지 않는다.
- 객체 경로
  - 물건 상세: `v1/items/{sha256(item_key)}.json` ← **여기에 문서 본문이 들어 있다**
  - 목록/지도용 스냅샷: `v1/snapshot.json.gz` ← `public_auction_summary`만 담기므로 **개인정보 없음**
  - 사진: `v1/assets/{sha256}{확장자}`
- 즉 **손봐야 할 건 물건 상세 JSON 하나뿐**이다. 지도와 목록은 건드릴 필요 없다.

## 언제 가릴 것인가 — 낙찰 기준 (2026-08-20 결정)

옥션원은 **물건이 낙찰되면 개인정보를 가린다.** 우리도 같은 정책으로 간다.

이유가 명확하다. 입찰 참여자에게는 세입자 정보가 판단에 필요하지만, **낙찰이 끝나면 공개할 이유가 사라진다.** 오히려 낙찰 후에도 계속 떠 있는 게 문제다.

### 낙찰 여부를 어떻게 아는가

물건 상세 payload에 두 가지 단서가 있다.

**(A) 기일내역의 기일결과** — 정확하지만 놓칠 수 있다

```
detail.tables[]  중 caption에 '기일내역'이 들어간 표
  → rows[] 에서 [1]이 '매각기일'인 행의 [4]가 기일결과
  → 가장 마지막 매각기일의 결과가 '매각'이면 낙찰
```

기일결과 값은 대부분 `유찰`이고, 낙찰되면 `매각`으로 찍힌다.

**한계**: 낙찰되면 물건이 법원 목록에서 사라지고, 우리 상세 재조회 대상에서도 빠진다. 그래서 낙찰 사실을 늦게 알거나 못 알 수 있다. 실제로 현재 DB에서 마지막 기일결과가 `매각`인 물건은 26건뿐이다. 실제 낙찰 건수보다 한참 적다.

**(B) `is_active` 플래그** — 덜 정확하지만 빠짐없다

```
is_active === false  → 가린다
```

수집기는 매각기일이 지나고 3일이 되도록 목록에 다시 안 나타나면 비활성으로 바꾼다. **낙찰됐거나, 취하됐거나, 종결된 물건**이 여기 해당한다. 현재 15,813건이다.

유찰된 물건은 새 기일을 받아 목록에 다시 나타나므로 활성으로 남는다. 즉 비활성이면 "입찰이 끝났다"고 봐도 무방하다.

### 권장 규칙

```
가린다  ←  is_active === false  또는  마지막 매각기일 결과 === '매각'
```

둘 중 하나만 걸려도 가린다. (B)가 그물 역할을 하고 (A)가 빠른 감지를 맡는다. **(B)만 써도 실용적으로는 충분하다.**

취하·종결된 물건까지 가리게 되지만, 그것도 어차피 공개할 이유가 없으니 문제되지 않는다.

### 더 정확히 하려면 (수집기 쪽 개선 제안, 아직 미구현)

법원경매정보에 **매각결과검색** 화면이 따로 있다. 낙찰 여부와 **낙찰가**를 정확히 알려준다. 이걸 수집하면

1. 낙찰 판정이 정확해진다 → 마스킹 시점이 정확해진다
2. **낙찰가 데이터가 쌓인다** → 시세 판단에 쓸 수 있는 가장 가치 있는 데이터다
3. 종결 처리가 추정이 아니라 사실 기반이 된다

단, 사이트가 **매각기일 다음날부터 7일간만** 제공한다. 그 안에 못 받으면 영영 못 받는다. 지금 수집 주기가 3시간이라 놓칠 일은 없다.

## 해야 할 일

### 1. 먼저 확인할 것 — 어디서 가릴 것인가

이게 제일 중요하다. 마스킹 위치를 잘못 잡으면 아무 의미가 없다.

| 방식 | 판정 |
|---|---|
| 서버(Vercel 함수 등)가 R2에서 읽어 → 이름 가리고 → 브라우저로 전달 | ✅ 안전 |
| 브라우저가 R2 서명 URL로 원본 JSON을 직접 받은 뒤 → 화면에서만 가리기 | ❌ 무의미 |

두 번째 방식이면 개발자도구 네트워크 탭에 원본 이름이 그대로 남는다. **꽁지맵이 현재 어느 방식으로 물건 상세를 가져오는지부터 확인할 것.**

두 번째 방식이라면 서버 경유로 바꾸거나, 아예 마스킹된 사본을 별도 경로(예: `v1/items-public/`)에 만들어 그쪽만 브라우저에 노출하는 방법도 있다.

### 2. 마스킹 규칙 (제안)

- 성은 남기고 이름을 가린다. `김철수` → `김○○`, `이영` → `이○`
- 한글 2~4자 이름이 대상. 법인명(`○○주식회사`)이나 지명은 건드리지 않는다.
- 마스킹 대상 문맥: `점유자`, `임차인`, `성명` 라벨 뒤에 오는 값
- **오탐 주의.** 단순히 "2~3자 한글"을 다 가리면 `법원`→`법○`, `번호`→`번○`, `안덕면`→`안○○`처럼 멀쩡한 단어가 깨진다. 실제로 테스트하다 이 함정에 빠졌다. 라벨 기준으로 좁히거나, 제외 단어 목록을 두는 편이 낫다.

제외해야 할 흔한 단어들: 없음, 있음, 미상, 불명, 전부, 일부, 조사, 현황, 정보, 내역, 목록, 해당, 기재, 권리, 관계, 신고, 배당, 요구, 종기, 보증, 금액, 임대, 차임, 전입, 확정, 일자, 점유, 부분, 기간, 여부, 성명, 주소, 채무자, 소유자, 아님, 임차, 거주, 폐문, 부재, 방문, 안내, 확인, 법원, 번호

### 3. 남길 것

세입자 **유무**, **전입신고일**, **확정일자**, **보증금**은 입찰 판단에 중요하니 그대로 두는 게 좋다. 가릴 건 이름뿐이다.

### 4. 검증

작업 후 물건 상세를 하나 열어 브라우저 네트워크 탭에서 응답 본문을 직접 확인할 것. 화면만 보면 안 된다.

현황조사서가 붙어 있으면서 임대차 관계가 있는 물건을 골라야 의미 있는 테스트가 된다.

## 참고 — 다른 업체는 어떻게 하나

옥션원 같은 유료 경매정보 업체는 개인정보를 가리지 않는다. 아예 `이해관계인검색`(사람 이름으로 물건 찾기) 메뉴까지 두고 있다.

다만 조건이 다르다. 그쪽은 **유료 회원 로그인 뒤에** 정보를 두고, 법인으로서 분쟁 대응 체계를 갖추고 있다. 불특정 다수에게 공개하는 것과 특정 회원에게 제공하는 것은 무게가 다르다.

꽁지맵이 로그인 없는 공개 지도 서비스라면 가리는 게 안전하고, 회원제로 갈 계획이라면 업계 관행 수준으로 완화해도 위험이 크게 낮아진다. **서비스 형태에 따라 결정할 문제다.**

(이 문서를 쓴 사람은 변호사가 아니다. 유료화나 본격적인 공개 전에는 전문가 상담을 권한다.)

## 수집기 쪽 결정 사항

- 수집기는 **원본을 그대로 저장한다.** 가리지 않는다. 나중에 정책이 바뀌어도 되돌릴 수 있게 하기 위함이다.
- 문서 **파일 자체**(PDF 등)는 받지 않는다. 본문 텍스트와 표만 저장한다.
- 따라서 마스킹은 전적으로 웹 쪽 책임이다.
