# 법원경매 수집기 — 이 저장소의 역할

**여기는 데이터를 만드는 쪽이다.** 화면은 만들지 않는다.

법원경매정보(courtauction.go.kr)를 긁어 SQLite에 쌓고, 공개용으로 가공해 R2에 올린다. 그 뒤는 웹 저장소(`~/Documents/court-auction-web`)가 맡는다.

## 두 저장소의 경계

```
[이 저장소]                          [court-auction-web]
법원 사이트 → SQLite → 가공 → R2  →  읽기 → API → 화면
```

**경계는 R2 객체다.** 이쪽은 R2에 무엇을 어떤 모양으로 올릴지 책임지고, 저쪽은 그걸 어떻게 보여줄지 책임진다.

| 올리는 것 | 내용 |
|---|---|
| `v1/snapshot.json.gz` | 진행 물건 요약 (활성 + 좌표). 낙찰은 빠져 있다 |
| `v1/sold.json.gz` | 낙찰 물건 요약. 구조는 snapshot과 완전히 같다 |
| `v1/items/<해시>.json` | 물건별 상세. 낙찰 여부 무관하게 존재 |
| `v1/assets/<sha256>.<확장자>` | 사진 원본 |

payload 모양을 바꾸면 웹이 깨질 수 있다. 필드를 **더하는 것**은 안전하고, 이름을 바꾸거나 빼는 것은 웹 쪽과 맞춰야 한다.

## 세션 규칙

세션이 여럿 붙으면 한 작업대를 나눠 쓰다 엉킨다. 세 가지만 지킨다.

- **푸시는 반드시 사용자에게 물어본다.** `git push`는 승인 없이 하지 않는다. 커밋까지는 알아서 해도 된다.
- **일이 끝나면 `DEVLOG.md` 맨 위에 기록을 남긴다.** 다른 세션이 무슨 일이 있었는지 아는 유일한 통로다. 코드 단위 변경은 `git log`가 갖고 있으니 옮겨 적지 않는다.
- **코드를 고칠 세션은 워크트리로 격리한다.** 로그·DB만 읽는 세션은 원본 폴더를 그대로 쓴다 — 읽기만 하니 안 부딪힌다.

격리된 작업대에는 **`data/`가 따라오지 않는다**(gitignore). 거기서 수집 명령을 돌리면 `--db data/auction.sqlite3` 기본값이 cwd 상대경로라 **에러 없이 빈 DB를 새로 만든다.** 실제 DB는 원본 폴더에만 있다. 수집·DB 조회·데몬 재시작은 **원본 폴더에서만** 한다.

새 워크트리는 `origin/main`에서 갈라져 나온다. **main을 밀린 채로 두면 격리 세션이 낡은 코드에서 시작한다.** 일이 끝난 브랜치는 그때그때 main에 넣는다.

## 구조

| 파일 | 역할 |
|---|---|
| `crawler.py` | 목록 수집. 화면 4종(진행·예정·매각결과·인기도)을 `SearchPageConfig`로 분기 |
| `detail_crawler.py` | 사건번호로 상세·사진·법원문서 수집. 별도 데몬 |
| `store.py` | SQLite 전부. 스키마·업서트·생명주기·푸시 후보 |
| `enrichment.py` | DB 레코드 → 공개 payload. 순수 함수만 |
| `web_push.py` | R2 증분 업로드, 스냅샷 생성 |
| `geocoder.py` | 주소 → 좌표·PNU (브이월드) |
| `land_use.py` `building_registry.py` `official_price.py` `transactions.py` | 공공 API 부가정보 |
| `cli.py` | 명령과 수집 사이클 |
| `web.py` `runners.py` `web_static/` | 맥 로컬 대시보드 (운영 확인용, 웹 서비스와 무관) |

## 자동 실행

launchd가 다섯 개를 띄운다. `launchctl kickstart -k gui/$(id -u)/com.court-auction.<이름>`으로 재시작한다.

| | 하는 일 | 주기 |
|---|---|---|
| `com.court-auction.collect` | 수집 사이클 전체 | 3시간마다 |
| `com.court-auction.collect-details` | 상세·사진·문서 | 쉬지 않고 |
| `com.court-auction.server` | 로컬 대시보드 | 상시 |
| `com.court-auction.logrotate` | 로그를 날짜별로 분리 | 매일 00:05 |
| `com.court-auction.baseline` | 법원 공식 통계와 대조 (G11) | 매달 1일 10:00 |
| `com.court-auction.maintenance-watch` | 사이트 점검이 끝나면 상세 수집 자동 재개 (`data/maintenance_pause` 있을 때만) | 30분마다 |

**법원 사이트 점검 때**(추석·설 등): `touch data/maintenance_pause` 후 상세 수집기만
`bootout` 한다. 끝나면 `maintenance-watch` 가 알아서 워커 1 로 켠다. 목록 수집기는 둬도
된다 — 목록을 못 읽은 사이클은 생명주기 정리를 건너뛴다. 자세한 것은 `CRAWL-LOAD.md`.

수집 사이클 한 바퀴 순서다.

```
목록 수집(진행+예정) → 매각결과 → 인기도 → 토지이용계획
→ 생명주기 정리 → 지오코딩 → 공시가격 → 건축물대장 → 실거래가 → R2 푸시
```

## 반드시 알아야 할 함정

**① 코드를 고쳐도 데몬은 모른다.** `collect-loop`은 한 프로세스가 안에서 사이클을 돈다. 파일을 고쳤으면 `launchctl kickstart -k`로 재시작해야 반영된다. 로그의 `exit=0`은 자식 종료코드가 아니라 그냥 찍는 문자열이다.

**기억에 맡기지 말 것.** 2026-09-18과 09-20에 이틀 연속 밟았고, 두 번째는 **커밋 6분 전에 데몬이 시작해** 훑기 우선순위 수정이 이틀 동안 안 먹었다. 종국 확인이 0건이던 이유다.

```bash
python3 scripts/check_daemon_fresh.py         # 데몬이 지금 코드로 도는지 확인
python3 scripts/check_daemon_fresh.py --fix   # 낡았으면 재시작까지
```

데몬별로 **그 데몬이 실제로 읽는 소스**의 수정 시각과 프로세스 시작 시각을 댄다. 코드를 고친 뒤에는 이것부터 돌린다.

**② payload를 바꿔도 R2엔 안 온다.** `pending_item_pushes()`는 `pushed_at <= MAX(updated_at, …)`인 물건만 후보로 잡는다. **payload 모양이 바뀐 것은 판단 근거에 없다.** 전량 재업로드가 필요하면 `web_sync`의 `kind='item'` 행을 지워 후보로 되돌린다. 스냅샷은 매번 통째로 갈아치우므로 목록만 새 필드가 보이는 상태가 되기 쉽다.

**③ 사이트의 '총 N건'은 화면마다 뜻이 다르다.** 물건상세검색은 물건 수(옥션원과 오차 0으로 확인), 매각예정물건은 일괄매각 필지가 따로 잡힌 목록행 수다. 종료 조건으로 쓰면 진행 화면만 맞다.

**④ 창이 망가지면 조용히 0건이 된다.** 예외가 안 나서 '구간 실패'로도 안 잡힌다. 연속 3구간 0건이면 창을 새로 띄우게 해뒀다(`EMPTY_STREAK_LIMIT`). 법원별 0건 패턴을 보지 않으면 사이클 절반을 잃고도 모른다.

**⑤ 행정구역 개편이 지오코딩을 통째로 깬다.** 법원은 옛 이름을, 지도 API는 새 이름을 쓴다. 지역별 좌표 실패율이 튀면 이걸 의심한다. `MERGED_SIDO`·`RENAMED_SIGUNGU` 참고. 지역 검증은 원본이 아니라 **실제로 던진 쿼리**와 비교해야 한다.

**⑤-1 이건 지오코딩만의 문제가 아니다 — 대조·대기의 기준을 틀리는 것이 이 저장소에서 가장 자주 나는 사고다.** 2026-09-17 하루에 여섯 번 밟았다.

| 무엇을 했나 | 왜 틀렸나 |
|---|---|
| 좌표 지표를 원본 주소와 댔다 | 수집기는 깎은 쿼리와 댄다. 정확한 핀 125건을 틀렸다고 셌다 |
| 실거래를 지번만으로 맞췄다 | 한 시군구에 같은 지번이 동마다 있다. 매칭률이 40%로 부풀었다 |
| `sale_date`를 `'2026-09-25'`와 비교했다 | DB는 `2026.09.14` 점 형식. `.` > `-` 라 지난 기일이 미래로 잡혔다 |
| 로그에서 문구를 세어 추세를 봤다 | 그 문구는 9/10에 **생긴** 것이다. 없던 날을 0으로 읽고 위기라고 보고했다 |
| 표가 차기를 `visible`·`N개월`·`td`로 기다렸다 | 셋 다 데이터 도착 전부터 참. 16건 전부 빈 표를 받고 성공으로 넘어갔다 |
| 지표가 `LIMIT 200`을 정렬 없이 걸었다 | 가장 오래된 것을 집어, 고쳐도 영영 안 움직일 뻔했다 |

전부 **결과가 아닌 것을 결과의 대용으로 삼은 것**이다. 고치기 전에 한 번씩 묻는다.

- **무엇과 대조하나?** 코드가 대조하는 것과 같은 것인가. 다르면 지표가 코드로 못 막는 것을 계속 고발하고, 교정 도구는 안 고쳐지는 것을 영원히 다시 시도한다.
- **무엇을 기다리나?** 읽어 갈 그것인가, 그 근처의 다른 것인가. 조건이 이미 참이면 안 기다린 것과 같다.
- **표본이 고친 코드를 볼 수 있나?** 정렬 없는 `LIMIT`은 옛 데이터만 본다.
- **이 문자열이 그때도 있었나?** 로그·필드 이름은 커밋으로 생기고 사라진다. `git log -S`로 확인한다.

그리고 **관용은 실패를 숨긴다.** `except: pass`로 넘긴 자리는 조용히 0건이 되는 자리다(함정 ④). 관용을 둘 거면 **결과를 세는 수단**을 같이 둔다 — `scripts/status.py`에 결과 기반 탐침을 넣는 것이 이 저장소의 방식이다.

**⑥ 낙찰가는 7일 안에 받는 게 정상 경로다. 다만 영영 못 얻는 건 아니다.** 매각결과 화면은 기일 다음날부터 이레만 보여준다. 그런데 **사건 화면의 '기일 내역' 탭**에는 물건번호별 기일결과가 `매각 (175,900,000원)` 꼴로 **금액까지 영구히** 남는다(실측: '매각' 기일행의 85%에 금액이 붙고, 결과 화면에서 받아둔 낙찰가와 일치). 물건이 낙찰돼 목록에서 사라져도 **사건이 미종국이면** 이 탭은 열린다. 물건 상세 쪽 기일내역에는 금액이 없으니 헷갈리지 말 것. 수집기가 오래 멈춰 생긴 구멍은 사건 화면으로 메울 수 있다.

**다만 '영구히'는 아니다.** 경매사건검색 화면 유의사항: *"부동산은 검색은 되나 **사건이 종국되면 기일관련 정보를 제공하지 않고**, 이후 30일이 지나면 기본정보 만을 제공합니다."* 실측으로 확인했다 — 취하로 종국된 평택지원 2024타경1116은 `기일정보`·`최근입찰결과`가 공란이다. **창은 7일이 아니라 '사건 종국까지'**(낙찰→대금납부→배당이 보통 2~6개월)다. 우리 DB의 `종국결과`가 전수 `미종국`인 것도 같은 이유다. 미종국일 때 걷어둬야 한다(2026-09-16 확인, 아직 코드로는 안 쓴다).


**⑦ 문서에도 시한이 있다.** `기일별검색` 사용설명: *"매각물건명세서는 매각기일이 열리기 **1주 전부터**, 현황조사서와 감정평가서는 매각기일 **2주 전부터 매각기일까지** 조회가 가능합니다."* **기일이 지나면 세 문서 모두 못 받는다.** 낙찰가와 같은 성격이라, 수집기가 한 주 멈추면 그 주 기일 물건의 문서는 영구 손실이다. 덧붙여 **현황조사서는 지목·건물 용도·선박에만 작성된다** — 없는 게 정상인 물건이 있으니 실패로 세지 말 것.

**⑧ 감정평가서는 법원 서버에 없다.** 한국감정평가사협회(`ca.kapanet.or.kr`) 뷰어를 iframe으로 띄우는 구조이고 Adobe Reader 기반이라 본문이 DOM에 안 들어온다. 그래서 `collected` 5.5만 건이 전부 빈 껍데기다(상태가 거짓말을 한다). 뷰어 URL은 98%를 이미 갖고 있다. `DATA-GAPS.md` G6.
## 내보내면 안 되는 것

**감정평가서 원본은 한국감정평가사협회(`ca.kapanet.or.kr`) 것이다.** 그 뷰어 페이지가
*"무단 복제 및 **링크하여** 사용하는 경우 위반사항에 따라 형사처벌을 받을 수 있다"* 고 명시한다.
**링크까지 금지**이므로 우리 payload 에 주소를 실어 내보내는 것 자체가 그 사용에 해당한다.

실제로 2026-09-18 까지 세 갈래로 나가고 있었고 전부 막았다(`enrichment.RESTRICTED_DOCUMENTS`).

| 나가던 것 | 지금 |
|---|---|
| `documents[].metadata_json` (협회 뷰어·PDF 주소) | payload 에서 제외 |
| `documents[].file_path` (받아 둔 파일의 로컬 경로) | payload 에서 제외 |
| `documents[].url` (감정평가서 내려받기) | 감정평가서는 빈 문자열 |

**화면에 보여줄 감정평가 내용은 `property.appraisal_summary` 로 간다.** 그것은 법원이
자기 사이트(물건상세 화면)에 직접 공개하는 `감정평가요항표 요약` 이라 출처가 다르다.

그 안에서도 **사실**(사용승인일·구조·층수·난방·승강기·토지형상·마감재)과 **평가사가 쓴
문장**은 다르다. 사실은 저작권 대상이 아니다. 문단을 통째로 옮기는 것보다 사실을 뽑아
쓰는 쪽이 안전하고 제품으로도 낫다 — 검색·필터·비교가 되기 때문이다.

## 원천적으로 못 하는 것

등기부현황·예상배당표·전입세대열람내역서는 못 얻는다. 이건 그대로다.

**~~공고 전 물건은 홈페이지 어디에도 없다~~ → 2026-09-16 정정. 있다.**

`경매공고 > 배당요구종기공고`(`PGJ141M00&pgjId=142M01`)에 **경매개시결정된 사건**이 뜬다. 화면 유의사항이 명시한다 — *"조회된 사건들은 경매개시 결정이 되었으므로…"*. 매각공고(기일 14일 전)보다 몇 달 이르다.

```
경매개시결정 → 배당요구종기 공고 → (감정평가) → 매각공고(기일 14일 전) → 매각기일
     ↑ 여기서 이미 공개된다                        ↑ 우리는 여기부터 본다
```

실측(서울중앙지방법원 경매1계 한 계): 공고에 뜬 **73건 중 73건(100%)이 우리 DB에 없다.** 예를 들어 2026타경101257은 2026.07.01 개시인데 9/16까지 우리는 모른다. 법원 60개에 계가 여럿이니 **1만 건대 규모**로 추정된다 — 옥션원과의 4천 건 격차보다 크다.

한계는 있다. **사건 단위라 물건별 감정가·최저가가 없다**(감정평가 전이라 당연하다). 목적은 **사건번호를 먼저 확보**해 상세 수집기로 채우는 것이다. 자세한 것은 `DATA-GAPS.md` G15.

매각기일 임박 구간은 여전히 우리가 빠짐없이 가져온다.

## 점검할 때 보는 것

```bash
# 사이클별 요약 — 법원별 0건 패턴까지 봐야 한다
grep -E "물건 수집 완료|매각결과|인기도|토지이용|생명주기|웹 푸시" logs/collect-all.log | tail

# 데몬 상태
launchctl list | grep court-auction
```

로그는 매일 00:05에 잘려 `logs/archive/<이름>-<날짜>.log`로 옮겨진다. **아무것도 지우지 않는다.**
어제 이전 것을 보려면 `logs/` 대신 `logs/archive/`를 뒤진다.

```bash
# 오늘 것만
tail -f logs/collect-details.log

# 지난 기록까지 통째로
grep "웹 푸시" logs/archive/collect-all-*.log logs/collect-all.log
```

숫자를 볼 때는 **활성 / 활성+미래기일 / 활성+좌표** 를 구분한다. 웹 목록에 뜨는 수는 마지막 것이다.

DB 시각은 전부 **UTC**다. KST와 9시간 차이라 "9시간째 멈춘 것처럼" 보이기 쉽다.

## 수집 부하 — 영구 차단이 가장 큰 위험

**데이터가 없으면 서비스가 없다.** 속도는 되찾을 수 있지만 차단은 되돌리기 어렵다.

2026-09-18에 감정평가서 상태를 교정하면서 **3만 건이 한꺼번에 재수집 큐에 들어갔고**, 이틀 만에 세션 거절이 2% → 11%로, 차단 의심이 66 → 212회로 늘었다. `받아둔 적 있는 사건을 '없다'고 함` 은 차단의 전형이다.

- **대량 교정은 나눠서 푼다.** 한 번에 큐를 크게 부풀리지 않는다
- **워커 3 · 지연 3초를 넘기지 않는다**
- **거절률이 5%를 넘으면 과한 것이다** — `python3 scripts/status.py` 의 **'최근 창 세션거절률'**
  로만 본다. `grep -c` 는 개수라 비율이 아니고, 하루 합계는 나쁜 새벽과 좋은 낮을 섞는다.
  공식이 다른 숫자를 비교하지 말 것 — 2026-09-22 에 그렇게 차단 원인을 오판했다.
- 부하를 올렸으면 **거절률을 반드시 다시 잰다.** 처리량이 늘어도 거절이 함께 늘면 이긴 것이 아니다
- **재시작 직후 몇 시간의 좋은 숫자는 허니문이다.** 새벽을 한 번 넘긴 반나절 이상을 보고 판단한다

자세한 것은 `CRAWL-LOAD.md`.

## 참고 문서

- **새 세션은 이 명령부터 돌린다.** 문서보다 이게 먼저다.
  ```bash
  python3 scripts/status.py
  ```
  DB·코드를 직접 재서 **지금 무엇이 끝났고 무엇이 남았는지, 내 세션이 다음에 뭘 할지**를
  출력한다. 사람이 적은 상태를 읽지 않으므로 갱신을 잊어도 틀리지 않는다.
  (`--write` 를 붙이면 `STATUS.md` 로도 남는다. 그 파일은 손으로 고치지 말 것)
- **`sessions/_README.md` — 세션 분담.** 자기 몫의 파일 소유권과 함정을 확인한다
- **`gaps/_INDEX.md` — 격차 16건 색인.** 각 항목이 `gaps/G**.md` 하나씩이고 원인·결과·고치는 법·확인 방법이 붙어 있다
- `DATA-GAPS.md` — 위 16건의 통합본(우선순위표·법원 화면 점검표·세션 분담 근거)
- `SCREENING-REDESIGN.md` — 위험도 재설계 (G14, 별도 세션)
- `SALE-RESULT-GAP.md` — 매각결과 누락 조사(해소됨)

- `PRIVACY-MASKING.md` — 개인정보 마스킹 배경과 정책
- `README.md` — 설치와 명령 사용법
