# 위험도(screening) 다시 설계하기 — 새 세션용

**상태: 설계 안 됨. 지금 것은 못 쓴다.**
2026-09-16에 지금 것이 망가져 있다는 것만 확인했다. **고치는 대신 여기로 분리했다.**

## 왜 여기서 안 고쳤나

점수 공식을 손보는 것은 버그 수정이 아니라 **기준 설계**다.
무엇을 위험으로 볼지, 가중치를 어떻게 줄지는 경매 도메인 판단이 필요하다.
급하게 숫자만 바꾸면 또 다른 엉터리가 된다.

사용자 방침(2026-09-16): *"위험도는 기록하고 새 세션에서 확실한 기준을 다시 잡는 게 좋겠다."*

## 지금 것이 왜 못 쓰는가 (실측)

`enrichment.build_screening`:

```python
score = 50                     # 50에서 시작
if fail_count:        score -= min(fail_count * 4, 24)
if 최저가율 <= 30:     score -= 12   (또는 <=50 이면 -6)
if 지분:              score -= 15
if 주소 불명:          score -= 10
if 가격 없음:          score -= 8
if 종료성 상태:        score -= 20
flags.append("권리확인 필요")   # ← 조건 없이 무조건
risk_level = "낮음" if score >= 65 else "보통" if score >= 40 else "높음"
```

**결함 1 — `낮음`은 도달 불가능하다.**
모든 분기가 `-=` 뿐이고 `+=`가 하나도 없다. 최대 점수가 50인데 `낮음`은 65 이상이다.

**결함 2 — 경고가 전부 울린다.**
활성 400건 무작위 표본:

| flag | 400건 중 |
|---|---|
| `최저가율 N%` | **400 (전부)** |
| `권리확인 필요` | **400 (전부)** |
| `유찰 N회` | 355 |
| `지분 매각 의심` | 23 |
| `주소 확인 필요` | 1 |

| risk_level | 비중 |
|---|---|
| 높음 | 52% |
| 보통 | 48% |
| 낮음 | **0%** |

모든 물건에 뜨는 경고는 정보량이 0이다. 화재경보기가 24시간 울리는 것과 같다.
오히려 변별력 있는 신호(`지분 매각 의심` 23건)를 묻는다.

**이게 지금 웹 화면에 나가고 있다** (`app/items/[id]/page.tsx`, `lib/types.ts`).

## 설계할 때 쓸 수 있는 재료 (이미 갖고 있는 것)

새 기준을 짜기 전에 **우리가 뭘 갖고 있는지부터 보라.** 오늘 조사로 드러난 것들이다.

| 신호 | 출처 | 보유량 |
|---|---|---|
| 지분 매각 여부·지분율 | `property.share` | 지분 물건 전부 |
| 재매각·대금미납·매각불허가 이력 | 사건 화면 `물건상태` | 활성의 97.5% |
| 과거 낙찰가와 깨진 사유 | 사건 기일내역 | `DATA-GAPS.md` G1 |
| 임의/강제/형식적경매 | 사건명 | 전량 |
| 대항력 판단 재료 (최선순위 설정일 + 전입일) | 매각물건명세서 본문 | 2.8만 건 |
| 특수권리 13종 | `special_rights` | 전량 |
| 용도지역 | `land_use` | 73% |
| 좌표 신뢰도 | `coordinate_quality` | 전량 (단 G12 주의) |
| 유찰 횟수·최저가율 | 목록 | 전량 |

**옥션원의 특수물건 분류 40종**도 참고할 것 (`DATA-GAPS.md` G2 주변). 우리는 13종이다.
특히 없는 것: 지분입찰·건물만/토지만 입찰·대지권미등기·선순위 가등기/가처분·
토지별도등기 인수조건·형식적경매 4종.

## 설계 원칙 (제안)

1. **모든 물건에 붙는 것은 flag가 아니다.** `권리확인 필요`는 물건별 정보가 아니라
   서비스 전체의 고지사항이다. 화면 하단 안내문으로 옮긴다.
2. **값과 경고를 구분한다.** `최저가율 64%`는 경고가 아니라 값이고,
   이미 `price.minimum_bid_percent`에 따로 있다.
3. **등급은 실제로 갈려야 한다.** 3단계를 쓸 거면 셋 다 나와야 한다.
   안 나올 거면 2단계로 정직하게 줄인다.
4. **"모르는 것"과 "위험한 것"을 섞지 않는다.** 대항력 판단에 필요한 자료가 없는 것과
   대항력 있는 임차인이 있는 것은 다르다.
5. **근거를 같이 보여준다.** "높음"만 뜨면 사용자는 뭘 조심해야 할지 모른다.

## 먼저 답해야 할 질문

- 이 점수는 **누구를 위한 것인가?** 초보 입찰자의 1차 거름망인가, 전문가의 체크리스트인가.
- **틀렸을 때 어느 쪽이 더 나쁜가?** 위험한 걸 안전하다고 하는 것 vs 안전한 걸 위험하다고 하는 것.
- 옥션원·다른 서비스는 이런 등급을 **주는가, 안 주는가?** 안 준다면 왜 안 줄까.

## 함정

- **웹이 이미 이 값을 쓰고 있다.** 필드를 없애거나 이름을 바꾸면 화면이 깨진다.
  `CLAUDE.md` 경계 규칙대로 **더하는 것은 안전, 빼거나 바꾸는 것은 웹과 합의**다.
- `enrichment.py`는 순수 함수만 두는 곳이다. DB 조회를 끌어들이지 말 것.

---

## 설계 재료 — **우리 자료로 기준을 만들 수 있다** (실측 2026-09-18)

사용자 지적: *"이거 기준 애초에 우리 자료로 정할 수 없는 거 아닌가?"*
→ **정할 수 있다.** 결과가 쌓여 있다. 밖에서 가져올 필요가 없다.

```
auction_sale_results   유찰 70,340 · 매각 12,289 · 변경 6,415
                       각 행에 sale_amount · minimum_bid · appraisal 이 붙어 있다
```

### ① 매각가율 — 물건종류가 위험의 대부분을 설명한다

낙찰가 ÷ 감정가, 표본 9,980건.

| | 값 |
|---|---|
| 하위 10% | 24.0% |
| 25% | 40.0% |
| **중앙** | **57.6%** |
| 75% | 73.2% |
| 상위 10% | 87.1% |

**물건종류별 중앙값** (표본 200건 이상)

| 종류 | 표본 | 중앙 매각가율 |
|---|---|---|
| 아파트 | 1,742 | **77.8%** |
| 다세대 | 1,108 | 61.6% |
| 오피스텔 | 906 | 60.8% |
| 연립·빌라 | 1,176 | 58.8% |
| 상가·근린 | 993 | 55.8% |
| 기타 | 951 | 51.1% |
| 전답 | 888 | 35.9% |
| 임야 | 440 | **34.3%** |

**임야는 감정가의 34%, 아파트는 78%에 팔린다.** 같은 "최저가가 감정가의 50%" 라도
아파트면 시세보다 싼 것이고 임야면 오히려 비싼 것이다. **지금 점수표는 이 차이를
전혀 모른다** — `최저가율 <= 30 이면 -12점` 을 모든 물건에 똑같이 적용한다.

### ② 낙찰까지 몇 번 유찰되나 — 중앙 2회

팔린 물건 11,761건의 유찰 횟수.

| 유찰 | 건수 | 비중 |
|---|---|---|
| 0회(첫 기일 낙찰) | 3,623 | 30% |
| 1회 | 1,800 | 15% |
| 2회 | 2,591 | 22% |
| 3회 | 1,632 | 13% |
| 4회 | 769 | 6% |
| 5회 | 534 | 4% |
| 6회 이상 | 327+ | 2%+ |

**30%가 첫 기일에 팔린다.** 지금 점수표는 `유찰 N회` 를 무조건 감점하는데,
2회까지는 **중앙값 이내**라 그것만으로 위험 신호가 아니다.

### ③ '유찰이 쌓이면 팔릴 확률' 은 **아직 못 낸다** — 분모가 오염돼 있다

단순히 세면 이렇게 나오는데 믿으면 안 된다.

```
유찰 0회 92% · 1회 7% · 2회 43% · 3회 50% · 4회 42% · 5회 55%
```

1회가 7%로 튀는 것은 **그 무리의 대부분이 아직 진행 중**이기 때문이다(결론이 안 난
물건이 '안 팔림' 으로 세어진다). 제대로 내려면 **종결된 물건만** 보거나 생존분석이
필요하다. 지금 숫자를 기준에 쓰면 "1회 유찰이 가장 위험" 이라는 엉터리가 나온다.

### 설계할 때 먼저 답할 것

1. **'위험' 이 무엇인가.** 안 팔릴 위험인가, 비싸게 살 위험인가, 권리상 함정인가.
   지금 점수표는 셋을 한 숫자에 섞어 놓아서 무엇을 말하는지 알 수 없다.
2. **기준선을 종류별로 둘 것인가.** ①이 그래야 한다고 말한다.
3. **'낮음' 이 무엇인가.** 지금은 도달 불가능한 분기다(가점이 하나도 없다).
   중앙값보다 좋은 물건을 '낮음' 이라 부를 것인지부터 정해야 한다.
4. **③을 제대로 낼 것인가.** 종결 물건만 추리면 되지만, 종국결과(G03)가 아직
   전수 0건이라 '안 팔리고 끝난 것' 과 '진행 중' 을 못 가른다. **G03 이 먼저다.**

---

## 2026-09-21 — 코드 선결조건은 풀렸다. 이제 **결정만** 남았다 (세션 B)

지난 절 끝(line 176-177)이 *"③을 제대로 내려면 G03 이 먼저다"* 였다. **그 G03 을
오늘 코드 완료로 확정했다**(`gaps/G03`: 파서·시험 완료, 종결 데이터는 훑기가 채우는 중).
그래서 ③의 막힘은 이제 '코드' 가 아니라 '데이터 대기' 다.

그사이 설계에 쓸 신호도 늘었다:
- **싸게 사는 건지(신호 B)**: 물건종류별 매각가율표(위 ①)에 더해, 실거래 매칭이
  옛 3.5% → **`matched` 23%**(같은 필지·같은 건물)로 올랐다(`gaps/G13`). 종류 평균이
  아니라 **이 물건 시세**와 비교할 여지가 생겼다.
- **권리상 함정(신호 A)**: 대항력 재료(최선순위 설정일 + 전입일)를 현황조사서에서
  뽑아 쓴다(`PRIVACY-MASKING`·임차인 파싱). `special_rights` 13종도 그대로.

**남은 것은 오직 도메인 결정이다.** 아래만 정해지면 구현은 `enrichment.build_screening`
한 함수 교체이고 반나절이면 된다. 결정 없이 숫자만 바꾸면 이 문서가 처음부터 경고한
'또 다른 엉터리' 가 된다 — 그래서 여기서 멈추고 사용자에게 물었다.

### 반드시 사용자가 정할 것 (구현을 여는 열쇠)
1. **점수가 무엇을 뜻하나** — 권리상 함정(A) / 싸게 사는지(B) / 안 팔릴지(C).
   지금은 셋을 한 숫자에 섞어 무의미하다. 하나로 좁히거나, 섞지 말고 따로 보여준다.
2. **몇 단계인가** — 3단계를 쓸 거면 셋 다 나와야 한다(지금 '낮음' 0%). 안 되면 2단계.
3. **틀리면 어느 쪽이 더 나쁜가** — 위험한 걸 안전하다고 vs 안전한 걸 위험하다고.
   이게 임계값을 어디에 둘지를 정한다.

구현·표시는 세션 E(+세션 D 표시). payload 에서 기존 `screening.flags` 를 빼는 것은
웹과 합의해야 하니(경계 규칙), 새 설계는 **더하는 방향**을 우선한다.
