# 수집 부하 — 영구 차단을 피하는 선

**이 저장소의 가장 큰 사업 위험은 법원 사이트에서 영구 차단당하는 것이다.**
데이터가 없으면 서비스가 없다. 속도는 언제든 되찾을 수 있지만 차단은 되돌리기 어렵다.

## 지금 우리가 보내는 부하

| | 설정 | 성격 |
|---|---|---|
| 상세 수집 | 워커 **3** · 지연 **3.0초** · `--loop`(쉬지 않음) | 시간당 약 1,500 요청 |
| 목록 수집 | 지연 2.0초 · 구간 120개 | 사이클당 수천 요청 |
| 사이클 주기 | 3시간마다(실제 4.5시간, 하루 5.1회) | |
| 공고 수집 | 하루 한 번 05:40 · 법원 60 × 경매계 | 회당 약 600 검색 |

**법원 서버가 보기엔 한 IP 에서 24시간 끊이지 않는 트래픽이다.**

## 실측 — 부하를 올리면 바로 반응한다

2026-09-18 에 감정평가서 상태를 교정하면서 **3만 건이 한꺼번에 재수집 큐에 들어갔다.**
그 뒤 이틀치다.

| 날짜 | 시도 | 세션 거절 | 차단 의심 |
|---|---|---|---|
| 09-17 | 9,958 | 265 (2%) | 66 |
| 09-18 | 9,793 | 329 (3%) | 72 |
| 09-19 | 13,244 | 869 (6%) | 230 |
| 09-20 | 7,895 | 897 (**11%**) | 212 |

**시도가 33% 늘자 거절이 5배**가 됐다. 사이트가 우리를 알아보고 밀어내고 있다는 뜻이다.
`받아둔 적 있는 사건을 '없다'고 함(세션 거절)` — 있는 사건을 없다고 답하는 것은
차단의 전형적인 모양이다.

## 지켜야 할 선

1. **한 번에 큐를 크게 부풀리지 않는다.** 대량 교정(상태 되돌리기, 재조회)은
   **나눠서 푼다.** 3만 건을 한꺼번에 풀면 그 부하가 며칠 이어진다.
2. **워커를 3보다 올리지 않는다.** 지연을 줄이지 않는다.
3. **거절률을 본다.** 5%를 넘으면 무언가 과하다는 신호다.
   ```bash
   grep -c "세션 거절" logs/collect-details.log
   grep -c "차단 의심" logs/collect-details.log
   ```
4. **거버너를 끄지 않는다.** 연속 오류 5회에 냉각(60초→최대 900초), 단일 워커 +
   지연 3배로 내려갔다가 연속 성공 10회에 복귀한다. 이게 마지막 안전장치다.
5. **밤에 몰아치지 않는다.** 자정을 넘긴 사이클은 진행 수집을 통째로 날린다(G17).
   헛돌면서 부하만 준다.

## 밀려나기 시작하면 — 단계별 대응

거절률이 오르면 **약한 것부터** 쓴다. 아래로 갈수록 확실하지만 손실도 크다.

| 단계 | 방법 | 효과 |
|---|---|---|
| 1 | 대량 재큐를 멈추고 큐가 빠지길 기다린다 | 근본 원인 제거. 며칠 |
| 2 | 워커 3 → 2 (동시성↓) | **요청 간격(지연)보다 이게 지렛대다** |
| 3 | 워커 2 → 1 (보수 모드와 같음) | 더 느리지만 발자국이 최소 |
| 4 | **수집을 하루이틀 멈춘다** | 가장 확실. 손님이 끊기면 사이트가 우리를 잊는다 |

**4번(중단)은 겁낼 필요 없다.** 잃는 것은 **신선도**뿐이다 —
- 매각결과는 사이클마다 7일치를 통째로 다시 긁으니 이틀 늦어도 창 안(함정 ⑥)
- 문서는 기일 2주 전부터 열리니 이틀은 여유(함정 ⑦)
- 낙찰가만 7일 창이라, **중단은 낙찰 없는 조용한 날(주말 등)에 건다**

중단하는 법:
```bash
launchctl bootout gui/$(id -u)/com.court-auction.collect
launchctl bootout gui/$(id -u)/com.court-auction.collect-details
# 다시 켜기
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.court-auction.collect.plist
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.court-auction.collect-details.plist
```

## 부하를 올려야 할 때

값이 분명할 때만 올리고, **올린 뒤 거절률을 반드시 다시 잰다.**

```bash
# 바꾸기 전 기준선
grep -o "완료 [0-9]*개" logs/collect-details.log | awk '{s+=$2} END {print "완료", s}'
grep -o "실패 [0-9]*개" logs/collect-details.log | awk '{s+=$2} END {print "실패", s}'
```

**처리량이 늘어도 거절이 함께 늘면 이긴 것이 아니다.** 실패한 시도도 법원 서버는 처리한다.

## 기록

- **2026-09-20 17:32**: 지연 2.0 → 3.0. 원인은 09-18 의 3만 건 일괄 재수집.
- **2026-09-20 23:15**: 지연 3.0 은 **효과 없었다.** 5.5시간 재보니 실패율 45% → 44%,
  거절 60건/h → 54건/h 로 사실상 그대로. 지연(요청 간격)은 지렛대가 아니었다.
  거절의 **76%가 '있는 활성 사건을 없다고 함'** = 소프트 차단이고 연속 오류로
  몰린다(버스트). 그래서 **워커 3 → 2** 로 내렸다(동시성이 진짜 지렛대).
  기준선: 완료 9,378 · 실패 7,737 · 거절 1,320 · 차단 318. **효과는 몇 시간 뒤 재측정.**

- **2026-09-21 00:06**: **차단으로 확정.** 방금 거절당한 활성 사건 3건을 하나씩
  천천히 검색하니 3건 다 정상으로 나왔다 — 사건은 멀쩡하고 몰아칠 때만 거절이다.
  지연·워커 축소가 안 통한 이유다(문턱이 per-request 가 아니라 총량/동시성).
  **상세 수집을 멈추고 목록 수집만 남겼다.** 상세 재수집 surge 가 법원을 때리는
  주범이고, 목록은 새 물건 발견이라 살려 둔다. 재개는 `scripts/resume_details.sh`.
  대가: 만료 임박 종국 훑기 ~16건은 놓칠 수 있다(차단 위험보다 작다).

- **2026-09-21 11:24**: **진짜 원인은 셸 버그였다.** `run_detail_collector.sh` 의
  `--workers 2 --delay 3.0 --loop` 이 exec 인자 줄 사이 주석에 잘려 **한 번도 적용된
  적이 없었다**(데몬은 기본 워커 3·지연 1.5·loop 없음으로 돎). '지연·워커 축소가
  효과 없다' 던 측정이 전부 깨진 설정끼리 비교였다. 스크립트를 고치고 재개하니:

  | | 실패율 | 세션거절(누적) | 차단의심 |
  |---|---|---|---|
  | 깨진 설정(워커3·지연1.5) | 45% | 1,388 | 330 |
  | 고친 설정(워커2·지연3, 34분) | **2%** | +3 | +0 |

  **부하 축소는 통한다 — 적용만 됐으면 됐다.** 34분은 얇으니 2~3시간 더 지켜본다.

> **교훈**: 지연을 올리면 나아질 거라고 **가정하고 바꿨는데 틀렸다.** 게다가 그
> '바꾼 것' 이 프로세스에 실제로 도달했는지도 확인 안 했다 — `ps` 로 인자를 봤으면
> 셸 버그를 첫날 잡았다. **바꾼 뒤: (1) 프로세스 인자를 확인하고 (2) 결과를 잰다.** 부하 문제도
> '무엇이 지렛대인지' 를 먼저 재야 한다 — 요청 간격이 아니라 동시 세션 수였다.
> 바꾼 뒤 반드시 다시 잰다.

---

## 2026-09-22 01:40 — ~~데몬을 동시에 재시작했다가 차단을 재발시켰다~~ → **2026-09-23 정정: 재시작은 원인이 아니었다** (세션 B)

> **정정 (2026-09-23).** 아래 경위의 인과는 **틀렸다.** 재시작 시각을 확인하지 않고
> 인과를 단정했고, 공식이 다른 숫자 셋을 섞어 비교했다. 같은 공식·독립된 두 출처로
> 다시 재니 **붕괴는 재시작 2시간 반 전인 15시에 이미 시작됐다.**
>
> | 09-21 (KST) | 상세 성공/시간 (DB `detail_collected_at`) | 패스별 실패율 (로그 집계) |
> |---|---|---|
> | 11~14시 (11:27 재개 직후) | 251 · 505 · 470 · 404 | 3~6% |
> | **15~16시** | **6 · 3** ← 붕괴 | 20~44% |
> | 17시 (17:27 재시작) | 12 | — |
> | 18~23시 | 51 · 103 · 55 · 53 · 229 · 204 (오히려 회복) | 13~36% |
> | 09-22 00~01시 | 42 · 68 | 69~86% |
>
> - 11:27~17:27 상세 프로세스는 한 번도 안 바뀌었다(pid 연속). 악화는 그 **한 프로세스
>   안에서** 로그 L1681 패스부터 시작됐고, 재시작 지점은 L2874 다.
> - collect 사이클과 겹친 시간(13:22~15:16, 17:27~19:05, 22:06~23:43)에 상세가 오히려
>   멀쩡하거나 회복했다. **'목록·상세 동시 부하 → 차단' 은 데이터가 지지하지 않는다.**
> - **'3% → 90%' 는 공식이 다른 숫자였다.** 3% = 진입 대비 실패 줄, 90% = '수집 완료' 줄
>   수(= 패스 수!) 대비 실패 줄. 같은 공식(패스 집계)으로는 25~44% 였다.
> - **09-21 13:59 의 '3% 로 2.5시간 유지 → 회복 확정' 은 재시작 직후 허니문이었다**
>   ([[detail-crawler-session-rot]] 이 미리 경고한 그것). 1시간 뒤 무너졌다. 워커 2·지연 3
>   이 **지속** 가능하다는 증거는 사실 없다.
>
> **데이터가 가리키는 것:** 법원은 (1) 몇 시간 동안 물량이 쌓이면 조이기 시작하고
> (워커 2 로 시간당 400~500건 → 3.5시간 뒤 붕괴), (2) **새벽(00~05시)엔 부하가 낮아도**
> 거절이 늘었다(워커 1 에서도). 01:40 정지 자체는 그 새벽 창이 실제로 나빴으니
> 보수적 판단으로 틀리지 않았다 — **이유가 틀렸다.**

<details><summary>원래 적었던 경위 (틀린 인과 — 기록으로 남김)</summary>

~~교훈: 로깅 정도의 변경으로 회복 중인 수집 데몬을, 그것도 여럿 동시에 재시작하지 말 것.~~

경위. 09-21 자가재시작 로그(문구만 바꾸는 변경)를 반영하려고
`check_daemon_fresh --fix` 로 **collect·collect-details·server 셋을 17:27 에 동시
재시작**했다. ~~collect 를 `kickstart -k` 하면 즉시 목록 수집 사이클이 돈다. 그게
collect-details 재시작과 겹쳐 동시 부하 스파이크가 됐고, 2.5시간 3%로 안정돼 있던
상세 수집이 재시작 직후부터 6시간 넘게 ~90% 차단으로 뒤집혔다.~~
(아카이브 17:27~00:05: 수집완료 35 vs 물건목록없음 495·거절 221 — **'수집완료 35' 는
패스 수였다.** 물건 수가 아니다.)

</details>

계속 두드리면 차단이 굳으므로 **상세 데몬을 정지**했다
(`launchctl bootout gui/$(id -u)/com.court-auction.collect-details`).

### 다음부터 (2026-09-23 정정 반영)
- **재시작 직후 몇 시간의 좋은 숫자로 '회복' 을 선언하지 않는다.** 09-21 에 두 번
  (11:24 '34분 2%', 13:59 '2.5시간 3%') 그랬고 둘 다 허니문이었다. **최소 반나절,
  새벽을 한 번 넘긴 뒤** 판단한다.
- 무엇이 원인인지 말하기 전에 **그 일이 일어난 시각부터 잰다.** 원인 후보의 시각과
  악화 시작 시각을 나란히 놓는다 — DB `detail_collected_at` 시간대별 건수가 독립 증거다.
- 부하는 **한 가지 공식으로만** 본다: `python3 scripts/status.py` 의 '최근 창 세션거절률'
  (기준 5%). `grep -c` 로 센 개수, '수집 완료' 줄 수, 하루 합계를 서로 비교하지 않는다.
- 재시작은 여전히 **하나씩·필요할 때만** 한다(일반 위생). 다만 그게 차단 원인이라는
  증거는 없다 — 조심할 것은 **지속 물량**이다.
- 되살리기: `zsh scripts/resume_gentle.sh` (워커 1·지연 5, collect 는 안 건드림).
  워커 2 로 올리는 건 새벽을 넘긴 반나절 이상의 창을 보고 사람이 정한다.

---

## 법원 사이트 점검 때 (2026-09-24 추석 3일 점검에서 정함)

점검 중엔 수집기가 여는 앱 주소(`/pgj/index.on`)가 `error.html` 로 넘어가며 "시스템 작업"
을 보여준다. **첫 화면(`/`)은 점검 중에도 200 에 제목까지 멀쩡해 보인다** — 그걸로 판단하면
점검 중에 '끝났다' 고 착각한다. 공지에 끝나는 시각은 안 적혀 있었다.

| | 그대로 두면 | 그래서 |
|---|---|---|
| 상세 수집기 | 사건마다 20초 타임아웃 → 물건별 재시도 간격이 늘어 **점검 뒤 서류 수집이 늦어진다** | 멈춘다(`bootout`) |
| 목록 수집기 | 목록 0건 · 생명주기 정리가 '기일+3일' 로 유찰 물건을 종결해 사이트에서 사라지게 한다 | 둔다 — 목록을 못 읽은 사이클(본 수 < 활성 10%)은 생명주기를 건너뛴다. R2 푸시는 계속 |
| 공공 API·R2 푸시 | 법원과 무관 | 계속 |

**멈추는 법:** `touch data/maintenance_pause` → `launchctl bootout gui/$(id -u)/com.court-auction.collect-details`

**되살리는 법(자동):** `com.court-auction.maintenance-watch` 가 30분마다 앱 주소를 한 번 보고,
점검이 끝났으면 ① 점검 중 타임아웃으로 실패 표시된 물건의 재시도 시각을 비우고 ② `resume_gentle.sh`
(워커 1·지연 5)로 켜고 ③ 표시 파일을 지운다. 기록은 `logs/maintenance-watch.log`. 예약 작업을
안 쓴 것은 사람 없는 시각에 승인 창에서 멈춘 적이 있어서다(09-22).

**점검 직후가 중요하다.** 기일 1~2주 전부터 열리는 서류(함정 ⑦)는 기일이 지나면 영구 손실이다.
2026-09-24 점검 뒤 첫 기일 09-28~30 에 6,830건이 잡혀 있었다.
