# 수집 부하 — 영구 차단을 피하는 선

**이 저장소의 가장 큰 사업 위험은 법원 사이트에서 영구 차단당하는 것이다.**
데이터가 없으면 서비스가 없다. 속도는 언제든 되찾을 수 있지만 차단은 되돌리기 어렵다.

## 지금 우리가 보내는 부하

| | 설정 | 성격 |
|---|---|---|
| 상세 수집 | 워커 **3** · 지연 **3.0초** · `--loop`(쉬지 않음) | 시간당 약 1,500 요청 |
| 목록 수집 | 지연 2.0초 · 구간 120개 | 사이클당 수천 요청 |
| 사이클 주기 | 3시간마다(실제 4.5시간, 하루 5.1회) | |
| 공고 수집 | 하루 한 번 05:40 · 법원 60 × 경매계 | 회당 약 600 검색 |

**법원 서버가 보기엔 한 IP 에서 24시간 끊이지 않는 트래픽이다.**

## 실측 — 부하를 올리면 바로 반응한다

2026-09-18 에 감정평가서 상태를 교정하면서 **3만 건이 한꺼번에 재수집 큐에 들어갔다.**
그 뒤 이틀치다.

| 날짜 | 시도 | 세션 거절 | 차단 의심 |
|---|---|---|---|
| 09-17 | 9,958 | 265 (2%) | 66 |
| 09-18 | 9,793 | 329 (3%) | 72 |
| 09-19 | 13,244 | 869 (6%) | 230 |
| 09-20 | 7,895 | 897 (**11%**) | 212 |

**시도가 33% 늘자 거절이 5배**가 됐다. 사이트가 우리를 알아보고 밀어내고 있다는 뜻이다.
`받아둔 적 있는 사건을 '없다'고 함(세션 거절)` — 있는 사건을 없다고 답하는 것은
차단의 전형적인 모양이다.

## 지켜야 할 선

1. **한 번에 큐를 크게 부풀리지 않는다.** 대량 교정(상태 되돌리기, 재조회)은
   **나눠서 푼다.** 3만 건을 한꺼번에 풀면 그 부하가 며칠 이어진다.
2. **워커를 3보다 올리지 않는다.** 지연을 줄이지 않는다.
3. **거절률을 본다.** 5%를 넘으면 무언가 과하다는 신호다.
   ```bash
   grep -c "세션 거절" logs/collect-details.log
   grep -c "차단 의심" logs/collect-details.log
   ```
4. **거버너를 끄지 않는다.** 연속 오류 5회에 냉각(60초→최대 900초), 단일 워커 +
   지연 3배로 내려갔다가 연속 성공 10회에 복귀한다. 이게 마지막 안전장치다.
5. **밤에 몰아치지 않는다.** 자정을 넘긴 사이클은 진행 수집을 통째로 날린다(G17).
   헛돌면서 부하만 준다.

## 부하를 올려야 할 때

값이 분명할 때만 올리고, **올린 뒤 거절률을 반드시 다시 잰다.**

```bash
# 바꾸기 전 기준선
grep -o "완료 [0-9]*개" logs/collect-details.log | awk '{s+=$2} END {print "완료", s}'
grep -o "실패 [0-9]*개" logs/collect-details.log | awk '{s+=$2} END {print "실패", s}'
```

**처리량이 늘어도 거절이 함께 늘면 이긴 것이 아니다.** 실패한 시도도 법원 서버는 처리한다.

## 기록

- **2026-09-20**: 지연 2.0 → **3.0**. 거절률 45%(완료 6,770 · 실패 5,606)에서 출발해
  효과를 재는 중. 원인은 09-18 의 3만 건 일괄 재수집이었다.
