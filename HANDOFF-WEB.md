# 웹 인수인계 — 수집기가 새로 싣는 것들 (2026-09-17)

**받는 쪽**: `~/Documents/court-auction-web` (세션 D)
**주는 쪽**: 이 저장소(수집기), 세션 B

## 한 줄

**데이터는 다 있다. 화면이 안 쓴다.** 새로 긁을 것도, 기다릴 것도 없다. 그리기만 하면 된다.

## 경계

R2 객체가 경계다. 이쪽은 **무엇을 어떤 모양으로 올릴지**, 그쪽은 **어떻게 보여줄지** 책임진다.

| 객체 | 내용 |
|---|---|
| `v1/snapshot.json.gz` | 진행 물건 요약 — **목록·지도가 쓰는 것** |
| `v1/sold.json.gz` | 낙찰 물건 요약, 구조는 snapshot과 동일 |
| `v1/items/<해시>.json` | 물건별 상세 |

---

## 1. 새로 실린 것 — 목록(snapshot)에 전부 들어 있다

기준: 2026-09-17, 목록 물건 **32,432건**.

### ① `auction.deposit` — **돈이 걸린 것. 이것부터 하면 된다**

```json
"deposit": {"amount": 1711400, "rate": 0.2}
```

`rate`는 **최저매각가격 대비 매수신청보증금 비율**이다. 보통 0.1(10%)인데,
**0.1을 넘으면 화면에서 경고해야 한다.** 모르고 10%만 준비해 가면
**입찰이 그 자리에서 무효**가 된다.

| 비율 | 물건 수 |
|---|---|
| 0.1 (보통) | 31,794 |
| **0.2** | **575** |
| **0.3** | **39** |
| 값 없음(`null`) | 24 |

> ### ⚠ 비율을 추정하지 말 것
> "재매각이면 20%"는 **참이 아니다.** 실측하면 재매각 물건 중 20%가 302건,
> 10%가 78건, **30%가 37건**이다. 규칙으로 계산하면 27%에서 틀리고,
> 30%짜리를 20%로 안내하면 그것도 무효다.
> **법원이 적어 둔 `amount`와 `rate`를 그대로 쓸 것.**

### ② `auction.resale` — 재매각 이력

```json
"resale": {"is_resale": true, "reason": "대금미납", "status_flow": "매각준비 -> … -> 대금미납"}
```

활성 **442건**(대금미납 388 · 매각불허 74). 한 번 낙찰됐다 깨진 물건이다.
`status_flow`는 법원이 적은 물건상태 전체 흐름이라 그대로 보여줘도 읽을 만하다.

**보증금과 겹치지만 같지 않다.** 따로 보여줘야 한다 — 재매각인데 보증금이
안 오른 물건도, 재매각 표시가 없는데 보증금이 오른 물건도 있다.

### ③ `auction.case_type` — 경매 종류

```json
"case_type": "부동산강제경매"
```

| 값 | 물건 수 | 뜻 |
|---|---|---|
| 부동산임의경매 | 19,527 | 담보권 실행(근저당 등) |
| 부동산강제경매 | 12,057 | 집행권원(판결 등) |
| 공유물분할을위한경매(+부동산) | 523 | **형식적경매** |
| 청산을위한형식적경매 | 211 | **형식적경매** |

근거가 다르면 취소 가능성과 권리 관계가 다르다. 권리 분석이 갈리는 구분이다.

### ④ `auction.parties` — 당사자 구성

```json
"parties": {
  "counts": {"임차인": 2, "교부권자": 3, "가압류권자": 3, "채권자": 1},
  "creditor_individual": false
}
```

법원은 당사자 **이름**을 가린다(`광OOOOOOO`). 하지만 **구분은 안 가린다.**
거기에 권리 분석 신호가 그대로 있다.

| 당사자 | 있는 물건 | 비율 | 뜻 |
|---|---|---|---|
| 교부권자 | 29,885 | **92%** | 조세 체납 |
| 가압류권자 | 15,575 | 48% | 가압류 |
| 임차인 | 15,519 | 48% | 대항력 |
| 근저당권자 | 14,561 | 45% | 담보 |

> ### ⚠ 교부권자는 변별력이 없다
> **92%에 있다.** "조세 체납 있음"을 위험 표시로 쓰면 **아무것도 안 걸러진다.**
> 쓰려면 **수**를 보거나 압류권자와 겹치는지를 봐야 한다.

`creditor_individual`은 채권자가 개인인지(`true`) 기관인지(`false`)다.
이름 모양으로 판별한다 — 개인은 성 한 자 + 가림 두 자(`이OO`). 활성 기준 개인 13.8%.
**은행별 분류는 못 한다** — 이름이 첫 글자만 남아 은행·캐피탈·공공을 가릴 방법이 없다.

### ⑤ `auction.closing` — 물건이 왜 사라졌나

```json
"closing": {"result": "취하", "date": "2026.09.09"}
```

취하·기각·취소는 목록 상태로는 알 수 없다. 사건 화면에만 있다.

**지금은 전부 `미종국`이다.** 당연하다 — 여태 살아 있는 물건만 상세를 받아왔다.
2026-09-17에 사라진 물건을 다시 훑는 길을 열었고(약 1만 건 대기), **하루쯤 지나면
취하·기각이 처음으로 채워진다.** 그때부터 값이 생긴다.

### ⑥ `auction.special_rights` — 새 값 셋

웹이 딱지로 쓰는 배열이다. 이번에 세 가지가 늘었다.

| 딱지 | 물건 수 | 비고 |
|---|---|---|
| `지분매각` | **2,118** | 전에도 `property.share`엔 있었지만 이 배열엔 없었다 |
| `형식적경매` | **805** | 공유물분할·청산 포함 |
| `재매각` | **725** | 전에는 비고에 글자가 있는 419건만 |

기존 값(`유치권`·`법정지상권`·`맹지`·`농지취득자격증명` 등)은 그대로다.

### ⑦ `map.coordinate_quality` / `coordinate_source` — 전부터 있었는데 안 쓰고 있다

2026-09-17에 **잘못 찍힌 핀 693건을 고치면서 핀에 등급이 생겼다.**

| `coordinate_source` | 물건 수 | 뜻 | 화면 |
|---|---|---|---|
| `address` | 31,818 | 지번이 정확히 잡힘 | 그냥 핀 |
| `building` | 603 | 건물명 장소검색 | **근사 표시** |
| `lot` | 6 | 부번이 지도에 없어 **본번**으로 잡음(옆 필지) | **근사 표시** |
| `district` | 5 | 지번이 아예 없는 물건(어업권·건설기계) → **리 단위** | **근사 표시** |

`quality`가 `verified`가 아니면 **정확한 핀과 다르게 그려야 한다.** 지금은 똑같이 보인다.

> **`lot`·`district` 핀은 PNU가 비어 있다.** 옆 필지의 공시가격·실거래를 붙이면
> 남의 땅 정보가 된다. 이 둘에는 시세 비교를 붙이지 말 것.

---

## 2. 언제부터 보이나

| 어디 | 언제 |
|---|---|
| `snapshot.json.gz` (목록·지도) | **다음 푸시부터 전부.** 스냅샷은 매번 통째로 다시 만든다 |
| `items/<해시>.json` (상세) | **그 물건이 갱신될 때 따라간다.** 전량은 아니다 |

상세 파일을 전량 맞추려면 763MB 재업로드다. 급하지 않아 안 했다.
목록에 다 실려 있으니 **목록 기준으로 화면을 짜면 기다릴 것이 없다.**

---

## 3. 아직 안 되는 것 — 수집기 쪽 결정이 필요하다

**좌표가 없는 물건은 스냅샷에 아예 들어가지 않는다.** `lat IS NOT NULL` 필터가
목록 생성 단계에 걸려 있다. 그래서 **'위치 미상'으로 목록에 띄우고 싶어도 데이터가 안 간다.**

지금 활성 **152건**이 여기 해당한다(대부분 좌표를 곧 얻는 대기 물건이고,
**6건은 주소가 지도에 아예 없어서 영영 못 얻는다**).

띄우려면 수집기 쪽에서 필터를 고쳐야 한다 — 목록 건수와 지도 동작이 같이 바뀌므로
**양쪽이 합의하고 하는 게 맞다.** 필요하면 말해 달라.

---

## 4. 바꾸면 안 되는 것

payload에 **필드를 더하는 것은 안전**하다. **이름을 바꾸거나 빼는 것은 양쪽이 맞춰야 한다.**
위 필드 이름은 이미 올라가고 있으므로 그대로 읽으면 된다.

---

## 5. 직접 확인하는 법

수집기 저장소(원본 폴더)에서 한 건을 통째로 찍어볼 수 있다.

```bash
PYTHONPATH=src .venv/bin/python -c "
from court_auction_crawler.store import AuctionStore
from court_auction_crawler.enrichment import public_auction_summary
import json
s = AuctionStore('data/auction.sqlite3')
rows = [r for r in s.iter_public_rows(active=True)]
print(json.dumps(public_auction_summary(rows[0])['auction'], ensure_ascii=False, indent=1))
"
```

R2에 올라간 실물을 보려면 `v1/snapshot.json.gz`를 받아 `items[0].auction`을 열면 된다.

---

## 6. 근거 문서

| 무엇 | 어디 |
|---|---|
| 좌표 등급이 왜 생겼나 | `gaps/G12-엉뚱한좌표.md` |
| 보증금·재매각 | `gaps/G04-재매각-물건상태.md` |
| 사건명 | `gaps/G07-사건명-임의강제.md` |
| 당사자 | `gaps/G10-당사자-실명과-낙찰자출처.md` |
| 종국결과 | `gaps/G03-종국결과-취하기각.md` |
| 웹이 안 쓰는 payload 전체 목록 | `gaps/G13-웹이-안쓰는payload.md` |
| 무슨 일이 있었나 | `DEVLOG.md` (2026-09-17) |
