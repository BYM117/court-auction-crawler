# 매각결과 2% 누락 — 미해결

**상태: 원인 미규명. 새 세션이 이어받을 것.**
2026-09-16 조사 기록. 코드는 아직 하나도 안 고쳤다.

## 왜 이걸 쫓아야 하나

낙찰 **금액**은 법원이 기일 다음날부터 **7일만** 보여준다. 놓치면 그 기일의 낙찰가는
영영 얻을 수 없다. 우리가 가진 것 중 유일하게 시한이 걸린 데이터다.

유찰 여부·유찰 이력은 상세 페이지의 기일내역에 영구히 남으므로 급하지 않다.
**기일내역에는 금액이 없다**(확인함 — '매각'/'유찰' 글자만 나온다).
그래서 이 2%는 "나중에 메우면 되는 것"이 아니다.

## 지금까지 확인된 숫자

기일이 지나 종결됐다가 **새 기일을 받아 되살아난** 물건만 본다. 되살아났다는 건
그날 기일이 실제로 열렸다는 증거다(진행상태 유찰 N→N+1, 최저매각가격 하락으로 확인).

| | 건수 |
|---|---|
| 되살아남 + 결과행 있음 | 16,419 |
| 되살아남 + 결과행 없음 | **336 (2.0%)** |

- 336건 중 일부는 기일 변경(유찰 횟수·최저가 그대로)이라 진짜 놓침은 더 적다.
  8/31 코호트 표본: 결과없음 43건 중 31건만 유찰 확정.
- 날짜별 0~4%, 법원별로도 고르다(최다가 서울중앙 48건인데 서울중앙이 큰 법원이다).
  **특정 사이클 실패도, 특정 법원 문제도 아니다.**

**매각된 물건의 누락률은 우리 데이터만으로 검증이 안 된다.** 결과 화면이 유일한
증거라서 놓치면 놓쳤는지도 모른다. 같은 화면의 같은 행이니 비슷할 것으로 보지만
확인된 바 없다. **이게 정작 제일 중요한 숫자인데 아직 못 쟀다.**

## 아니라고 확인한 것 (다시 파지 말 것)

**① 페이지 잘림 아님.** 통영지원은 31사이클 내내 176건 → (9/14 기일 뒤) 207건으로
고정이다. 잘린다면 사이클마다 흔들려야 한다. 어떤 법원도 페이지 블록 경계
(페이지크기 40 × 블록 10 = 400) 근처가 아니다. 최대가 서울남부 611건.

**② 키 매칭 아님.** 9/4 이후 고아 결과행(=items에 없는 결과행) 0건.
그 전 108건은 caseno 수정 전 잔재다.

**③ 법원 통째 누락 아님.** 31사이클 전부 0건이던 해남·여주·남원·거창·강릉을
파봤더니 **그 기간에 실제로 기일이 없었다.** 법원 기일 달력에 몇 주씩 공백이 있다.
(여주 8/26→9/16, 거창 8/21→10/16, 남원 8/31→10/12, 안산 9/03→9/17)

**④ 더 자주 긁어서 될 일 아님.** 지금 7일 창 동안 56번(하루 8회 × 7일) 긁는데도
2%가 남는다. 결과는 기일 다음날 오전에 몰려 들어오고 그 뒤 증가분은 +10 수준이다.
**빈도를 올리는 건 해법이 아니다.**

## 남은 가설 — 유망한 순서

### H1. 파서가 받은 행을 조용히 버린다 ★ 여기부터 볼 것

`store.record_sale_results`가 이렇게 생겼다.

```python
if not (case_no and common["item_no"] and sale_date):
    continue          # 조용히 버림
...
return {"received": len(rows), "saved": len(prepared), ...}
```

**`received - saved`가 버려진 행 수인데, 계산만 되고 아무 데도 안 찍힌다.**
`cli.run_result_cycle`의 `save_court`는 `saved`만 출력한다.

일괄매각·병합 사건에서 물건번호 칸이 비거나 합쳐져 나오면 통째로 사라진다.
버려진 행은 DB에 안 남으니 지금은 사후 확인이 불가능하다.

**할 일**: `save_court`에 `received - saved`를 찍고, 0이 아니면 그 행의 raw를
파일로 떨군다. 한 사이클만 돌려보면 답이 나온다. 코드 세 줄이다.

### H2. 페이지 전환이 한 페이지를 건너뛴다

`crawler._go_next_page`는 클릭 후 `_wait_for_results_change`를 **10초**만 기다린다.
타임아웃이 나도 `_results_token`이 바뀌었으면 True를 돌려준다. 느린 응답에서
옛 내용을 한 번 더 읽고 다음 클릭이 두 페이지를 건너뛰면, 중복은 `seen`이 걸러주고
**건너뛴 페이지는 아무도 모른다.**

**할 일**: 결과 화면에서 페이지별 수집 건수를 로그에 남긴다. 마지막 페이지가 아닌데
40건이 아닌 페이지가 나오면 그 자리다.

### H3. 매각결과 화면에는 '수집 부족 의심' 경고가 아예 안 걸린다 ★ 사각지대

`crawler._collect_result_pages`:

```python
counts_items = config.mode == "current"   # 진행 화면에서만 True
```

그래서 매각결과 화면(`mode="result"`)은 **총 N건과 대조하지 않고, 부족해도 경고가
안 뜬다.** 지금 잘리고 있지 않더라도(①에서 확인) 앞으로 잘리면 조용히 잃는다.

단, CLAUDE.md 함정 ③대로 화면마다 '총 N건'의 뜻이 다르다. 매각결과 화면의 총계가
물건 수인지 목록행 수인지 **먼저 확인해야** 경고 기준으로 쓸 수 있다.

### H4. 사이트가 원래 일부를 안 준다

상세 수집기에서 이미 겪은 성질이다(DEVLOG 9/15: "사이트가 원래 10~15%는 안 준다").
결과 화면에도 같은 일이 일어난다면 우리가 할 수 있는 게 없다.
**H1~H3을 다 배제한 뒤에야 이 결론을 내릴 것.** 먼저 결론 내리지 말 것.

### H5. 매각허가 이전 상태는 결과 화면에 안 뜬다

낙찰 뒤 매각결정기일(보통 기일 +7일)에 허가/불허가가 갈린다. 그 사이 물건이
결과 화면에서 빠져 있다가 나중에 들어오는지 확인 안 됐다. 그렇다면 **기일 다음날
한 번만 긁는 것은 위험**하고, D+8 이후에 한 번 더 긁어야 한다.

**할 일**: 같은 기일의 결과 건수를 D+1부터 D+7까지 날짜별로 기록해 증가 여부를 본다.

## 다음 세션이 할 일 (순서대로)

1. **H1 계측** — `received - saved`를 로그에 찍고 버려진 행을 덤프한다. 한 사이클.
2. **H2 계측** — 결과 화면 페이지별 건수를 로그에 남긴다. 같은 사이클에 같이.
3. 1~2에서 안 나오면 **H5** — 기일별 결과 건수의 날짜별 추이를 며칠 쌓아 본다.
4. **매각 물건의 누락률**을 재는 방법을 찾는다. 현재 우리 데이터로는 불가능하다.
   (옥션원 같은 외부 대조군이 필요할 수 있다)
5. 다 배제되면 H4로 닫고, 그때 **DEVLOG에 "여기까지가 한계"로 명시**한다.

## 재현용 조회

되살아난 물건 기준으로 누락률을 다시 재는 스크립트. `auction_events`의
`deactivated` 이벤트와 현재 상태를 맞춰 본다.

```python
import json, sqlite3, collections
DB = "data/auction.sqlite3"
con = sqlite3.connect(DB); con.row_factory = sqlite3.Row
res = {(r["item_key"], r["sale_date"]) for r in
       con.execute("SELECT item_key, sale_date FROM auction_sale_results")}
cur = {r["item_key"]: (r["is_active"], r["sale_date"]) for r in
       con.execute("SELECT item_key, is_active, sale_date FROM auction_items")}

miss = ok = 0
for r in con.execute("SELECT item_key, new_json FROM auction_events "
                     "WHERE event_type='deactivated' AND created_at >= '2026-08-25'"):
    n = json.loads(r["new_json"]); sd = n.get("매각기일") or ""
    c = cur.get(r["item_key"])
    if not c or not sd: continue
    active, now_date = c
    if not (active and now_date > sd):   # 새 기일 받고 되살아난 것만 = 기일이 실제로 열림
        continue
    if (r["item_key"], sd) in res: ok += 1
    else: miss += 1
print(f"확보 {ok} / 놓침 {miss} = {100*miss/(ok+miss):.1f}%")
```

법원별 사이클간 결과 건수 변동(잘림이면 흔들린다):

```bash
for f in logs/archive/collect-all-*.log logs/collect-all.log; do
awk '/자동 수집 시작/{d=$4" "$5}
/^\[[0-9]+\/60\] 결과 /{ if(p) print d"\t"c"\t0"; c=$3; p=1; next }
/^  -> [0-9]+건 \(낙찰/{ if(p){ n=$2; sub("건","",n); print d"\t"c"\t"n; p=0 } next }
/^매각결과: /{ if(p){ print d"\t"c"\t0"; p=0 } }' "$f"; done \
| awk -F'\t' '{if(!(k=$2 in mn)||$3<mn[$2])mn[$2]=$3; if($3>mx[$2])mx[$2]=$3}
   END{for(k in mn) printf "%-18s %5d ~ %5d\n", k, mn[k], mx[k]}' | sort
```

## 함정 (읽고 시작할 것)

- **분모를 `sale_date`가 지난 물건 수로 잡지 말 것.** 유찰 물건은 다음 기일이
  공고되면(기일 14일 전) `sale_date`가 미래로 옮겨가 분모에서 빠진다. 그래서 분모에
  남는 건 '아직 다음 기일이 안 잡힌 물건'에 쏠리고 누락률이 13%로 부풀려진다.
  이 착시로 한 번 틀린 보고를 했다.
- **코드를 고칠 거면 워크트리로 격리**하되, 격리 작업대에는 `data/`가 안 따라온다.
  DB 조회·수집 명령·데몬 재시작은 원본 폴더에서만 한다. (CLAUDE.md 세션 규칙)
- **코드를 고쳐도 데몬은 모른다.** `launchctl kickstart -k gui/$(id -u)/com.court-auction.collect`
