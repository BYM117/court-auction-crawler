# 수집기 격차 목록

2026-09-16 조사. **코드는 하나도 안 고쳤다.** 옥션원을 대조군으로 놓고 우리가 못 하거나
틀리게 하고 있는 것을 모았다. 각 항목에 원인·결과·고치는 법·확인 방법을 붙였다.

> **관통하는 한 문장: 우리는 거의 다 받아놨고, 안 읽고 있다.**
> 아래 열 개 중 여섯은 새로 긁을 게 없다. 이미 DB에 있는 걸 파싱만 하면 된다.

---

## G1. 낙찰가를 '사건 종국 전까지' 받을 수 있는데 안 읽는다 ★ 제일 큼

> **2026-09-16 정정: '영구 보관'이 아니다.** 법원 경매사건검색 화면의 유의사항:
> *"부동산은 검색은 되나 **사건이 종국되면 기일관련 정보를 제공하지 않고**, 이후 30일이
> 지나면 기본정보 만을 제공합니다."*
> 실측으로 확인했다 — 종국(취하)된 평택지원 2024타경1116은 `기일정보`·`최근입찰결과`가
> **공란**이다. 우리 DB의 낙찰가가 2023년 것까지 남아 있는 이유는 그 사건들이 아직
> `미종국`이기 때문이다(전수 55,345건이 전부 미종국).
>
> 즉 창은 **7일이 아니라 '사건 종국까지'** 다. 낙찰 → 대금납부 → 배당 → 종국이 보통
> 2~6개월이니 훨씬 넉넉하지만 **무기한은 아니다.** 미종국 사건에서 미리 걷어둬야 한다.

**원인**
법원 상세는 두 갈래로 들어온다. 우리가 읽는 `detail_json['sections' | 'tables']`는
물건 단위 화면이고, 안 읽는 `detail_json['case']` 아래에 사건 단위 화면이 따로 있다.

```
detail_json['case']['case_tables']                 사건 기본내역
detail_json['case']['filing_and_service_tables']   문건처리 내역 · 송달내역
detail_json['case']['schedule_tables']             사건 기일내역  ← 낙찰가가 여기 있다
```

사건 기일내역의 행은 이렇게 생겼다(7칸).

```
[물건번호, 감정평가액, 기일(시각), 기일종류, 기일장소, 최저매각가격, 기일결과]
["2","304,000,000원","2025.12.11(10:00)","매각기일","경매법정 4별관 211호","20,891,000원","매각(21,000,000원)"]
["2","304,000,000원","2025.12.18(14:00)","매각결정기일","3층 7호법정 4별관","최고가매각허가결정"]
["2","304,000,000원","2026.01.27(16:00)","대금지급기한","민사집행과 경매11계","납부(2026.01.20)"]
```

물건 단위 기일내역(5칸)에는 금액이 없다. 그래서 "기일내역엔 금액이 없다"고 잘못 결론냈다.

**결과**
- `CLAUDE.md` 함정 ⑥("놓치면 그 기일 낙찰가는 영영 얻을 수 없다")이 **틀렸다.**
- `SALE-RESULT-GAP.md`의 "7일 시한이 걸린 것은 낙찰 금액 하나뿐"도 **틀렸다.**
- 매각결과 화면을 하루 8번 긁는 근거가 약해진다. 놓쳐도 상세에서 회수할 수 있다.

**검증됨 (2026-09-16)**
매각결과 화면과 겹치는 699건을 대조했다. **금액 일치 699, 불일치 0.** 같은 값이다.

| | 건수 |
|---|---|
| 결과 화면과 금액 일치 | **699 (불일치 0)** |
| 우리 DB에 물건은 있는데 결과행이 없음 | **1,451 ← 회수 가능** |
| 우리 DB에 물건 자체가 없음 | 2,789 |

참고로 유찰 기준 '진짜 놓친 결과'가 336건이었다. **상세 경로가 그보다 훨씬 많이 회수한다.**

**고치는 법 — 두 가지 함정을 먼저 알 것**

1. **표가 세 리스트에 흩어져 있다.** 실측 500건에서 `매각(N원)` 행은 전부
   `filing_and_service_tables`의 `기일 내역`에 있었다. 그런데 다른 물건에서는
   `schedule_tables`에 있다. 리스트 이름을 믿지 말고 **세 개를 다 훑고 caption으로 찾을 것.**
2. **행의 첫 칸은 "이 물건"의 번호가 아니다.** 사건 단위 표라서 형제 물건이 섞인다.
   실측: 3,029개 행 전부가 이 물건의 물건번호와 달랐다. 당연하다 — 낙찰된 물건은
   사이트에서 사라지고 우리가 들고 있는 건 그 형제니까.
   → **`(법원, 사건번호, 행의 물건번호)`로 item_key를 새로 만들어 붙여야 한다.**
   이걸 안 하면 0건이 나오거나(필터가 다 막음) 엉뚱한 물건에 남의 낙찰가가 붙는다.
3. `기일결과`에서 `매각(N원)`·`납부(날짜)`·`최고가매각허가결정`·`최고가매각불허가결정`을
   뽑아 구조화한다.

**확인**
```sql
SELECT COUNT(*) FROM auction_items WHERE detail_json LIKE '%"매각(%원)"%';
```
→ 4,756건. 그 안의 낙찰가 행은 18,444개(중복 포함), 기일 범위 2023.03.06 ~ 2026.09.08.

---

## G2. 지분경매 2,497건에 딱지가 없다 ★ 제일 싸게 고침

**원인**
`enrichment.SPECIAL_RIGHT_KEYWORDS`에 `지분`이 없다. 13개뿐이고 옥션원은 40개다.

**결과**
활성 물건의 7.7%가 일반 물건처럼 보인다. 지분은 낙찰받아도 단독으로 쓸 수 없는
물건이라 입찰자에게 완전히 다른 물건이다.

**고치는 법**
`SPECIAL_RIGHT_KEYWORDS`에 `("지분", "지분경매")` 한 줄. 데이터는 이미 목록 비고에 있다.
`special_rights`는 배열이라 웹이 안 깨진다(값만 하나 늘어난다).

같이 볼 만한 것: `건물만`(115) · `토지만`(171) · `대지권미등기`(12) · `재진행`(7).
가등기(27) · 가처분(29) · 형식적경매(0) · 임금채권(3)은 목록 비고에 거의 없다 —
등기부에 있는 것이라 우리가 원천적으로 못 얻는 쪽이다. 개수가 작은 게 그 증거다.

**확인**
```sql
SELECT SUM(raw_json LIKE '%지분%') FROM auction_items WHERE is_active=1;
```

---

## G3. 취하·변경·정지를 모른다

**원인**
목록 화면의 `진행상태`가 주는 값은 `신건`과 `유찰 N회` **둘뿐**이다(DB 전수 확인).
매각결과 화면도 `유찰`·`매각` 둘뿐이다. 취하된 물건은 **상태 변화 없이 그냥 사라진다.**
`store.apply_lifecycle`이 '기일 지남' 또는 '7일 미목격'으로만 종결 처리한다.

옥션원의 상태 어휘는 18개다.
```
진행물건   신건 · 유찰
매각전부   매각/매각허가 · 잔금납부물건 · 배당기일/종결
진행외물건 미진행 · 변경/연기 · 불허가/허가취소 · 정지
종국물건   각하 · 기각 · 이송 · 취소 · 취하 · 모사건종결 · 기타
```

**결과**
- 실측: 평택지원 2024타경1116은 옥션원이 **취하**, 우리는 **유찰 6회·활성**.
  기일(9/14) 5일 전인 9/9에 목록에서 사라졌는데 우리 웹엔 입찰 가능으로 떠 있다.
- 같은 상태가 **상시 194건**(기일이 미래인데 3일 이상 미목격인 활성 물건).
- 왜 사라졌는지 영원히 모른다. 취하(끝)와 변경(다시 옴)을 구분 못 한다.

**확정됨 — 법원이 종국결과를 직접 준다 (2026-09-16, 실제 화면으로 확인)**

옥션원이 '취하'라고 표시한 평택지원 2024타경1116을 법원 경매사건검색에서 직접 조회했다.

```
종국결과   취하
종국일자   2026.09.09        ← 우리 DB의 last_seen_at과 정확히 같은 날
물건상태   매각준비 -> 매각공고 -> 유찰
기일정보   (공란)            ← 종국되어 기일 정보가 끊김
```

**우리 DB에는 `종국결과`가 전수 55,345건 전부 `미종국`이다.** 한 건의 예외도 없다.
활성 물건만 상세를 받으니, 종국된 순간부터 영영 안 본다.

**검증 못 한 가설 (참고로 남김)**

*틀린 가설*: 문건처리 내역을 키워드로 훑는다.
표본 6,000건에서 '변경'이 43.6%, '연기'가 22.2% 걸린다. 실제로는
`송달장소변경신고서 제출`, `배분기일취소통보 제출`, `경매비용환급계좌변경신청` 같은
무관한 줄이다. **오탐이 압도적이라 이대로는 못 쓴다.**

*맞는 길*: 사건 기본 내역에 **`종국결과`** 칸이 있다. 여기에 취하·기각·각하·배당종결이
찍힌다. 그런데 우리가 가진 값은 **7,815건 전부 `미종국`** 이다.

이유: `store.list_detail_targets`가 `is_active=1`만 대상으로 한다. 물건이 사라지면
비활성이 되고, **그 뒤로는 상세를 영영 다시 안 받는다.** 그래서 '미종국' 스냅샷만 남는다.

→ **물건이 목록에서 사라졌을 때 상세를 딱 한 번 더 받으면 종국결과가 찍힌다.**
   `list_detail_targets`에 '방금 비활성이 된 물건' 조건을 하나 넣는 일이다.
   이게 취하/기각/이송/배당종결을 한꺼번에 해결한다.

**확인**
```sql
SELECT COUNT(*) FROM auction_items
 WHERE is_active=1 AND sale_date > date('now')
   AND julianday('now') - julianday(last_seen_at) >= 3;
```

---

## G4. 재매각·불허가·미납 이력이 화면에 안 나온다

**원인**
`parse_special_rights`의 재료가 **목록 비고와 주소뿐**이다(`enrichment.py:274`).
상세의 기일내역을 안 본다.

**결과**
활성 물건 32,132건 중 과거에 낙찰됐다 깨진 물건이 **1,312건**인데, 비고에 '재매각'
글자가 있는 419건만 딱지가 붙는다. **893건은 그냥 '유찰 N회'로 보인다.**

이게 왜 돈 문제인가: **재매각은 매수신청보증금이 최저가의 20%다**(보통 10%).
비고에 '재매각'이 있는 활성 423건 중 **423건 전부**가 20%를 같이 적고 있다. 예외 없다.
모르고 가면 보증금 부족으로 입찰 무효다.

| 활성 물건의 이력 | 건수 |
|---|---|
| 과거 매각 | 1,312 |
| 대금미납 | 1,074 |
| 매각불허가 | 213 |
| 매각허가취소 | 32 |

**고치는 법 — 기일내역 파싱이 필요 없다 (2026-09-16 발견)**

사건 화면의 **`물건상태`** 한 필드에 전체 이력이 문자열로 들어 있다. **물건 단위라
형제 물건이 안 섞인다.** 표본 6,000건 중 **5,851건(97.5%)** 이 이미 갖고 있다.

| 물건상태 | 건수 |
|---|---|
| `매각준비 -> 매각공고` | 3,448 |
| `매각준비 -> 매각공고 -> 유찰` | 1,819 |
| `매각준비 -> 매각공고 -> 매각 -> 매각허가결정 -> 대급납부` | 353 |
| `매각준비 -> 매각공고 -> 매각 -> 매각허가결정` | 120 |
| `매각준비 -> 매각공고 -> 매각 -> 매각허가결정 -> 대금미납` | **84** |
| `매각준비 -> 매각공고 -> 매각 -> 매각불허결정` | **21** |
| `매각준비 -> 매각공고 -> 매각` | 6 |

`대금미납`·`매각불허결정`이 들어 있으면 재매각이다. 문자열 포함 검사 한 줄이면 된다.

**함정: 법원 사이트 오타를 그대로 받아야 한다.** `대금납부`가 아니라 **`대급납부`** 다.
`대금`으로 찾으면 353건을 통째로 놓치고, `대금미납`만 걸린다.

---

## G5. `sold_amount`가 재매각 때 안 지워진다 (진짜 버그)

**원인**
`store.record_sale_results`가 매각을 보면 `sold_amount`를 박고 `is_active=0`으로 내린다.
물건이 되돌아오면 `upsert_items`가 상태를 보고 `is_active=1`로 되살리지만
**`sold_amount`·`sold_date`는 초기화하지 않는다.**

**결과**
`enrichment.build_sold`가 `sold_amount`만 보므로 **지금 입찰 가능한 물건에
"낙찰 X원"이 찍힌다.** 현재 18건.

**앞으로 는다.** 대금미납은 낙찰 30~40일 뒤에 드러나는데, 우리가 낙찰가를 모으기
시작한 게 8/20이라 첫 미납 주기가 아직 안 돌았다.

**고치는 법**
`upsert_items`에서 물건이 다시 활성이 될 때 `sold_amount`/`sold_date`를 비운다.
단 G1의 기일내역 이력은 남겨서 "과거에 얼마에 낙찰됐다 깨졌다"를 보여주는 편이 낫다.

**확인**
```sql
SELECT COUNT(*) FROM auction_items WHERE is_active=1 AND sold_amount IS NOT NULL;
```

---

## G6. 감정평가서 54,942건이 '수집됨'인데 빈 껍데기다

**원인**
감정평가서는 Adobe 뷰어 iframe 안에 있어 본문을 못 가져온다. 그런데
`save_document_status`가 `collected`로 기록한다. 현황조사서·매각물건명세서는 HTML
표라서 `metadata_json`에 제대로 들어가는데, 감정평가서만 껍데기가 남는다.

**결과**

| 문서 | '수집됨' | 실제 내용 있음(>2KB) |
|---|---|---|
| 현황조사서 | 55,048 | 22,263 |
| 매각물건명세서 | 35,700 | 28,651 |
| **감정평가서** | **54,942** | **0** |

평균 길이 702바이트. 실제 PDF를 받은 건 **1건**(31MB). 웹에 감정평가서 탭이 있는데
보여줄 게 없다. 그리고 **상태가 거짓말을 해서 오늘까지 아무도 몰랐다.**

**고치는 법**
둘 중 하나.
1. 못 받는 게 맞다면 상태를 `collected`가 아니라 `metadata_only`나 전용 값으로 바꾼다.
   **이게 먼저다.** 거짓 상태를 놔두면 다음 사람도 속는다.
2. 받을 방법을 찾는다(뷰어가 실제로 긁어오는 URL을 네트워크 탭에서 확인).

**확인**
```sql
SELECT document_type, COUNT(*), ROUND(AVG(LENGTH(metadata_json))),
       SUM(LENGTH(metadata_json)>2000)
  FROM auction_documents WHERE status='collected' GROUP BY 1;
```

---

## G7. 임의/강제와 형식적경매를 갖고 있으면서 안 내보낸다

**원인**
사건 기본 내역의 `사건명` 칸을 파싱하지 않는다. `enrichment`의 payload에 안 실린다.

**결과**
옥션원은 `경매절차(임의/강제)`를 검색 조건으로 쓰고, 특수물건에 형식적경매 4종을 둔다.
우리는 값을 갖고도 못 쓴다. 강제경매와 임의경매는 권리 분석이 다르다.

표본 8,000건의 `사건명` 분포:

| 사건명 | 건수 |
|---|---|
| 부동산임의경매 | 4,491 |
| 부동산강제경매 | 3,071 |
| 공유물분할을위한경매 (+부동산) | 110 |
| 청산을위한형식적경매 (+부동산) | 74 |
| 자동차임의경매 | 42 |
| 소형선박임의경매 | 8 |

**고치는 법**
`사건명`을 파싱해 payload에 `auction.case_type`으로 더한다(필드 추가라 안전).
`형식적경매`가 들어간 사건명은 `special_rights`에도 넣는다.

---

## G8. 인근매각통계 표가 3,892건 전부 비어 있다

**원인**
법원 상세에 `인근매각통계` 표가 있고 우리도 긁는다. 그런데 **머리글만 오고 데이터 행이 0개**다.
표본 4,000건 중 데이터가 있는 물건 0건, 빈 표만 3,892건. 화면이 별도 요청으로 채우는데
우리가 그 전에 표를 떠오는 것으로 보인다(미확인).

**결과**
법원이 주는 '같은 동네 최근 매각 통계'(기간·매각건수·평균감정가·평균매각가·매각가율·
평균유찰횟수)를 통째로 못 쓴다. 옥션원은 같은 자리에 `입찰인원수`와 자체 추정가까지 붙인다.

**확인함 (2026-09-16, 실제 화면)**
물건상세 화면에 **`인근매각통계`·`인근매각물건` 버튼이 따로 있다.** 표는 페이지 로드
시점에 머리글만 있고(rows=1), 버튼을 눌러야 채워지는 구조다. 우리 크롤러는 그 버튼을
안 누르므로 영원히 빈 표를 받는다.

JS로 `el.click()`을 해봤지만 표가 안 채워졌다. **WebSquare가 신뢰된 클릭(실제 마우스
이벤트)만 받기 때문으로 보인다** — `_go_next_page`에 이미 같은 주석이 있다:
*"WebSquare 페이저(w2pageList)는 신뢰된 클릭만 받는다."*

**고치는 법**
`detail_crawler`에서 물건상세를 연 뒤 `인근매각통계` 버튼을 **Playwright의 실제 클릭**
(`locator.click()`)으로 누르고, 표가 채워질 때까지 기다린 뒤 긁는다. 페이저에서 쓴
방식 그대로다. 같은 자리의 `인근매각물건` 버튼도 함께 볼 것.

**확인**
```python
# 표는 있는데 데이터 행이 0인 물건 수
```
표본 4,000건 → 데이터 있음 0 / 빈 표 3,892.

---

## G9. 기간입찰 물건이 0건이다 (확인 필요)

**원인 (가설)**
표본 6,000건의 `입찰방법`이 **전부 `기일입찰`** 이다. 기간입찰이 한 건도 없다.
옥션원은 `기간입찰` 전용 메뉴(`/auction/ca_gigan.php`)를 따로 둔다.

우리 크롤러는 '물건상세검색(진행)'과 '매각예정물건' 두 화면만 본다.
**기간입찰 물건이 그 두 화면에 안 나오는 것인지, 아니면 원래 드문 것인지 확인 안 됐다.**

**결과**
만약 전용 화면이 따로 있다면 그 물건들을 통째로 놓치고 있다.

**고치는 법**
법원경매정보에 기간입찰 전용 검색이 있는지부터 확인한다. 있으면 `SearchPageConfig`를
하나 더 만들면 된다(화면 4종을 이미 그렇게 분기하고 있다).

---

## G10. 당사자 내역을 100% 갖고 있으면서 안 쓴다

**원인**
사건 기본 내역에 `당사자 내역` 표가 있다(채권자·채무자겸소유자·임차인·가압류권자·교부권자).
표본 6,000건 **전부**에 있다. payload에 구조화해서 싣지 않는다.

**결과**
옥션원은 이걸로 `이해관계인검색`과 `은행별경매물건`을 만든다.
"채권자가 은행인가 개인인가"는 권리 분석의 출발점인데 우리는 못 쓴다.

**고치는 법**
법원이 이미 마스킹해서 준다(`이OO`, `광OOOOOOO`). **역추적하지 말 것.**
개인 식별이 아니라 **채권자 유형 분류**(은행/캐피탈/공공/개인)까지만 쓰는 것이
`PRIVACY-MASKING.md` 취지에 맞는다.

---

## 문제 아니었던 것 (다시 파지 말 것)

**현황조사서의 임차인 정보는 이미 끝까지 돌고 있다.**
점유인·점유부분·보증금/차임·전입일자·확정일자 표가 `metadata_json`에 들어가고,
`store.get_item`이 `documents[].metadata`로 실어 보내고, 웹의 `lib/privacy.ts`가
이름을 가려 렌더링한다. 22,263건에 실제 내용이 있다.

즉 `CLAUDE.md`의 "전입세대열람내역서를 못 얻는다"는 맞지만,
**전입일자·확정일자는 현황조사서로 이미 갖고 있다.**

---

## 원천적으로 못 따라가는 것

**응찰자 수와 2등 입찰가.** 옥션원은 목록에 "입찰 3명"을 찍고 상세 인근매각통계에
"입찰인원수" 칸이 있다. 법원경매정보에는 없다 — 경매법정 현장 수집이다.
데이터 계약이나 사람 없이는 안 된다.

**옥션원을 긁는 것은 선택지가 아니다.** 상세 열기 함수 이름이 페이지 로드마다
바뀌고(`AjrRQXBt` → `FpLWcnIB`), 푸터에 무단수집 금지가 명시돼 있다.
눈으로 대조하는 용도까지다.

---

## 고치는 순서 (제안)

| | 항목 | 크기 | 비용 |
|---|---|---|---|
| 1 | G5 `sold_amount` 초기화 | 18건, 증가 중 | 작음 |
| 2 | G2 지분 키워드 | 2,497건 | 한 줄 |
| 3 | G6 감정평가서 상태 정직하게 | 54,942건 | 작음 |
| 4 | G1 `case.schedule_tables` 파서 | 18,444건 | 중간 |
| 5 | G4 재매각 딱지 — `물건상태` 문자열 검사 | 893건 | **한 줄** |
| 6 | G3 비활성 직후 상세 1회 재수집 → 종국결과·종국일자 | 상시 194건 | 중간 |
| 7 | G7 사건명(임의/강제·형식적경매) payload에 추가 | 전량 | 작음 |
| 8 | G10 당사자 내역 → 채권자 유형 | 전량 | 작음 |
| 9 | G8 인근매각통계 빈 표 | 3,892건 | 중간, 원인 확인 먼저 |
| 10 | G9 기간입찰 누락 여부 확인 | 미상 | 확인만 |

G1이 끝나면 `SALE-RESULT-GAP.md`와 `CLAUDE.md` 함정 ⑥을 같이 고쳐야 한다.

## 함정

- **코드를 고칠 거면 워크트리로 격리**하되, 격리 작업대에는 `data/`가 안 따라온다.
  DB 조회·수집 명령·데몬 재시작은 원본 폴더에서만. (CLAUDE.md 세션 규칙)
- **payload에 필드를 더하는 것은 안전하고, 이름을 바꾸거나 빼는 것은 웹과 맞춰야 한다.**
- 사건 단위 표는 **물건번호로 걸러야 한다.** 안 그러면 형제 물건 값이 섞인다.
