# 세션 D — 웹 (꽁지맵)

**소유 저장소**: `~/Documents/court-auction-web`
**작업대**: 그 저장소 (이 저장소와 별개)
**성격**: **이쪽이 올리는데 저쪽이 안 내리는** 것을 잇는다

## 맡는 것

| 순 | 항목 | 비고 |
|---|---|---|
| 1 | [G13 실거래가 표시](../gaps/G13-웹이-안쓰는payload.md) | **`matched` 없이 붙이면 안 됨** |
| 2 | [G12 좌표 품질 표시](../gaps/G12-엉뚱한좌표.md) | `approximate`를 드러낼 것 |
| 3 | G13 `property.share` 사용 | 지분 매각 표시 |
| 4 | G13 `registry_search_hint` 사용 | 등기부 검색 힌트 |
| 5 | [G05 `past_sales` 표시](../gaps/G05-낙찰가-초기화와이력.md) | B가 만들면 |

## 지금 쓰고 있는 것 / 안 쓰는 것

payload 전수(summary 103개 / detail 240개)를 대조한 결과다.

**쓰고 있다**: `screening` · `type_guess` · `special_rights` · `land_use` · `official` ·
`building` · `popularity` · `sold` · 문서 본문(`lib/privacy.ts`가 이름을 가려 렌더링)

**안 쓴다**: `property.transactions` · `property.share` ·
`property.registry_search_hint` · `map.coordinate_quality`

## ⚠ 실거래가는 그냥 붙이면 안 된다

| | 그 건물 매칭 | 동네 평균만 |
|---|---|---|
| 매매 | **664 (3.5%)** | 18,577 |
| 전월세 | **1,001 (5.2%)** | 18,240 |

**96.5%가 그 건물이 아니라 동네 평균**인데 필드명은 `transactions`이고 `building`에
건물명이 붙어 있다. **`sales.matched` / `rent.matched`를 반드시 화면에 드러낼 것.**
매칭이면 "이 건물", 아니면 "○○동 평균(880건)"처럼 범위를 명시한다.

> 목표는 네이버 부동산처럼 **"언제, 얼마에 팔렸다"**를 보여주는 것이다.
> 다만 **정확도를 올리는 건 세션 B의 숙제**다. B가 3.5%를 못 올리면
> 여기서 뭘 만들어도 틀린 숫자를 예쁘게 보여주는 것뿐이다.

## ⚠ 좌표도 마찬가지

`approximate` 1,041건 중 **22%가 아예 다른 동/리**에 찍혀 있다(`G12`).
지금은 `coordinate_quality`를 안 써서 **엉터리 핀이 정확한 핀과 똑같이 보인다.**
B가 근본을 고치는 동안에도 **품질 표시는 먼저 할 수 있다.**

## 함정
- **payload 필드가 늘어나는 것은 안전**하지만, 이름이 바뀌거나 빠지면 화면이 깨진다.
  변경은 세션 B와 합의.
- `G14` 위험도는 지금 화면에 나가는데 **전 물건에 같은 경고가 뜬다.**
  세션 E가 재설계 중이니 **그 전에 강조 UI를 만들지 말 것.**

## 공통 규칙 (모든 세션)

1. **한 파일은 한 세션만 만진다.** 남의 파일이 필요하면 그 세션에 요청한다.
2. **코드를 고치는 세션은 워크트리로 격리한다.** 단 격리 작업대엔 `data/`가 안 따라온다 —
   `--db data/auction.sqlite3` 기본값이 cwd 상대경로라 **에러 없이 빈 DB가 새로 생긴다.**
   **DB 조회·수집 명령·데몬 재시작은 세션 C에 부탁한다.**
3. **문서는 `cat >`로 쓰지 말 것.** 남의 세션이 같은 파일을 만들었을 수 있다.
   덧붙일 때는 앵커를 찾아 끼워 넣는다. (2026-09-16에 이걸로 사고 한 번 냈다)
4. **일이 끝난 브랜치는 그때그때 main에 넣는다.** 안 그러면 다음 워크트리가 낡은 코드에서 시작한다.
5. **푸시는 사용자 승인.** 커밋까지는 알아서.
6. **일이 끝나면 `DEVLOG.md` 맨 위에 기록.** 다른 세션이 아는 유일한 통로다.
7. **코드를 고쳐도 데몬은 모른다.** `launchctl kickstart -k gui/$(id -u)/com.court-auction.<이름>`

## 시작 전에 읽을 것
- `CLAUDE.md` — 저장소 규칙과 함정 ①~⑧
- `gaps/_INDEX.md` — 격차 16건 색인
- 이 문서가 가리키는 개별 `gaps/G**.md`
