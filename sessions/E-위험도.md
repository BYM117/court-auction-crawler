# 세션 E — 위험도(screening) 재설계

**소유 함수**: `enrichment.build_screening` (파일은 B와 공유 — **시점 조율 필수**)
**작업대**: 워크트리
**성격**: 버그 수정이 아니라 **기준 설계**

## 맡는 것
[G14](../gaps/G14-위험도-재설계.md) 하나. 설계 문서는 **[`../SCREENING-REDESIGN.md`](../SCREENING-REDESIGN.md)**.

## 지금 뭐가 망가졌나 (요약)

```python
score = 50                     # 50에서 시작, 모든 분기가 -= (가점 없음)
risk_level = "낮음" if score >= 65 else ...   # ← 최대 50인데 65 요구 = 죽은 분기
flags.append("권리확인 필요")   # ← 조건 없이 무조건
```

활성 400건 표본: **높음 52% / 보통 48% / 낮음 0%.**
flags의 `최저가율`과 `권리확인 필요`는 **400/400 전부**에 붙는다.

**이게 지금 웹 화면에 나가고 있다.**

## 왜 급하게 고치면 안 되나

> 2026-09-16 사용자 방침: *"위험도는 기록하고 새 세션에서 확실한 기준을 다시 잡는 게 좋겠다."*

숫자만 바꾸면 또 다른 엉터리가 된다. **무엇을 위험으로 볼지부터 정해야 한다.**

## 먼저 답할 질문 (설계 문서에 상세)
- 이 점수는 **누구를 위한 것인가?** 초보 입찰자의 1차 거름망인가, 전문가 체크리스트인가.
- **틀렸을 때 어느 쪽이 더 나쁜가?** 위험한 걸 안전하다고 하는 것 vs 그 반대.
- 옥션원·다른 서비스는 이런 등급을 **주는가, 안 주는가?** 안 준다면 왜.

## 쓸 수 있는 재료 (오늘 조사로 드러난 것)

| 신호 | 출처 | 보유 |
|---|---|---|
| 지분 매각·지분율 | `property.share` | 지분 물건 전부 |
| **재매각·대금미납·불허가** | 사건 `물건상태` | 활성의 97.5% ([G04](../gaps/G04-재매각-물건상태.md)) |
| **과거 낙찰가와 깨진 사유** | 사건 기일내역 | [G01](../gaps/G01-낙찰가-사건화면.md) |
| **임의/강제/형식적경매** | 사건명 | 전량 ([G07](../gaps/G07-사건명-임의강제.md)) |
| **대항력 재료**(최선순위 설정일 + 전입일) | 매각물건명세서 본문 | 2.8만 건 |
| 특수권리 13종 | `special_rights` | 전량 |
| 좌표 신뢰도 | `coordinate_quality` | 전량 (**[G12](../gaps/G12-엉뚱한좌표.md) 주의**) |

옥션원의 **특수물건 40종**도 참고할 것. 우리는 13종이다.
없는 것: 지분입찰·건물만/토지만 입찰·대지권미등기·선순위 가등기/가처분·
토지별도등기 인수조건·형식적경매 4종.

## 함정
- **웹이 이미 이 값을 쓰고 있다.** 필드를 없애거나 이름을 바꾸면 화면이 깨진다.
  더하는 것은 안전, 빼는 것은 세션 D와 합의.
- `enrichment.py`를 **세션 B와 공유**한다. 같은 시간에 만지지 말 것.
- 재료 중 `물건상태`·기일내역·사건명은 **세션 B가 파싱해줘야 쓸 수 있다.**
  **B의 작업이 끝난 뒤에 설계를 확정하는 편이 낫다.**

## 공통 규칙 (모든 세션)

1. **한 파일은 한 세션만 만진다.** 남의 파일이 필요하면 그 세션에 요청한다.
2. **코드를 고치는 세션은 워크트리로 격리한다.** 단 격리 작업대엔 `data/`가 안 따라온다 —
   `--db data/auction.sqlite3` 기본값이 cwd 상대경로라 **에러 없이 빈 DB가 새로 생긴다.**
   **DB 조회·수집 명령·데몬 재시작은 세션 C에 부탁한다.**
3. **문서는 `cat >`로 쓰지 말 것.** 남의 세션이 같은 파일을 만들었을 수 있다.
   덧붙일 때는 앵커를 찾아 끼워 넣는다. (2026-09-16에 이걸로 사고 한 번 냈다)
4. **일이 끝난 브랜치는 그때그때 main에 넣는다.** 안 그러면 다음 워크트리가 낡은 코드에서 시작한다.
5. **푸시는 사용자 승인.** 커밋까지는 알아서.
6. **일이 끝나면 `DEVLOG.md` 맨 위에 기록.** 다른 세션이 아는 유일한 통로다.
7. **코드를 고쳐도 데몬은 모른다.** `launchctl kickstart -k gui/$(id -u)/com.court-auction.<이름>`

## 시작 전에 읽을 것
- `CLAUDE.md` — 저장소 규칙과 함정 ①~⑧
- `gaps/_INDEX.md` — 격차 16건 색인
- 이 문서가 가리키는 개별 `gaps/G**.md`
