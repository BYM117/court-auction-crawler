# 개발기록

**새 기록은 맨 위에 쌓는다.** 세션이 여럿 붙어 있어서, 여기가 다른 세션이 무슨 일이 있었는지 아는 유일한 통로다.

한 항목에 **무엇을 왜 고쳤는지**와 **남은 것**이면 충분하다. 코드 단위 변경 내역은 `git log`가 이미 갖고 있으니 옮겨 적지 않는다.

---

## 2026-09-17 — 밤새 매각결과 보충: 낙찰가 808건(2,025억) 회수 (세션 A)

00:04~08:14, 15패스. 사이트 차단으로 12번 자가 중단됐고 그때마다 냉각 후 이어갔다.

**거둔 것**

| | |
|---|---|
| 보충한 결과행 | **8,853행** (유찰 5,874 / 변경 1,549 / 매각 1,419) |
| 금액이 붙어 온 행 | 1,187 |
| **낙찰 확정된 물건** | **808건 / 합계 2,025억원** |

회수한 기일이 2023.03까지 거슬러 올라간다. 몰려 있는 곳은 **2026.06(291건)과
2026.07(495건, 그중 481건에 금액)** — 결과 수집을 시작한 8/20 이전 구간이다.
**정상 경로로는 영영 못 얻었을 낙찰가다.**

**대기열이 앞으로 안 나가는 문제를 찾아 고쳤다**

남은 대상이 15,982 → 14,948로밖에 안 줄었다. 대상 조건이 '그 물건의 **현재 기일**에
결과행이 없다'인데, 그 기일에 법원이 결과를 아예 안 올리는 물건이 많다(census 기준
93%가 변경·취소라 애초에 경매가 안 열렸다). 그런 물건은 몇 번을 훑어도 안 빠지고
대기열 맨 앞에 그대로 남는다. 그래서 패스마다 같은 사건을 다시 걸었다 — 12패스는
715사건 2,232행, 15패스는 870사건 160행.

`auction_items.result_checked_at`에 훑은 시각을 적고 **한 번도 안 본 것부터** 돌게
했다. 못 채웠어도 적는다(다음 차례는 오되 한 바퀴 뒤다).

**아침 확인 패스는 수확이 없었다.** 대기열 맨 앞은 밤새 이미 훑은 자리라 당연하고,
여덟 시간을 두들긴 직후라 사이트도 식히는 중으로 보인다(사건 조회가 거의 다 실패).
**다음 보충은 몇 시간 쉬었다가 돌릴 것.**

**남은 것**
- 대상 14,948건(사건 12,010개). 7월 7,447 · 8월 5,971이 아직 안 열린 노른자다.
  이제 대기열이 앞으로 나가므로 다음 밤샘이 훨씬 멀리 간다.
- 보충한 유찰·변경 행은 웹에 바로 안 간다. 낙찰 확정분은 `updated_at`이 갱신돼
  푸시 후보가 되지만 나머지는 아니다(CLAUDE.md 함정 ②).
- 상세 수집 데몬을 8시간 내려뒀더니 상세 대기열이 1,791건까지 쌓였다. 복구됐고
  지금 소화 중이다. 다음 밤샘 때도 같은 값을 치른다.

---

## 2026-09-17 — G12 진단: 엉뚱한 좌표의 78%는 코드가 아니라 **낡은 데이터**였다 (세션 C)

세션 B가 바로 일할 수 있게 DB 실측을 떠뒀다. **고치는 순서가 바뀐다.**
`scripts/geocode_recheck.py` (읽기 전용).

**표본 50건을 코드 한 줄 안 고치고 그대로 다시 돌렸더니 48건(96%)이 정상 좌표를 얻었다.**
옮겨간 거리 중앙값 2.13km, 최대 12.18km — 81%가 1km 넘게 틀려 있었다.

**왜냐면 망가진 좌표의 78%(813건)가 2026-07에 찍혔다.** 광주·전남 통합 대응
(`MERGED_SIDO`)은 **2026-08-24 `ebbf640`** 에 들어왔다. 그 전에는 `전라남도 …`를
브이월드가 못 찾아서 건물명 폴백으로 샜고, 폴백이 시도명을 건물명으로 써서 읍면
한 점에 뭉쳤다. **지금은 같은 쿼리가 그냥 통한다.**

**진짜 구조적 문제는 따로 있다 — 한 번 박힌 좌표는 다시 안 물어본다.**
`geocode-missing`은 *좌표가 없는* 물건만 본다(`cli.py:225`). **틀리게 있는 것은 영원히
안 고쳐진다.** 그래서 8월에 코드를 고쳤는데도 7월에 생긴 813건이 두 달째 지도에 남았다.

**버그 자체는 살아 있다.** 통합 대응 이후인 9월에도 52건이 새로 망가졌고, 지금도
`충청북도 음성군 대소면 삼호리 790` 같은 주소는 주소검색·getcoord 네 번 모두 '없음'이라
폴백으로 흘러간다(표본의 4%).

**버린 가설 하나** — `max_queries=4`가 지번 후보를 잘라낸다고 봤는데, 448건 중 **0건**이
잘렸다. 토지 주소는 정규화하면 후보가 1~2개뿐이라 상한에 안 닿는다.

**세션 B에게 — 순서를 지켜야 한다**
1. `_extract_building_hint`가 행정구역명을 못 돌려주게 막는다. **이게 먼저다** —
   안 그러면 재시도에서 실패하는 4%가 다시 가짜 좌표를 받는다.
2. 그다음 1,041건의 좌표를 지워 재시도 대상으로 돌린다(96% 회수). **DB 쓰기는 C에 부탁.**
3. 그래도 못 찾으면 좌표를 안 만든다.
4. **`approximate` 좌표를 기한 두고 재시도하는 구조**를 볼 것. 이번 건의 진짜 교훈이다.

---

## 2026-09-17 — 바깥 기준선을 깔았다: 법원 공식 통계와 대조 (G11, 세션 C)

우리 검증은 전부 '이전 우리 값'과 비교한다. 처음부터 적게 긁고 있었으면 영원히 모른다.
**법원이 주는 공식 매각통계를 바깥 자[尺]로 쓴다.** `scripts/baseline_compare.py`.

**세션 A가 필요 없어졌다.** 원래는 A가 긁고 C가 대조하는 계획이었는데, 화면을 60번
검색할 필요 없이 **JSON API 하나**면 됐다(`selectRletCortDspslStats.on`, 법원 코드는
`selectCortOfcCdLst.on`). curl은 WAF가 막아서 Playwright로 페이지를 한 번 열고 그 안에서
부른다. 60개 법원에 20초. **`crawler.py`도 `store.py`도 안 건드렸다** — 저장은
`data/official_stats.json`.

**정의가 우리와 다른 곳 두 군데**
- `경매건수` = 입찰(유찰 포함) 및 매각된 물건 → 우리 `유찰`+`매각` 기일행과 맞는다.
- `매각건수` = **최종 물건상태** 기준(`매각·매각허가결정·대금납부`). 우리는 **기일 결과**
  기준이라 **대금미납 재매각에서 어긋난다.** 그래서 경매건수로만 대조한다.

**통계는 다음 달 1일에 확정된다.** 응답에 `frstInptDt: 20260901`이 붙는다.

**기준선 숫자를 얻었다 — 온전히 받으면 100~102%다**
202608은 우리가 8/20에 수집을 시작해 달을 처음부터 못 봤다(전국 58.6%). 하지만
**8/13 이후에만 기일이 있던 법원 5곳**은 남양주 100%, 영덕 101%, 강릉 101%, 거창 102%,
남원 98%였다. **95% 아래면 그 법원에서 새고 있는 것이다.**

**깃발 8개는 전부 오탐이었다.** 영동지원 0%는 8월 기일이 8/4·8/11뿐이라 우리 창 밖이었고
(9월엔 110행 정상), 밀양 25%·안동 25%도 같은 이유였다. **부분 달로는 '적게 받은 법원'과
'기일이 일찍 있던 법원'을 못 가른다.** 도구가 부분 달이면 경고를 먼저 띄우게 해뒀다.

**남은 것 — 날짜 하나**
2026년 9월이 우리가 처음부터 끝까지 본 첫 달이고, 그 통계는 **2026-10-01**에 나온다.
그날 아래를 돌리면 G11이 처음으로 제 일을 한다.
```
.venv/bin/python scripts/baseline_compare.py --fetch 202609
.venv/bin/python scripts/baseline_compare.py 202609
```
95% 아래로 떨어지는 법원이 있으면 **그게 우리가 몰랐던 손실이다.**

덧: 두 스크립트 모두 `--check`로 자가 점검이 붙어 있다. 스크립트는 `.venv/bin/python`으로
돌려야 한다(시스템 python3.9에는 playwright가 없다).

---

## 2026-09-17 — 사이클을 줄여도 되는가: 재보니 전제 세 개가 틀렸다 (G17, 세션 C)

묶음 F가 *"하루 8사이클 중 6개가 낭비"* 라고 했고, G17이 *"낮 사이클은 낭비가 아니라
보험"* 이라고 뒤집었다. 어느 쪽인지 **로그 58일·사이클 296회로 재봤다.**
`scripts/cycle_value.py` — 읽기 전용이고 언제든 다시 돌아간다.

**전제 세 개가 틀렸다**
1. **하루 8회가 아니라 5.1회다.** launchd는 3시간마다 깨우는데 한 사이클이 1.5시간
   걸린다. 3+1.5 = 실제 주기 4.5시간.
2. **로그의 `N개 반영`은 물건 수가 아니다.** 긁은 줄 수다. 물건이 없는 화면이 안내 행을
   7~8줄 돌려주는데, 그게 `8개 반영 (신규 0, 변경 0, 동일 0)`으로 찍힌다.
   **0건이 8건으로 보인다.** 그리고 **커버리지 통계가 쓰는 값이 바로 이것이다.**
3. **'흔들림'은 2.1%가 아니라 12.1%다.** 들어간 수로 다시 세면 그렇다.

**제일 큰 것 — 자정을 넘긴 사이클은 진행 수집을 통째로 날린다**
창은 사이클 **시작 때** 정해진다(`current=9/14~9/27`). 자정이 지나면 시작일이 어제가 되고
진행 화면이 빈 화면을 돌려준다. 예정은 창이 두 달이라 멀쩡하다.
23시대 시작 사이클의 진행 놓침률 **23.1%**(다른 시간대 0.8~5%). 진행 법원 절반 이상을
날린 사이클 11회 중 6회가 23:20~23:38 시작이다. **하루 5회 중 1회가 반쪽이다.**

**답: 4회까지는 안전, 2회는 꼬리가 위험**

| K회/일 | 완전손실(평균) | 최악의 날 | 못 본 물건/일 |
|---|---|---|---|
| 2 | 2.1 | 106 | 403 |
| 4 | 0.1 | 25 | 11 |
| 5 (현재) | 0.0 | 16 | 1 |

완주한 사이클 하나면 120개를 다 본다. **한 번이면 충분한데 어느 사이클이 망가질지 몰라서
다섯 번 돈다** — 보험이 맞다. 다만 보험료는 사이클당 1.5시간 + R2 283MB/791초다.
**생명주기는 줄이기를 막지 않는다** — `unseen_future_days=7`은 하루 2회여도 14회 연속
미목격을 요구하고, 한 법원이 하루 통째로 빠질 확률이 1.9%다.

**남은 것 (세션 A 몫)**
- **자정 넘김 수정** — 진행 수집 직전에 창을 오늘 날짜로 다시 잡는다. 줄이는 것보다 크다.
- **경고 기준을 '긁은 줄 수' → '들어간 수'로.** 지금은 0건이 8건으로 기록돼 안 걸린다.
- **경고 판정을 같은 날 다른 사이클과 비교하도록.** (G17 원래 1번)

---

## 2026-09-17 — 놓친 낙찰가를 사건 화면에서 되찾는 길을 깔았다

앞 기록에서 "사건 화면의 기일 내역에 낙찰가가 영구히 남는다"를 확인했고, 이번에
그걸 실제로 쓰는 코드를 넣었다. **7일 창을 놓쳐도 메울 수 있다.**

**무엇을 만들었나**
- **평소 상세 수집이 사건 화면에 들른 김에** 빠진 결과행을 함께 메운다. 추가 조회 0회.
- `collect-details --backfill-results` — 기일이 지났는데 그 기일의 결과행이 없는
  물건만 골라 사건 단위로 도는 전용 패스. 물건 상세는 아예 건드리지 않는다.
  종결 물건은 상세조회 버튼이 막혀 있어 시도해봐야 unavailable만 쌓인다.

**지켜야 했던 선 세 개** (테스트로 묶어 뒀다)
1. **이미 받아둔 행은 덮지 않는다.** 기일내역은 '매각'인데 금액이 빠질 때가 있다
   (실측 15%). 덮어쓰면 결과 화면에서 받아둔 낙찰가를 잃는다. `DO NOTHING`이다.
2. **되살아난 물건을 옛 낙찰로 내리지 않는다.** 대금미납 재매각은 목록 기일이 옛
   낙찰보다 뒤에 있다. 이걸 팔린 걸로 처리하면 입찰 가능한 물건이 목록에서 사라진다.
3. **이레 창이 아직 열린 기일은 대상에서 뺀다.** 정상 경로가 곧 가져간다. 안 빼면
   대기열 맨 앞이 통째로 '곧 들어올 것들'로 차서 같은 걸 두 번 긁는다.

**첫 보충 실적** (대상 1,500건 / 사건 195건까지 돌고 사이트 차단으로 자가 중단)
- **결과행 772행 보충**, 그중 **낙찰가 18건 회수**.
- 회수한 기일에 2026.03.23 · 05.22 · 06.08 · 07.20이 섞여 있다. **몇 달 지난
  낙찰가가 실제로 돌아왔다.** 6건은 낙찰 확정으로 목록에서 내려가 sold 스냅샷에 들어간다.
- 최근 기일은 수확이 적다. 법원이 기일내역에 결과를 늦게 채우기 때문이다
  ('매각'만 먼저 뜨고 금액이 나중에 붙는다). 오래된 것부터가 수확이 좋다.

**한 번 헛디뎠다.** 처음엔 물건 버튼 개수를 먼저 보는 기존 흐름을 그대로 탔는데,
종결 사건은 버튼이 통째로 없어서 9건 중 6건이 실패하고 보충 0행이 나왔다. 결과만
메우는 패스는 버튼을 아예 보면 안 된다.

**남은 것**
- 백로그가 아직 **15,000행 가까이** 남았다(사건 12,000여 개). `--backfill-results
  --loop`로 붙여 두면 차단될 때마다 냉각하고 이어서 돈다. 상세 수집 데몬과 같은
  락을 쓰므로 데몬을 내리고 돌려야 한다.
- 보충한 행이 웹에 바로 안 갈 수 있다. 낙찰 확정분은 `updated_at`이 갱신돼 푸시
  후보가 되지만, 유찰·변경만 늘어난 물건은 `pending_item_pushes()` 기준에 안 걸린다
  (CLAUDE.md 함정 ②). 전량 재업로드가 필요하면 `web_sync`의 item 행을 지운다.

---

## 2026-09-16 — 페이지 첫 물건 유실 수정 + 낙찰 누락률 0.8% + "7일 지나면 끝"은 틀렸다

같은 날 앞 기록의 후속. **고쳐 넣었고, 낙찰 누락률을 처음으로 실측했고, 이 저장소가
오래 믿어온 전제 하나가 틀렸다는 것을 확인했다.**

**① 고침 적용** — `crawler._extract_court_items`에서 셀 없는 행도 rowspan 잔량을
깎게 했다. 실제 화면 확인: 속초지원 11건 중 11건 추출(전 10건). 검사 하나
추가(`tests/test_crawler_extract.py`) — 고치기 전 코드에서 실패하는 것 확인.
수집 데몬 재시작 완료.

**② 낙찰 누락률 ≈ 0.8%** — 기일이 지났는데 결과행이 없는 비활성 물건 3,108건이
미지수였다. 무작위 표본 508건의 사건 화면을 훑어 그날 진짜 무슨 일이 있었는지 봤다.

| | 508건 기준 |
|---|---|
| 변경 (경매 안 열림) | 381 (75.0%) |
| 그 날짜 기일 자체가 없음 | 92 (18.1%) |
| 유찰 (놓친 유찰) | 27 (5.3%) |
| **매각 (놓친 낙찰)** | **8 (1.6%)** |

**93%는 애초에 경매가 안 열린 것이다.** 3,108건에 펼치면 놓친 낙찰 ~49건,
같은 기간 잡은 낙찰 6,352건 대비 **0.8%**.

**③ 전제가 틀렸다 — 낙찰가는 영구히 남는다.** 사건검색의 **'기일 내역' 탭**이
물건번호별로 `매각 (175,900,000원)` 꼴로 **금액까지** 준다. '매각' 기일행의 85%에
금액이 있고, 2025년 기일도 그대로 보이며, 우리가 결과 화면에서 받아둔 낙찰가와
대조해 불일치 0건이었다. **종결돼서 물건상세조회가 막힌 물건도 이 탭은 열린다.**
물건 상세 쪽 기일내역에는 금액이 없어서 없는 줄 알았던 것이다. CLAUDE.md 함정 ⑥을
고쳤다.

**④ 먼저 헛디딘 것** — 종결 물건은 '물건상세조회' 버튼이 비활성이라 물건 상세로는
426건 중 380건(88%)이 조회 불가였다. 답은 물건 쪽이 아니라 사건 쪽에 있었다.

**남은 것**
- **보충 경로를 만들 만하다.** 결과행이 빠진 물건을 사건 단위로 훑으면 한 번 조회로
  그 사건 모든 물건의 기일결과와 낙찰가가 나온다. `_extract_case_shared`가 이미
  `schedule_tables`로 받아오는데 물건 상세에만 얹고 버린다.
- 고침 효과 검증: 일주일 뒤 기일내역 대조로 유찰 누락 1.0%가 얼마나 내려갔는지 본다.
- 매각결과 화면의 총건수 대조는 여전히 사각지대(H3).
- 결과행의 최초 수집 시각(`first_collected_at`)이 없어 결과가 며칠 뒤 올라오는지
  사후 확인이 안 된다.

---

## 2026-09-16 — 매각결과 누락의 범인: 표 헤더 rowspan이 페이지 첫 물건을 먹었다 (조사, 코드 변경 없음)

`SALE-RESULT-GAP.md`가 남긴 숙제를 이어받아 원인을 잡았다. **수집기가 화면에 있는
행을 못 읽고 있었다.** H1(파서가 버린다)이 맞았는데, 버리는 자리가 `store`가 아니라
그 앞 DOM 추출기였다. 코드는 아직 안 고쳤다.

**① 누락률을 정답지로 다시 쟀다 — 2.0%가 아니라 1.0%**
상세의 **기일내역**은 법원이 영구 보관하는 기록이고 '매각/유찰'이 날짜별로 박혀 있다
(금액만 없다). 이걸 분모로 쓰면 되살아난 물건만 보던 코호트 방식보다 정확하다.
8/21~9/14 기일내역이 유찰로 확정한 16,448건 중 결과행 확보 16,278건 = **누락 1.0%**.
기존 2.0%에는 기일변경이 섞여 있었다.

**② 손실은 행 단위다 — 페이지도 법원도 아니다**
누락 170건이 (법원×기일) 93묶음에 흩어져 있고 한 묶음 최대 5건이다. 그중 **49건은
같은 사건의 형제 물건은 잡혔는데 자기만 빠졌다**(예: 서울중앙 2008타경25092 —
물건 2~15는 잡히고 1번만 없음). 페이지가 잘렸다면 40건씩 통째로 사라진다.

**③ 오늘 화면을 직접 열어 재현했다**
서울중앙 매각결과 5페이지: **화면 179건 / 추출 174건 — 페이지마다 정확히 1건**.
속초지원 11건 중 10건. 빠지는 것은 항상 그 페이지의 **첫 물건**이다.

**④ 원인 — `crawler._extract_court_items`의 rowspan 이월**
매각결과 표의 헤더는 `rowspan=3`인데 헤더 블록 셋째 줄이 **셀이 0개인 빈 `<tr>`**이다.
JS가 `if (!cells.length) continue`로 이 줄을 건너뛰면서 rowspan 잔량을 안 깎는다.
남은 잔량이 다음 행(=그 페이지 첫 물건)으로 흘러들어 열이 통째로 밀리고, 물건번호
자리에 '용도'가 들어와 `/^\d+$/` 검사에서 탈락한다. 예외도 안 나고 로그도 안 남는다.

진행 화면은 멀쩡하다(142/142 실측). 매각결과 표에만 있는 헤더 구조다. 게다가
'수집 부족 의심' 경고는 `mode == "current"` 전용이고 임계값도 5%라, 40건당 1건
(2.5%) 손실은 어느 화면에서도 안 걸린다.

**⑤ 왜 2.5%가 아니라 1%로 보이나 — 유찰 행은 화면에서 일찍 사라진다**
속초 9/9 기일: 우리 DB엔 유찰 72·매각 8. 오늘(D+7) 화면에 남은 건 **유찰 3건,
매각 8건 전부**. 유찰은 다음 기일이 잡히는 대로 결과 화면에서 빠진다. 56번 긁어도
유찰 행에 주어지는 기회는 훨씬 적고, 그 짧은 동안 페이지 첫 줄에 걸리면 영구 손실이
된다. 낙찰 행은 7일 내내 남아 회수 기회가 많다.

**⑥ 고침안은 검증까지 해뒀다 (3줄)**
셀 없는 행에서도 pending 잔량을 1씩 깎게 한다. 오늘 받은 실제 페이지로 재생하니
손실 5→0, 1→0이고 진행 화면은 142/142 그대로다.

**남은 것**
- 워크트리에서 고쳐 넣고 `launchctl kickstart -k`. 고치면 결과 화면 수집량이
  페이지당 1건씩 늘어야 한다(서울중앙급이면 사이클당 5건).
- **낙찰 누락률은 아직 못 쟀다.** 잡은 낙찰은 비활성이 되고 상세를 다시 안 받아서
  기일내역으로 대조가 안 된다(8월 낙찰 4,117건 중 기일 뒤 재수집 17건뿐).
  기일이 지났는데 결과행이 없는 비활성 물건 **3,108건**의 상세만 다시 받으면
  '놓친 낙찰'이 몇 건인지 바로 나온다(시간당 ~430건이면 8시간).
- 매각결과 화면의 총건수 대조는 여전히 사각지대다(H3).

---

## 2026-09-16 — 옥션원 대조로 격차 10건, 그중 4건은 법원 화면으로 직접 검증

`DATA-GAPS.md` 신설(G1~G10). **조사만 했고 코드는 안 고쳤다.**

옥션원에 로그인해 우리와 나란히 놓고 봤다. 결론이 하나로 모인다 —
**우리는 거의 다 받아놨고, 안 읽고 있다.**

**법원 화면으로 직접 확인한 것 4개**

1. **낙찰가 창은 7일이 아니다. 그렇다고 영구도 아니다.**
   경매사건검색 유의사항: *"사건이 종국되면 기일관련 정보를 제공하지 않고, 이후 30일이
   지나면 기본정보 만을 제공합니다."* 종국(취하)된 평택지원 2024타경1116을 열어보니
   `기일정보`·`최근입찰결과`가 실제로 공란이었다. **창은 '사건 종국까지'**(보통 2~6개월)다.
   우리 DB의 `종국결과`가 전수 55,345건 전부 `미종국`인 것도 같은 이유다.

2. **법원이 `종국결과`와 `종국일자`를 직접 준다.** 그 평택 건은 `취하 / 2026.09.09`였고,
   우리 `last_seen_at`과 날짜가 정확히 같았다. 우리는 활성 물건만 상세를 받으므로
   **종국된 순간부터 영영 안 본다.** 비활성 직후 한 번만 더 받으면 된다.

3. **`물건상태` 한 필드에 전체 이력이 있다.** 표본의 97.5%가 이미 갖고 있다.
   `매각준비 -> 매각공고 -> 매각 -> 매각허가결정 -> 대금미납` 같은 문자열이다.
   기일내역을 파싱할 필요가 없었다. **법원 오타 `대급납부`를 그대로 받아야 한다**(353건).

4. **인근매각통계가 3,892건 전부 빈 표인 이유** — 화면에 `인근매각통계` 버튼이 따로
   있고 눌러야 채워진다. JS 클릭은 안 먹는다(WebSquare 신뢰된 클릭). 페이저와 같은 문제다.

**옥션원 대조로 안 것**

- 법원 매각결과 화면은 **유찰·매각만** 싣는다. 변경·정지·취하·기각은 안 싣는다.
  옥션원 매각통계 1,274건 기준 **11.5%**. 우리가 잰 '결과행 없음' 13%와 1.5%p 차이인데,
  그 1.5%p가 다른 세션이 찾은 rowspan 버그였다. 숫자가 세 방향에서 만난다.
- 상태 어휘: 우리 2개(`신건`·`유찰 N회`) vs 옥션원 18개.
- 특수물건: 우리 13개 vs 옥션원 40개. **`지분`이 빠져 있다 — 활성 2,497건**(목록 비고에 이미 있다).
- 우리가 갖고도 안 쓰는 것: 사건명(임의/강제·형식적경매), 당사자 내역(100% 보유),
  매각물건명세서 전문(`최선순위 설정`이 있어 대항력 계산이 가능하다).
- 못 따라가는 것: **응찰자 수·낙찰자·차회기일 즉시 확인.** 법정 현장 수집이라 방법이 없다.
- 옥션원은 상세 진입 함수 이름을 로드마다 바꾸고(`AjrRQXBt`→`FpLWcnIB`) 무단수집을
  금지한다. **긁는 것은 선택지가 아니다.** 눈으로 대조하는 용도까지다.

**사고 친 것**
같은 폴더에서 다른 세션이 동시에 작업 중이었고, `SALE-RESULT-GAP.md`를 `cat >`로
덮어썼다. `d3820c4` 버전으로 복구하고 내 몫(옥션원 대조 검증)만 덧붙였다.
**CLAUDE.md 세션 규칙대로 워크트리 격리가 왜 필요한지 실물로 겪었다.**

**남은 것**
- `DATA-GAPS.md`의 고치는 순서표 참고. G5(sold_amount 초기화)와 G2(지분 한 줄)가 제일 싸다.
- G9(기간입찰 0건)은 아직 확인 안 됐다. 법원에 전용 화면이 있는지부터 볼 것.

---

## 2026-09-16 — 수집 낭비를 따지다 격차 16건이 나왔다 (세션 둘이 동시에)

**조사만 했다. 코드는 한 줄도 안 고쳤다.** 결과는 `DATA-GAPS.md`(G1~G16),
`SCREENING-REDESIGN.md`, `SALE-RESULT-GAP.md`에 나눠 담았고 `CLAUDE.md`도 두 군데 고쳤다.

### 어떻게 시작했나

사용자 질문 하나였다. *"신건이 특정 시점에만 올라온다면, 신건 없는 날엔 리소스를
다른 데 쓸 수 있지 않을까? 수집 안 된 것들만 다시 정확하게 수집한다든지."*

가설은 반쯤 맞았다. 신건은 금요일 자정이 아니라 **매각기일 정확히 14일 전**에 올라온다
(8/20 이후 신규 발견 물건의 최빈 간격이 14일, 5,601건). 기일이 월~금에 흩어져 있어
신건도 평일 내내 나온다. 토·일·월이 사실상 0인 것만 맞았다.

**그런데 요일보다 큰 게 나왔다.** 하루 8사이클 중 6개가 매일 신규 0·변경 0~16이다.
낮·저녁 여섯 번이 각각 1.5~3시간씩 120개 화면을 훑고 "바뀐 거 없음"을 확인한다.

그래서 "남는 리소스를 어디에 쓸까"를 찾다가 **"쓸 데가 없다"**는 것을 알았다.
상세 없는 활성 물건 19건, 문서 없는 물건 19건, 좌표 없는 물건 159건. 다 차 있었다.
상세 수집기는 할 일이 없어 같은 34건을 1분마다 재시도하고 있었다.

**답이 뒤집혔다.** 아낀 요청을 다른 수집에 돌리는 게 아니라 **요청 자체를 줄이는** 것이
이득이다. 병목은 우리 시간이 아니라 법원 사이트가 우리를 참아주는 한도이기 때문이다.

### 그러다 진짜 문제로 빠졌다

"수집 안 된 것"을 찾다가 매각결과 누락률 13%를 발견했고, 여기서부터 길이 갈렸다.

**세션이 둘 붙어 있었다.** 다른 세션이 같은 폴더에서 동시에 파고 있었고,
그쪽이 **진짜 원인을 찾았다** — 매각결과 표의 헤더 `rowspan=3` 중 셋째 줄이 셀 0개인
빈 `<tr>`인데, 그 줄을 건너뛰면서 rowspan 잔량을 안 깎아 **페이지마다 첫 물건이 조용히
사라지고 있었다.** 서울중앙 179→174, 속초 11→10. 예외도 로그도 없었다. 고쳐서 main에 넣었다.

나는 바깥에서 같은 질문을 쟀다. 옥션원 매각통계 1,274건을 세니
**법원 매각결과 화면은 유찰·매각만 싣고 변경·정지·취하·기각(11.5%)은 안 싣는다.**
우리가 잰 13%에서 그걸 빼면 1.5%p가 남고, 그게 rowspan 버그가 먹던 양과 같은 자리다.
**독립된 두 측정이 만났다.**

### 사고도 쳤다

내가 `cat >`로 다른 세션의 `SALE-RESULT-GAP.md`를 덮어썼다. `d3820c4`로 복구하고
내 몫(옥션원 대조 검증)만 덧붙였다. **CLAUDE.md 세션 규칙이 왜 워크트리 격리를 말하는지
실물로 겪었다.** 지금도 그쪽이 `crawler.py`를 만지고 있어 위험이 남아 있다.

### 축이 둘이었고, 더 나쁜 건 두 번째에서 나왔다

| | 방법 | 나온 것 |
|---|---|---|
| ① 옥션원 대조 | 그쪽에 있고 우리에 없는 것 | G1~G11 대부분 |
| ② 우리 내부 검증 | 우리 데이터·코드를 직접 뜯기 | **G12·G13·G14** |

**제일 나쁜 것들은 ②에서 나왔다.** 엉뚱한 좌표 449건, 도달 불가능한 위험도 분기.
옥션원과 나란히 놓아서는 안 보이는 것들이다. 비교 대상이 있어야 보이는 격차와,
우리 것만 뜯어야 보이는 결함은 다른 종류다.

### 관통하는 한 줄

**우리는 거의 다 받아놨고, 안 읽고 있다.**

`detail_json['case']` 아래에 사건 화면 세 덩어리(사건내역·문건송달·기일내역)가
들어와 있는데 `sections`/`tables`만 읽고 있었다. 거기에:
- 낙찰가가 `매각(21,000,000원)` 꼴로 들어 있다 (결과 화면과 699건 대조, 불일치 0)
- `물건상태`에 `매각준비 -> 매각공고 -> 매각 -> 매각허가결정 -> 대금미납` 전체 이력이 있다 (97.5% 보유)
- `종국결과`·`종국일자`가 있다 (취하/기각을 알 수 있는 유일한 길)
- `사건명`에 임의/강제/형식적경매가 있다
- `당사자 내역`에 채권자·채무자·임차인이 있다 (100% 보유)

### 그리고 마지막에 제일 큰 게 나왔다

**`CLAUDE.md`의 "공고 전 물건은 홈페이지 어디에도 없다"가 틀렸다.**

`경매공고 > 배당요구종기공고`에 **경매개시결정된 사건**이 뜬다. 매각공고보다 몇 달 이르다.
서울중앙 경매1계 한 계만 봐도 **73건 중 73건(100%)이 우리 DB에 없다.**
2026타경101257은 7/1 개시인데 9/16까지 우리는 존재조차 몰랐다.
법원 60개에 계가 여럿이니 1만 건대로 추정되고, 옥션원과의 4천 건 격차보다 크다.

**이걸 아무도 못 찾은 이유는 단순하다 — 그 화면을 아무도 안 열어봤다.**

### 시행착오 (반복하지 말 것)

1. **분모를 잘못 잡아 13%를 보고했다.** 유찰 물건은 다음 기일이 공고되면 `sale_date`가
   미래로 옮겨가 분모에서 빠진다. 되살아난 물건만 분모로 써야 한다.
2. **"착시였다"고 성급히 닫았다.** 13% 중 11.5%p는 착시가 아니라 실재하는 상태
   (변경·취하·기각)이고 **우리가 못 보는 정보**였다. 착시로 치우면 그걸 영영 안 보게 된다.
3. **"사이트가 원래 안 준다"로 닫으려 했다.** 사용자가 막았고, 실제로는 우리 DOM 파서
   버그였다. **외부 탓으로 닫기 전에 우리 쪽을 다 뒤져야 한다.**
4. **`SPECIAL_RIGHT_KEYWORDS`만 보고 "지분 딱지가 없다"고 단정했다.** 실제 payload를
   만들어보니 `property.share`와 `screening.flags`에 이미 있었다. **코드 한 군데가 아니라
   결과물을 봐야 한다.**
5. **옥션원 상세를 표 제목만 훑었다.** 그래서 "낙찰가를 지우자"는 틀린 제안을 했다.
   옥션원은 `▶본건매각 2026.01.19 / 매각가 130,000,000원 / 1명 입찰 / 대금미납`처럼
   **과거 낙찰을 이력으로 보여준다.** 내가 덤프한 데이터 안에 이미 있었는데 못 봤다.
6. **"핀이 없는 게 틀린 핀보다 낫다"고 했다.** 포기였다. 정상 지번이 왜 실패하는지부터
   봐야 한다.
7. **"링크아웃하면 된다"고 했다.** 사용자 방침은 우리 사이트에서 직접 보는 것이다.
8. **`_extract_building_hint`가 시도명을 반환한다는 걸 코드만 읽고는 못 찾았다.**
   함수를 실제로 호출해보고 나서야 드러났다. **실행이 독해를 이긴다.**

### 조사 막바지에 나온 두 가지 (9/16 늦게)

**낙찰자 실명 출처 — 법원 웹에는 없다(확정).**
옥션원 상세에 `입찰3명, 매수인:서초구 김미자 / 차순위금액 389,100,000원`이 실명으로 있고
URL의 `person_hide` 파라미터로 표시만 토글한다. 법원 웹 6개 화면을 전수 확인했는데
매수인명·차순위·응찰자수가 어디에도 없다. 우리 DB의 `최근입찰결과` 20,000건도
`날짜+결과+금액+허가결정`까지다. 같은 사건을 같은 날 법원에서 열어보니 공란이었다 —
**옥션원이 법원 웹보다 빠르다.** 출처가 현장 청취인지 직접 수급인지는 외부에서 구별 불가라
단정하지 않는다. `차순위금액`은 **2등 입찰가**다(사용자 확인).

**G17 — 낮·저녁 사이클은 '헛도는 것'이 아니라 '보험'이었다.**
에러 로그·테스트·R2를 훑다가 **커버리지 경고를 처음 열었다. 6일에 54번 울렸는데 아무도 안 봤다.**
매 사이클 120개 중 12~17개가 0건인데 대부분은 정상이다(진행 화면이 '오늘~13일' 창이라
그 창에 기일이 없으면 0이 정답이고, 실제로 하루 단위로 통째다).
진짜 신호는 **같은 날 안에서 0↔비0이 흔들리는 것**이고 840개 조합 중 18개(2.1%)다.
광주 478건·목포 264건처럼 큰 법원이 한 사이클만 통째로 빠진다.

**이것이 이 조사의 출발점이었던 "낮 사이클은 낭비"를 뒤집는다.** 메워진 물건은
이미 DB에 있으므로 '동일'로 처리돼 신규·변경 카운트에 안 잡힐 뿐이었다.
**사이클 축소는 이 복구 효과를 계산한 뒤에.**

`지도검색`은 꽁지맵 영역이라 여기서 다루지 않기로 했다(사용자 판단). 미확인 목록에서 뺀다.

### 남은 것과 순서

`DATA-GAPS.md`의 우선순위표를 볼 것. 요지만:

- **G15 배당요구종기공고** — 가장 크지만 새 파이프라인이라 시간이 든다
- **G11 법원 공식 집계와 대조** — 유일하게 '모르는 문제'를 드러내는 장치.
  rowspan 버그가 정확히 그 부재로 오래 살아남았다
- **G12 좌표 449건** — 유일하게 '틀린' 데이터
- **G5·G1·G4(=4·7·9번)** — 전부 같은 데이터에서 나오므로 한 번에 파싱
- **묶음 F(운영 낭비)** — 위를 고친 뒤에 해도 늦지 않다. 다만 **계산은 미리 해둔다.**
  사이클을 줄이려면 `apply_lifecycle`의 전제를 다시 계산해야 한다
  (주석: *"수집이 하루 4~5회 도니 7일이면 30회"* → 하루 2회면 14회)

**세션 분담을 먼저 정해야 한다.** 다른 세션이 `crawler.py`를 만지는 중이고,
오늘 문서에서 겪은 충돌이 코드에서 일어나면 훨씬 비싸다.

---

## 2026-09-16 — 매각결과 '누락 13%'는 착시였다 (조사만, 코드 변경 없음)

수집 낭비를 따지다 "기일이 지났는데 결과행이 없는 물건이 13%"를 발견해 조사했다.
**결론: 실제 누락은 2%다. 13%는 분모를 잘못 잡은 것이다.**

**틀린 계산**
`sale_date`가 지난 물건 수를 분모로 썼다. 그런데 유찰 물건은 다음 기일이 공고되면
(기일 14일 전) `sale_date`가 미래로 옮겨가 분모에서 빠진다. 그래서 분모에 남는 것은
'아직 다음 기일이 안 잡힌 물건'에 쏠려 있고, 그 쪽이 결과행이 없을 확률이 높다.

**바른 계산 — auction_events로 되짚기**
기일이 지나 종결 처리됐다가 **새 기일을 받아 되살아난** 물건은 그날 기일이 실제로
열렸다는 증거다(진행상태 유찰 N→N+1, 최저매각가격 하락으로 확인). 이 집합으로 재니:

| | 건수 |
|---|---|
| 되살아남 + 결과행 있음 | 16,419 |
| 되살아남 + 결과행 없음 | **336 (2.0%)** |

336 중에도 일부는 기일 변경(유찰 횟수·최저가 그대로)이라 진짜 놓침은 더 적다.
8/31 코호트 표본에서는 43건 중 31건만 유찰 확정이었다.

'미상'으로 남는 나머지는 대부분 **아직 다음 기일이 공고 안 된 유찰 물건**이다.
코호트가 늙을수록 줄어든다: 9/08 11.6% → 8/24 5.8% → 8/21 4.9%.

**아니라고 확인한 것들**
- **페이지 잘림 아님.** 통영지원은 31사이클 내내 176 → (9/14 기일 뒤) 207로 고정.
  어떤 법원도 페이지 블록 경계(40×10=400) 근처가 아니다. 최대가 서울남부 611.
  단, `_collect_result_pages`의 '수집 부족 의심' 경고는 `mode == "current"`에서만
  켜진다. **매각결과 화면은 잘려도 경고가 안 뜬다** — 지금은 문제없지만 사각지대다.
- **키 매칭 아님.** 9/4 이후 고아 결과행 0건(그 전 108건은 caseno 수정 전 잔재).
- **법원 통째 누락 아님.** 31사이클 전부 0건이던 해남·여주·남원·거창·강릉은
  그 기간에 **실제로 기일이 없었다**(여주 8/26→9/16, 거창 8/21→10/16, 남원 8/31→10/12).
  법원 기일 달력에 몇 주씩 공백이 있다.

**부수 소득 — 사이클마다 매각결과를 다시 긁을 이유가 없다**
결과는 기일 다음날 오전에 몰려 올라온다(9/15 기일 → 9/16 01:29엔 7,799, 07:38엔 8,226).
그 뒤 사이클들의 증가분은 +10 수준이다. 지금은 7일 창 동안 56번(8회/일 × 7일) 긁는데도
2%를 못 잡는다. **더 자주 긁어서 해결되는 종류가 아니다.** 하루 1~2회로 줄여도 손실이 없다.

**남은 것 → `SALE-RESULT-GAP.md`로 넘겼다 (미해결, 새 세션이 이어받음)**
- 2%도 낙찰가라 놓치면 영영 못 얻는다. 닫지 말 것. 첫 단서는 `record_sale_results`가
  `received - saved`(유효성 검사에서 버린 행 수)를 계산해 놓고 로그에 안 찍는 것.
- 매각된 물건의 누락률은 우리 데이터만으로 검증 불가(결과 화면이 유일한 증거).
  같은 화면·같은 행이므로 유찰과 비슷한 2% 안팎으로 본다.
- 유찰 누락은 사실상 손실이 아니다. 상세의 기일내역에 유찰 이력이 영구히 남는다.
  **7일 시한이 걸린 것은 낙찰 '금액' 하나뿐이다**(기일내역은 '매각/유찰'만, 금액 없음 — 확인함).

---

## 2026-09-15 — 상세 수집기: 브라우저부터 교체 + 조회불가 무한루프

커밋 `2713316`, `250c238`. main에 빨리감기 병합 후 **푸시 완료**.

9/8부터 이어온 상세 수집기 실패 추적의 마지막 두 손질이다.

**① 차단 의심에 냉각 사다리부터 타던 문제 (2713316)**
거버너는 차단 의심이 뜨면 60→120→240→480초로 냉각을 올리며 단일 워커 보수 모드로
버티다가, 워치독이 성공률 급락을 감지해야 비로소 브라우저를 새로 열었다. 효과는 그
반대 순서다 — 9/11~9/15 자가 복구 167회를 재보니 **교체 직전 25% → 냉각만 거친 뒤
48% → 교체 직후 65%**. 이제 첫 차단 의심에서 바로 패스를 접고 새 브라우저로 재개한다.

실측(같은 날 앞뒤 비교):

| | 변경 전 00:05~12:09 (12.1h) | 변경 후 12:09~18:47 (6.6h) |
|---|---|---|
| 차단 의심 | 53회 | 7회 |
| 냉각 단계 | 60·120·240·480·900초 | 60초만 |
| 냉각 합계 | 4.5시간 (전체의 37%) | 0.1시간 (1.5%) |

처리량은 이 날 비교가 불가능하다. 낮에 **백로그가 처음으로 다 비었다**(대상 1,681 →
39). 오후의 낮은 성공률·처리량은 할 일이 없어서지 변경 탓이 아니다. 어제는 하루 종일
109 밑으로 안 내려갔다.

**② 조회 불가 물건이 영원히 재시도 대상으로 돌아오던 문제 (250c238)**
①의 결과로 백로그가 비면서 드러난 묵은 버그다. 패스가 1분마다 같은 사건으로 다시
시작했고 선두 물건은 `detail_fail_count`가 41이었다. `list_detail_targets`의
'목록 갱신' 조건이 상태를 안 가린 탓 — unavailable은 백오프가 아니라
`detail_next_retry_at=NULL`로 표시되는데 그 조건이 NULL을 '지금 대상'으로 읽어서
**시도→조회불가→NULL→즉시 대상**으로 영원히 돌았다. 7건이 걸려 있었다. 그 조건에서
unavailable을 뺐다(재공고는 아래 전용 조건이 `detail_checked_at` 기준으로 잡아준다).

**누적 성과** (9/7 → 9/15)
성공률 52% → 88~90%, 상세 없는 활성 물건 118건 → **11건**, 사진 없는 활성 1,395 → 1,104.

**9/16 실측 — 두 변경 모두 확인됨 (아침 9h / 낮 13.3h 두 차례)**

네트워크 오류 0건, 데몬 18.7시간 무중단. 냉각 사다리가 사라졌다.

| | 9/13 | 9/14 | 9/15 | **9/16 (13.3h)** |
|---|---|---|---|---|
| 냉각 합계 | 17.3h (72%) | 6.1h (25%) | 4.6h (19%) | **0.6h (4.5%)** |
| 냉각 단계 | ~900초까지 | ~900초까지 | ~900초까지 | **60초만 (37회)** |
| 시간당 성공 | 41건 | 317건 | 296건 | **432건** |
| 성공률 | 41% | 86% | 81% | 87% |

성공률은 그대로인데 **속도가 역대 최고**다. 24시간 환산 약 10,400건으로 종전 최고인
9/14의 7,605건을 크게 넘는다. 기다리던 시간이 일하는 시간으로 바뀐 것이 그대로 나온다.

**부하 시험도 통과했다.** 오전에 수집 사이클이 돌며 대기 물량이 30 → 2,762까지 찼는데
냉각은 0.6시간에서 더 늘지 않았고(09:07 34회 → 13:23 37회), 물량은 몇 시간 만에
201까지 빠졌다. 브라우저 교체는 13.3시간에 37회로, 되돌림 기준으로 잡았던
'하루 300회'에 한참 못 미친다.

무한루프도 끝났다. 패스 선두 사건이 매번 바뀌고, 조회 불가 사건은 9시간 로그에 1~3회만
나온다(고치기 전에는 1분마다). `detail_fail_count` 최댓값 403은 고침 이전 누적이며
그 뒤로 건드리지 않았다. 기일 뒤로 밀린 재시도는 61건 → **2건**.

이로써 9/8부터 이어온 상세 수집기 추적을 닫는다. 성공률 52% → 87%,
시간당 성공 약 200건 → 432건, 상세 없는 활성 물건 118건 → 19건.

**남은 것**
- 사이트가 원래 10~15%는 안 준다(새 브라우저로도 재현). 100%는 목표가 아니며
  성공률 80%대가 정상 운전 상태다.
- 재시작 직후 표본으로 판단하지 말 것 — 이 건으로 세 번 틀린 보고를 했다.
- 판단 지표는 성공률이 아니라 **냉각 합계 시간**과 **시간당 성공 건수**다.

---|---|---|---|---|
| 냉각 합계 | 17.3h (72%) | 6.1h (25%) | 4.6h (19%) | **0.6h (6.6%)** |
| 냉각 단계 | ~900초까지 | ~900초까지 | ~900초까지 | **60초만** |
| 시간당 성공 | 41건 | 317건 | 296건 | **362건** |
| 성공률 | 41% | 86% | 81% | 80% |

성공률은 그대로인데 **속도가 역대 최고**다(24시간 환산 약 8,700건, 종전 최고는 9/14의
7,605건). 기다리는 시간을 일하는 시간으로 바꾼 것이 그대로 나타났다.

무한루프도 끝났다. 패스 선두 사건이 매번 바뀌고, 조회 불가 사건은 9시간 로그에 1~3회만
나온다(고치기 전에는 1분마다). `detail_fail_count` 최댓값 403은 고침 이전 누적이며
그 뒤로 건드리지 않았다. 기일 뒤로 밀린 재시도도 61건 → **2건**으로 정리됐다.

**남은 것**
- 사이트가 원래 10~15%는 안 준다(새 브라우저로도 재현). 100%는 목표가 아니며,
  성공률 80% 부근이 정상 운전 상태다.
- 재시작 직후 표본으로 판단하지 말 것 — 이 건으로 세 번 틀린 보고를 했다.
- 판단 지표는 성공률이 아니라 **냉각 합계 시간**과 **시간당 성공 건수**다.

---

## 2026-09-15 — 세션 엉킴 정리

크롤러 저장소에 세션이 5개 붙어 한 작업대(워크트리 하나)를 같이 쓰다 엉켰다. 대화 도중에도 다른 세션이 같은 파일을 커밋할 만큼 실시간으로 부딪히고 있었다.

**한 것**
- 8월 26일 이후 활동이 없던 세션 2개 아카이브 (`프로젝트 폴더 구조 및 목적 확인`, `수집 데이터 검증`) — 크롤러 세션 5개 → 3개
- `CLAUDE.md`에 세션 규칙 추가 (푸시 승인 / 개발기록 / 워크트리 격리)
- 이 파일 신설
- `.claude/worktrees/`를 `.gitignore`에 추가 — 격리 작업대가 `git status`에 잡히지 않게

- 밀려 있던 `chore/launchd-paths-and-log-rotation`(6커밋)을 main에 빨리감기 병합하고 푸시 (승인받음).
  `git switch` 대신 `git fetch . <브랜치>:main`으로 main 딱지만 옮겼다 — 이 폴더에서 수집 데몬이 도는데
  switch를 먼저 하면 파일이 8/26 상태로 잠깐 되돌아가고, 그 순간 새 프로세스가 뜨면 옛 코드로 돈다.
- 다 끝난 브랜치 2개 삭제 (`chore/launchd-paths-and-log-rotation`, `fix/list-pagination-early-stop`).
  원격에 `main` 하나만 남았다.

**격리 워크트리 동작 확인** (한 번 만들어보고 지웠다)
- `origin/main` 최신에서 갈라진다 — main이 밀려 있으면 낡은 코드에서 시작하므로, 병합을 먼저 한 이유다
- `data/`는 실제로 따라오지 않는다 (문서에 적은 함정이 사실임을 확인)
- 격리 세션은 `git -C <원본경로>`로 원본 작업대를 건드리는 것조차 막힌다 — 규칙이 아니라 강제다

**알아낸 함정**
격리된 워크트리에는 `data/`가 따라오지 않는다(gitignore). 거기서 수집 명령을 돌리면 `--db data/auction.sqlite3` 기본값이 cwd 상대경로라 **에러 없이 빈 DB가 새로 생긴다.** 실제 4GB DB는 원본 폴더에만 있다.

---

## 2026-09-15 이전

이 파일이 생기기 전 기록은 `git log`에 있다.

```bash
git log --oneline
```
