# 개발기록

**새 기록은 맨 위에 쌓는다.** 세션이 여럿 붙어 있어서, 여기가 다른 세션이 무슨 일이 있었는지 아는 유일한 통로다.

한 항목에 **무엇을 왜 고쳤는지**와 **남은 것**이면 충분하다. 코드 단위 변경 내역은 `git log`가 이미 갖고 있으니 옮겨 적지 않는다.

---

## 2026-09-23 — Jev(TypeSafe AI) 평가 · 대항력 '포기' 헛경고 1,780건 발견 (Jev 평가 세션)

사용자가 준 테스트 키로 Jev(2026-09-15 공개, 글 대신 판정만 내는 모델)를 우리 자료에 붙여봤다.
**코드는 안 고쳤다.** 시험 스크립트는 세션 임시 폴더에만 있다.

**① 지금 웹에 나가는 버그 — Jev와 무관하게 급하다.** `parse_special_rights` 는 글자 포함만 본다.
`대항력` 이 든 활성 물건 2,058건 중 **1,808건(88%)이 "주택도시보증공사가 대항력을 포기"** 문구다 —
낙찰자가 보증금을 안 떠안는다는 **안전** 신호인데 `대항력있는임차인`(`_TRAP_HIGH`)으로 찍힌다.
**1,780건은 이것 하나 때문에 '위험 높음'** 이다. 유치권도 취하·철회·부존재 확정된 것까지 높음이다(표본 30 중 5).

**② 성적** — 정답은 Jev 돌리기 **전에** 손으로 고정했다.

| 표본 | 지금 코드 | 한 줄 규칙 | Jev(질문 2개) |
|---|---|---|---|
| 대항력 층별 45 | 25 | 45 | 45 |
| 대항력 까다로운 21 (포기+위험문구 전수) | — | 19 | 21 |
| 대항력 새 표본 24 (과적합 확인용) | — | 24 | 24 |
| 유치권 30 | 25 | 29 | 30 |

규칙이 놓친 3건이 하필 제일 위험한 것이다 — '포기한 임차인 말고 **다른** 미상 임차인' 2건,
'유치권부존재 소송 **원고 패소**'(= 유치권 인정) 1건. 규칙은 셋 다 안전이라 한다.

**③ 질문 설계가 성패를 가른다.** 한 질문에 세 갈래를 몰아 물었을 땐 까다로운 21건에서 14/21로
규칙보다 못했다 — "대항력 있음 … 단, 포기" 를 글자 그대로 읽었다(공식 약점 문서의 literal reading).
공식 권고대로 원자 질문 2개(포기가 있나 / 그와 별개로 남은 게 있나)로 쪼개고 합치기는 코드가 하니 120/120.

**④ 숫자.** 한국어 동작(공식 문서엔 언급 없음). 응답 중앙 ~370ms, 호출당 입력 ~550토큰,
활성 3.4만 건 전량 1회 ≈ $0.8. 날짜 비교·숫자·세기는 공식 약점 — 대항력 **성립** 판단(전입일 vs 설정일)은 코드에 둔다.

**남은 것**
- ①은 한 줄 규칙으로 오늘 고칠 수 있다(외부 의존 없음). payload 모양은 그대로(딱지 수만 준다)라
  웹 합의는 필요 없지만 **함정 ② — 상세 강제 재업로드**가 필요하다. 위험도 담당(세션 E) 몫.
- Jev를 붙일지는 사용자 결정. 붙인다면 `enrichment`(순수 함수) 밖 별도 단계로, 비고가 바뀐 것만 호출,
  실패하면 규칙으로 후퇴, `status.py` 에 결과 탐침. 초기 접근 단계 스타트업이라 규칙을 버리지 말 것.
- 표본이 작다(120건). 정답은 내가 매긴 것이고 경매 전문가 검수 전이다.

---

## 2026-09-23 13:30 — 하루 뒤 재검토: 어제의 차단 원인 진단은 틀렸다 (세션 B)

사용자 지시로 처음부터 다시 쟀다. **가장 큰 결론은 정정이다.**

**① '내 17:27 동시 재시작이 차단을 불렀다' 는 틀렸다.** 재시작 시각을 확인도 안 하고
인과를 단정했다. 독립된 두 출처가 같은 답을 낸다 — DB `detail_collected_at` 시간대별
성공: 11~14시 시간당 400~500건 → **15시 6건·16시 3건(붕괴)** → 17:27 재시작 뒤 18시 51·
19시 103(오히려 회복). 로그도 악화가 11:27 프로세스 안(L1681)에서 시작됐고 재시작(L2874)
은 한참 뒤다. collect 사이클과 겹친 시간엔 상세가 멀쩡했다. 경위·표는 `CRAWL-LOAD.md`
2026-09-22 절 정정. 01:40 정지 자체는 그 새벽 창이 실제로 나빴으니(패스 69~86%) 보수적
판단으로 틀리지 않았다 — **이유가 틀렸다.**

**② 숫자 혼동이 원인이었다.** '3%'(진입 대비 실패 줄) · '90%'('수집 완료' 줄 수 = **패스
수** 대비) · 패스 집계(완료/실패 개수)를 섞어 비교했다. 그리고 09-21 13:59 의 '2.5시간 3%
→ 회복 확정' 은 재시작 직후 허니문이었다(메모리가 이미 경고한 것). 1시간 뒤 무너졌다.

**③ 지금 상태는 건강하다.** 상세 수집기는 09-22 18:27 부터 19시간 무중단(자가재시작 0),
워커 1·지연 5. **최근 창 세션거절률 1%**(기준 5%). 나쁜 건 **새벽 00~05시**에 몰렸다 —
짧은 패스가 5연속 거절로 계속 중단됐다(워커 1 에서도). 하루 합계 24% 는 나쁜 새벽과
좋은 낮이 섞인 숫자다.

**④ '사건 검색 결과 없음' 은 버그가 아니다.** 최근 실패의 75%(143건)가 이것인데 전부
**그날 03:32 에 목록에 처음 뜬 새 물건**이고 141건이 결국 성공했다(첫 등장→성공 중앙
6시간). 목록엔 떴는데 법원 사건검색엔 아직 안 잡힌 것. 요청 낭비가 조금 있다 — 첫 시도를
늦추는 최적화는 법원 반영 지연을 따로 잰 뒤에(지금 6시간은 우리 재시도 간격일 수 있다).

**고친 것 (status.py)**
- G14 탐침이 **옛 6인자 시그니처**로 불러 예외 → None → '낮음이 안 나온다' 로 오보했다.
  새 시그니처로 세 단계를 다 부르고, None 은 '탐침이 못 잼' 으로 따로 표시. ⬜ → 🔸.
- 부하 줄을 **최근 300건 창의 세션거절률**(CLAUDE.md 5% 선과 같은 단위)로 바꿨다. 하루
  합계는 참고로 뒤에. 실패 중 '새 물건 검색 전' 수도 같이 보인다. `load_window` 로 빼서
  `test_status_metric` 에 시험 5개.

**사용자 결정 → 한 것**
- **G14 라이브 반영: '데이터에서 중복 없애고 올리기'.** 웹을 읽어 보니 옛 문구 하드코딩은
  없고 '낮음' 도 초록 칩으로 그린다. 다만 특수권리(노랑)와 위험도 깃발(회색)을 **같은 줄**에
  그려서 09-21 설계대로면 `[유치권][유치권]` 으로 겹친다. 그래서 `screening.flags` 는 `[]`,
  이유는 새 필드 `screening.reasons`. `collect` **하나만** 재시작(13:44, 상세는 그대로),
  item `web_sync` 63,401행 `pushed_at` 을 비워 재업로드를 걸었다(사이클당 1만·활성 먼저,
  활성 ≈ 하루). **남은 양은 status.py G14 줄이 센다 — 0 이면 ✅.** 첫 푸시 뒤 R2 를 직접
  읽어 확인하는 검증을 걸어 뒀다(결과는 아래에 덧붙임).
- **워커는 1 유지.** 워커 2 는 '3.5시간 좋다가 무너진' 기록밖에 없다. 새벽을 몇 번 더 넘기고.
- 참고: 9/22 14:00 예약 재개는 **예약 세션이 명령 승인 창에서 멈춰** 안 돌았다. 17:04 에
  수동 재개. 사람 없는 시각의 예약 작업은 승인 창에서 멈출 수 있다.
- 오늘 collect 재시작이 띄운 사이클은 **full(120구간)** 이었다 — 하루 한 번 몫이 마침 돌
  차례였다. 목록 수집 중에도 상세는 계속 돈다.

## 2026-09-22 18:30 — '실패가 우리 버그냐 법원 차단이냐' 전수 검증 (세션 B)

사용자가 정당하게 의심했다("정말 법원이 막는 거 맞아, 코드 버그 아니고?"). 추측
말고 증거로 갈랐다. **결론: 코드는 멀쩡하고, 실패는 법원의 일시적 세션 차단이다.**

- **생중계 단독 재현(결정적).** 데몬이 방금 "없다"고 거절한 사건(청주 2025타경51839·
  51876)을 데몬 멈추고 `--item-key … --force` 로 **혼자** 부르니 둘 다 즉시 성공
  (완료 1·실패 0). 같은 사건·코드·시각인데 부하 때는 실패, 혼자면 성공 = 차단이지 버그
  아니다. 버그면 둘 다 실패해야 한다. (재현법: 데몬 bootout → 단독 --item-key 조회)
- **전수 통계.** 활성 물건 상세수집 collected 98%(실패 상태는 62,407 중 15개). "한 번
  이라도 실패"한 13,192건 중 **56%가 결국 collected**(버그면 0%여야 한다). 코드 주석의
  과거 실측 59%와 일치.
- **품질.** collected 무작위 10건 전부 알맹이 있음(주소·감정가·최저가·case 테이블
  11~25·사진 1~5). 빈 껍데기 0.
- **정직한 단서.** 지금 재개 후 실패율이 ~9~16%로 ~~사고 전 3%보다 높다. 이건 코드가
  아니라 오늘 내 동시 재시작이 부른 차단의 여진이다.~~ **(09-23 정정: '3%' 는 다른
  공식이었고 재시작은 원인이 아니었다 — 맨 위 09-23 항목.)** 코드가 아니라는 결론은
  그대로 맞다. collect 는 이번에도 안 건드렸다(pid 그대로 확인).

`case_search_error`(detail_crawler.py:219) 가 실패를 3갈래(세션 거짓말/정말 없음/물건
없음)로 이미 잘 가른다. 무작정 '차단'이라 안 한다.

## 2026-09-22 01:40 — ~~데몬 동시 재시작이 차단을 재발시켰다~~ 상세 수집 정지 (세션 B)

> **09-23 정정: 아래 인과는 틀렸다.** 붕괴는 재시작 2시간 반 전(15시)에 이미 시작됐고,
> '3% → 90%' 는 공식이 다른 숫자였다. 맨 위 09-23 항목과 `CRAWL-LOAD.md` 정정 참조.

**상세 수집 데몬을 멈춰 뒀다.** (09-22 17:04 에 재개됨.)

~~로깅 문구만 바꾸는 변경(자가재시작 로그)을 반영하려고 `check_daemon_fresh --fix` 로
collect·collect-details·server 셋을 17:27 에 동시 재시작한 게 화근이었다. collect
재시작은 즉시 목록 수집 사이클(법원 대량 요청)을 돌리는데, 그게 상세 재시작과 겹쳐
동시 부하 스파이크가 됐다. 2.5시간 3%로 안정돼 있던 상세 수집이 재시작 직후부터
6시간 넘게 ~90% 차단으로 뒤집혔다(수집완료 35 vs 물건목록없음 495).~~ 소프트 차단이지
영구 밴은 아니다(`사건 화면은 정상`). 새벽 창이 실제로 나빠 `bootout` 으로 정지했다.

**재개는 2026-09-22 14:00(KST) 로 예약해 뒀다** (사용자 결정). 예약 작업
`resume-detail-crawler-gentle` 이 그때 `scripts/resume_gentle.sh` 를 돌린다 — 상세
수집기만 **워커 1·지연 5** 로 얌전히 켜고(속도는 `data/detail_pace.env` 에 박혀
KeepAlive·자가재시작을 거쳐도 유지), **collect 는 절대 안 건드린다.** 켠 뒤 거절률을
재서 사용자에게 보고하고, 워커 2 상향은 사람이 보고 따로 한다(자동 상향 없음).

**그 전에 수동으로 켜야 하면 이 한 줄만:** `zsh scripts/resume_gentle.sh`
(절대 collect 를 함께 재시작하지 말 것. `check_daemon_fresh --fix` 도 금지 — 여러
데몬을 한꺼번에 재시작한다.)

교훈·경위는 `CRAWL-LOAD.md`(2026-09-22 절). **문구만 바꾸는 변경으론 데몬을 재시작하지
말고, 꼭 해야 하면 하나씩 시차를 두고 — 특히 collect 는 상세가 조용할 때 따로.**

## 2026-09-21 — 남은 격차 차례대로: G03·G13 화면안내·자가재시작 로그 (세션 B)

부하 위기가 잡힌 뒤(위 09-20 항목), 남은 항목을 순서대로 정리했다.

**G03 (종국결과/취하·기각) — 코드는 완료. '0건' 은 버그가 아니다.** 끝까지 확인했다:
훑기는 돈다(사라진 물건 200건 재수집분이 전부 `종국결과=미종국`을 담고 컬럼에도 씀
→ 파서 작동), 취하 저장도 `tests/test_store.py` 가 이미 못 박고 있다(같은 경로).
종국 확인이 0건인 이유는 훑은 것이 아직 전부 진짜 미종국이라서다 — 취하·기각은
극소수라 순번이 안 왔을 뿐이다. 시한 안에 잡힌다(만료 7일 전 우선순위 ↑, 법원 시한
30일). `gaps/G03`. **값이 계속 0이면** 데몬 멈춤(`check_daemon_fresh`)이나 큐 굶김
(우선순위)을 의심한다.

**G13 세션 D 표시 지침 — 실거래 신뢰도 4단계.** 세션 B 매칭이 크게 올랐다: 새 로직
3,131건 기준 `parcel`(같은 필지) 16% + `name`(같은 건물) 7% = **`matched` 23%**
(옛 3.5%). 나머지 35,747건은 옛 스키마라 백필 중. `gaps/G13` 에 payload 경로·뱃지
문구·화면 규칙 3가지를 그대로 구현 가능하게 남겼다 — 핵심은 **`matched:false` 면
`building`·`recent[].name`(동네 남의 건물)을 물건과 나란히 붙이지 말 것**.

**데몬 자가재시작 로그 — 위 09-20 항목의 '개선점' 을 처리했다.** 재시작 줄에
타임스탬프가 아예 없어 '언제·왜 죽었는지' 를 못 짚던 문제다. 공통 길목
`common.self_restart` 에 `[utc] SELF-RESTART` 를 붙이고, `DbHealthWatchdog` 는 마지막
DB 오류를, 상세 워치독은 최초 트리거(정체/성공률급락/차단)를 재시작 줄에 실었다.
로그 문구만 바뀌고 수집 동작·부하는 그대로. `test_web_api` 에 시험 추가.

**덤: `check_daemon_fresh` 의 거짓 음성을 고쳤다.** `common.py` 가 어느 데몬 감시
목록에도 없어, 모두가 import 하는 그 파일을 고쳐도 '최신' 으로 잡혔다. `server`
데몬은 아예 감시 밖이라 09-15 부터 **6일째 낡은** 채였는데 안 잡혔다. common.py 포함
+ server 추가. 셋(collect·collect-details·server) 다 새 코드로 재시작하고
collect-details 인자(worker2·delay3·loop)를 `ps` 로 재확인했다.

**G14 위험도 — 재설계 완료(데이터측).** 사용자가 기준을 정했다(질문에 답):
*위험도 = 권리상 함정만.* `build_screening` 을 특수권리 목록에서 등급 내게 바꿨다
(높음=유치권·법정지상권·대항력/선순위임차인·별도등기·지분매각·분묘, 보통=맹지·농취증·
위반건축물·제시외·재매각·형식적경매, 없으면 낮음). 유찰·최저가율·주소·가격은 뺐다.
실측(활성 500): **낮음 0%→51%, 높음 52%→12%** — 이제 셋이 갈리고 근거가 잡음 대신
변별력 있는 신호다. `gaps/G14`·`SCREENING-REDESIGN`. **웹 반영·재푸시는 세션 D/E**
(경계 규칙 — flags 내용이 바뀌어 웹이 맞춰야 하고, 라이브 반영은 함정 ② 재푸시 필요).
수집 데몬은 라이브 섞임 방지로 일부러 이 변경만으론 재시작 안 했다(collect 가
`check_daemon_fresh` 에 '낡음' 으로 보이는 건 의도된 상태).

**남은 것:** 백필이 계속 빠진다 — 요항표·실거래(옛 스키마 35,747건)·종국 훑기.
G14 웹 롤아웃(세션 D/E). 그 밖에 코드로 급한 건 없다.

## 2026-09-20 — 수집 부하로 밀려나기 시작했다. 내가 만든 부하다 (세션 B)

**[09-21 해결]** 진짜 원인은 `run_detail_collector.sh` 의 인라인 주석이 `--workers·--delay·--loop` 을 잘라 **부하 축소가 한 번도 적용된 적이 없던 것**이었다. 스크립트를 고치고 재개하니 실패율 45% → 3%(2.5시간 유지). 차단 회복 확정. 며칠간의 '효과 없다' 는 전부 깨진 설정끼리 비교였다 — 바꾼 게 프로세스에 도달했는지 `ps` 로 확인 안 한 탓. 워커 2·지연 3 으로 안전하게 계속 돈다.

**법원이 세션을 거절하기 시작했다.** `받아둔 적 있는 활성 사건을 없다고 함` — 있는
사건을 없다고 답하는 소프트 차단이다. 거절률이 09-17 2% → 09-20 11% 로 5배가 됐다.

원인은 **내가 만들었다.** 09-18 에 감정평가서 상태를 정직하게 되돌린 것은 옳았지만,
그 부작용으로 **3만 건이 한꺼번에 재수집 큐에 들어갈 것을 계산하지 않았다.** 하루
시도가 9,958 → 13,244 로 뛰었다.

수습을 두 번 했다. 첫 번째는 헛짚었다.
- **지연 2.0 → 3.0**: 실패율 45% → 44%. **효과 없음.** "천천히 보내면 되겠지" 하고
  재보지도 않고 바꿨다. 이번 세션 내내 하지 말자던 실수를 부하 문제에서 또 했다.
- 무엇이 실패하는지 봤더니 거절의 **76%가 활성 물건** = 진짜 차단, 연속 오류로 몰림.
  요청 간격이 아니라 **동시 세션 수**가 지렛대였다. **워커 3 → 2** 로 내렸다.
  효과는 표본 1,500건 쌓인 뒤 재측정 중(가정 안 한다).

**영구 차단이 가장 큰 사업 위험**이라 정리해 뒀다 — `CRAWL-LOAD.md`. 단계별 대응
(재큐 멈춤 → 워커↓ → 하루이틀 중단)과 지켜야 할 선(워커 3·지연 3초 상한, 거절률
5% 신호선, 대량 교정은 나눠 풀기). **IP 를 바꿔 우회하는 것은 접근통제 회피라
안 한다** — 차단보다 나쁜 결과로 간다. 필요하면 법원과 데이터 계약이 답이다.

### 데몬 재시작 관련(질문받아 확인)
상세 데몬이 09-20 중 한 번 자가재시작했다. **크래시는 아니다**(에러 로그 0바이트).
`자가 복구 → 브라우저를 새로 엽니다` 는 거버너가 브라우저만 다시 여는 정상 동작이지
프로세스 종료가 아니다. 자가재시작은 `DbHealthWatchdog`(맥 잠자기 후 DB 핸들 깨짐)
로 보이나 그 경로가 로그를 안 남겨 단정은 못 한다. **개선점: 자가재시작 시 이유를
로그에 남기게 해야 이런 질문에 답할 수 있다.**

## 2026-09-19~20 — 옥션원을 뜯어 우리와 대조하고, 안 꺼내던 것을 꺼냈다 (세션 B · G18)

사용자가 옥션원 공개 샘플을 직접 확인하라 해서 상세 한 장(`ca_view.php?sample_key=1`)을
실제로 열었다. **이전에 나는 '옥션원도 텍스트를 긁는 것' 이라 확인 없이 단정했다가
사용자에게 정정당했다.** 이번엔 봤다.

**받아 두고 안 꺼내던 것 7종을 payload 로 꺼냈다**(새로 안 긁는다, 상세 표에서 추출).
배당요구종기 100% · 사건접수 100% · 청구금액 94% · 입찰방법 97% · 임차인 42% ·
가압류 63% · **지상권 17%**(법정지상권, 지금껏 화면에 안 나갔다).

**요항표에서 사실을 뽑았다**(문장이 아니라). 제시외물건·공부와의 차이·맹지·구조·
사용승인일. `없 음.`(띄어쓰기!)을 내용으로 세던 오탐을 실측으로 잡았다(64%→24%).

**점유인(임차인) 내역**을 현황조사서에서 뽑았다 — 전입일자·확정일자(대항력 핵심).
**이름은 낙찰 후 가린다**(사용자 방침). 진행 중엔 보이고, 낙찰되면 `김○○`, 중지·
재매각으로 살아나면 `is_active` 로 자연히 다시 보인다. 법인·날짜는 안 가린다.

**미래 기일·최저가 추정.** 처음엔 '추정을 사실처럼 내보낸다' 며 반대했으나, 우리
`sale_results` 44,524쌍으로 **법원별 실측 체감률**을 재서 만들었다(70% 40곳·80% 11곳).
옥션원 샘플값과 3차·4차가 정확히 일치. 각 줄에 `estimated`·`basis` 를 붙였다.

**G06 정정**: 감정평가서 '원천 불가' 는 과했다 — PDF 원본은 못 받지만 요항표는
법원이 화면에 텍스트로 준다. 협회 주소·PDF 는 '링크도 금지' 라 payload 에서 걷어냈다.
**G15 연결**: 안 만들기로 했다 — 공고 사건의 사건 화면이 감정가·기일을 아직 안 준다.
**G13**: 조회 6→24개월, 단독·상가 추가로 대상 87→93%. **G03**: 시한 있는 훑기를
앞세우게 고쳤다(종국결과 영구 손실 방지).

**함정 ① 을 이틀 연속 밟아 도구로 만들었다** — `scripts/check_daemon_fresh.py`.
데몬 시작 시각과 그 데몬이 읽는 소스 수정 시각을 대서 낡은 것을 잡는다. `--fix` 로
재시작까지. 만들자마자 1분 차이 하나를 잡았다.

## 2026-09-18 — 격차 6건을 닫았다. 그리고 **재는 자가 틀린 사고를 여덟 번** 밟았다 (세션 B)

| 격차 | 결과 |
|---|---|
| **G09** 입찰구분 | 고치기 전 실측 — 진행 3법원 모두 기일입찰=전체. 손실 0 확인 후 '전체'로 |
| **G13** 실거래 매칭 | 이름 버리고 **필지**로 대조. 건물 3.5% → **37%** |
| **G08** 인근매각통계 | **16/16** 채워짐. 소급 불가(종국 사건은 물건상세가 안 열린다) |
| **G15** 배당요구종기공고 | **수집 시작.** 서울중앙 376건 전부 우리에게 없던 사건 |
| **G06** 감정평가서 | 거짓 `collected` **56,123 → 6**. 본문은 원천 불가로 확정 |
| **G17** 자정 넘김 | 하루 5회 중 1회가 진행 60법원을 통째로 날리던 것 |
| G14 위험도 | **재측정만.** 기준 설계는 이 세션 일이 아니다(사용자 방침) |

### 오늘의 진짜 교훈 — `CLAUDE.md` 함정 ⑤-1로 박았다

여덟 번 같은 실수를 했다. 전부 **결과가 아닌 것을 결과의 대용으로 삼은 것**이다.

| 무엇 | 왜 틀렸나 |
|---|---|
| 좌표 지표를 원본 주소와 댔다 | 수집기는 깎은 쿼리와 댄다. 정확한 핀 125건을 틀렸다고 셌다 |
| 실거래를 지번만으로 맞췄다 | 한 시군구에 같은 지번이 동마다 있다. 40%로 부풀었다 |
| `sale_date`를 `'2026-09-25'`와 비교했다 | DB는 점 형식. `.` > `-` 라 지난 기일이 미래로 잡혔다 |
| 로그 문구를 세어 추세를 봤다 | 그 문구는 9/10에 **생긴** 것. 없던 날을 0으로 읽고 위기라 보고했다 |
| 표 대기를 `visible`·`N개월`·`td`로 걸었다 | 셋 다 도착 전부터 참. 16건 전부 빈 표를 받고 성공으로 넘어갔다 |
| 지표가 정렬 없이 `LIMIT 200` | 가장 오래된 것을 집어 고쳐도 영영 안 움직일 뻔 |
| 문서 판정이 `resources` 하나라도 있으면 참 | Adobe 아이콘이 늘 온다. 56,123건이 거짓 '수집됨' |
| 커버리지가 **긁은 줄 수**를 셌다 | 빈 화면이 안내 행을 7~8줄 준다. 0건이 8건으로 기록됐다 |

여덟 번 다 **숫자를 믿기 전에** 잡았지만, 로그 문구 건은 이미 "악화 추세"라고
보고한 뒤였다. 그래서 고치기 전에 묻는 네 가지를 함정 ⑤-1에 적었다 —
무엇과 대조하나 · 무엇을 기다리나 · 표본이 고친 코드를 볼 수 있나 ·
이 문자열이 그때도 있었나. **그리고 관용(`except: pass`)은 실패를 숨긴다.**

### 재검증에서 또 걸린 것

- **상세 데몬이 G06 수정을 안 물고 있었다**(함정 ①). 14:03에 고쳤는데 데몬은
  07:59에 시작한 것이라, 그 사이 56건을 다시 `collected`로 적었다. **하루에 두 번째다.**
- **G15가 60곳을 다 돌고 마지막에 한 번 썼다.** 중간에 죽으면 통째로 날린다.
  공고는 종기가 지나면 사라지므로 다시 못 얻을 수 있다. 법원마다 쓰게 고쳤다.
- **G13 '17%'는 분모가 달랐다.** 건물 37% · 토지 0% · 전체 17%. 토지 0%는
  버그가 아니라 본질이다(특정 지번이 6개월 안에 거래될 확률이 낮다).

### 남은 것
- G15 전국 수집 첫 판, `com.court-auction.notices` 매일 05:40
- G15가 얻은 사건번호를 상세 수집기로 넘기는 연결(개시 후 14일 제약을 봐야 한다)
- G17 2번(망가진 사이클 즉시 재시도) — 0번 효과를 재보고 판단
- G14 위험도 기준 설계 → `SCREENING-REDESIGN.md`
- G13 옛 모양 22,756건은 재조회해야 새 `match_level`이 붙는다(함정 ②)

## 2026-09-17 — 엉뚱한 좌표 0건. 그런데 여섯 번은 **재는 자**가 틀렸다 (세션 B)

`G12`를 닫았다. 좌표 있는 물건 58,999건 중 **다른 법정동에 찍힌 핀 0건.**

숫자가 이렇게 움직였다. `176 → 570 → 103 → 85 → 179 → 104 → 0`.
흔들린 것은 데이터가 아니라 지표였다. 교정을 돌리는데 숫자가 **늘어서** 팠다.

**지표가 수집기와 다른 것을 대고 있었다.** 수집기는 일부러 깎은 쿼리와 댄다(함정 ⑤).
지표만 원본 주소와 댔다. `양천로 400-12 (가양동, 더리브골드타워)`를 `양천로 400-12`로
묻고 브이월드가 `양천로 400-12 (등촌동)`이라 **정확히** 답한 것까지 틀렸다고 셌다 —
179건 중 125건. 다시 물어도 같은 답이 오니 **교정 도구가 제 지표와 싸웠다.**
규칙을 `geocoder` 한 곳에만 두고 지표·교정 도구가 가져다 쓰게 했다.

**가드에도 진짜 구멍이 있었다.** 거부가 항목이 아니라 **줄 단위**였다. 응답은
title·road·parcel 세 줄인데 하나라도 통과하면 항목을 받았다. 장소검색 결과는 road가
비어 있는 일이 많고 빈 줄은 거부 대상이 아니다. 그 한 줄이 parcel의 명백한 불일치를
덮어 `서구 영동빌라`가 가좌동 아닌 **심곡동**에 verified로 박혔다.

**가드를 조이니 핀이 없어지는 물건이 생겼다. 그건 답이 아니다.** 근사 사다리에 한 칸을
더 붙였다. `인천 서구 가좌동 146-44`는 주소 검색이 빈손이고 본번만 남기면 **경기도
고양시** 가좌동이 온다. 주소 검색은 지번이 있어야 답하므로 동까지만 물으면 아무것도
안 준다. 장소 검색은 동 이름만으로도 그 동 안의 지점을 준다(`가좌동 30-81` — 찾던
146-44 근처). district 사다리 끝에 그것을 붙였다. quality는 approximate 그대로.

**데몬이 고친 코드를 모르고 하루 종일 돌았다.** 오늘 지오코딩한 780건 중 40건이 틀린
동에 찍혔는데 커밋된 가드로 돌리면 전부 거부된다. 함정 ①. **교정 도구를 돌리기 전에
데몬부터 재시작**해야 한쪽에서 고치고 다른 쪽에서 새로 만드는 일이 없다.

### 남은 것
- `approximate`를 기한 두고 다시 물어보는 구조는 아직 없다. 한 번 박힌 좌표는 그대로다.

## 2026-09-17 — 입찰구분을 '전체'로. 고치기 전에 재 봤다 (세션 B)

`G09`. 진행·예정 두 검색 화면의 입찰구분 **기본값이 `기일입찰`** 이라 기간입찰 물건이
에러도 경고도 없이 빠지는 구조였다. 행이 줄 뿐이라 함정 ④의 연속 0건 감시에도 안 걸린다.

**고치기 전에 같은 조건으로 두 번 검색해 건수를 댔다**(사용자 요청). 진행 화면
서울중앙 223·수원 734·부산 271 — 기일입찰과 전체가 **전부 같다.** 예정 화면은
`총 N건` 표기가 아예 없어(함정 ③) 첫 장 행수로 갈음했고 8=8로 같다.
**지금 잃고 있던 것은 없다.** 문서의 추정이 실측으로 확인됐다.

그래서 고친 것은 재개될 때를 위한 예방이다. `SearchPageConfig`에 라디오를 얹고
폼이 준비된 직후 누른다. **라디오 input은 가려져 있어 라벨을 눌러야 하고**, JS로
`checked`를 세우면 값만 바뀌고 WebSquare가 모른다(G08·`_go_next_page`와 같은 문제).
못 눌러도 수집은 계속한다 — 기간입찰이 0인 지금 여기서 멈추는 게 더 나쁘다.

고친 뒤 다시 쟀다. 두 화면 모두 열자마자 '전체'가 눌려 있고 건수는 그대로다.
`scripts/g09_check_bid_type.py`로 언제든 다시 잰다. **기간입찰이 재개되면 이 스크립트가
처음 갈라진다.**

---

## 2026-09-17 — 웹이 보증금·좌표 등급을 읽기 시작했다. 그리고 B에 요청 한 건 (세션 D)

**B가 9/17에 넘긴 것을 화면이 읽는다** (`court-auction-web` 8b0702f).

- `auction.deposit` — 최저가의 10%가 아닌 물건에 목록 카드 딱지(`보증금 20%`)와
  상세 경고문. 비율은 추정하지 않고 법원이 적어 둔 값만 쓴다. 매각사례에서는 뺐다 —
  입찰할 일이 없는 물건에 빨간 딱지를 달 이유가 없다.
- `coordinate_quality` / `coordinate_source` — `verified`가 아니면 '위치 근사'로 따로 그린다.
  `building`·`lot`·`district`마다 무엇으로 잡았는지 문장이 다르다.
- `case_type` · `resale` · `parties` · `closing` — 상세 리포트에 줄로 붙였다.
  당사자는 **나열만** 한다. 교부권자가 92%에 있어 경고로 쓰면 아무것도 안 걸러진다는
  경고를 그대로 따랐다.
- `special_rights`의 새 값 셋(지분매각·형식적경매·재매각)은 **코드가 필요 없었다.**
  배열을 그대로 딱지로 그리고 있어서 값이 실리는 순간 저절로 보인다.

**이 필드 이름들은 이제 화면이 읽는다.** 더하는 것은 안전하지만 **빼거나 이름을 바꾸면
웹이 깨진다.** 바꿔야 하면 먼저 말해 달라.

**확인한 것 — 아직 R2엔 안 왔다.** 9/17 13:40 기준 스냅샷(생성 08:22 KST, 31,684건)을
API로 500건 떠 보니 `deposit`·`resale`·`case_type`·`parties`·`closing`이 **하나도 없다.**
좌표 등급만 이미 실려 있다. 커밋이 11:44~11:52, 데몬 재시작이 13:22이니 **이번 사이클 끝
푸시부터** 실릴 것이다. 웹은 전부 옵셔널로 다뤄 그때까지 조용히 비워 둔다. 함정 ①·②
그대로라 적어 둔다 — 푸시가 한 번 돌고 나면 목록은 다 보이고, 상세는 물건이 갱신되는
대로 따라온다.

**요청 — 좌표 없는 활성 152건을 스냅샷에 넣을지 정해 달라** (인계 문서 3번).
`lat IS NOT NULL`이 목록 생성 단계에 걸려 있어 지금은 '위치 미상'으로도 못 띄운다.
웹 쪽은 받을 준비가 거의 됐다(문구 한 줄만 '위치 미상'으로 고치면 된다).
다만 **목록 건수와 지도 동작이 같이 바뀐다** — 좌표 없는 항목은 지도 경계(bounds)
조회에서 빠져야 하고, 꽁지맵이 그것을 어떻게 그릴지도 따로 정해야 한다.
급하지 않다. 하겠다면 날을 맞춰 웹도 같이 고치겠다.

---

## 2026-09-17 — G03·G10: 사라진 물건을 한 번 더 보고, 가려진 당사자에서 신호를 캔다 (세션 B)

**G03 — 물건이 왜 사라졌는지 영영 몰랐다.** 목록 상태는 '신건'과 '유찰 N회'뿐이라
취하는 흔적 없이 사라진다. 사건 화면의 `종국결과`가 유일한 답인데,
`list_detail_targets`가 `is_active=1`로 막아 **비활성이 되면 상세를 영영 다시 안 받았다.**
그래서 우리 DB의 종국결과가 전수 '미종국'이다.

사라진 뒤로 한 번도 안 들여다본 물건을 딱 한 번 더 보게 했다(기본 30일 안).

**두 곳을 다 고쳐야 했다** — `is_active` 문만 열었더니 큐에 **0건**이었다. 재수집 사유
조건이 뒤에서 다시 막고 있었다. 그리고 `collected_at`이 아니라 **`checked_at`**으로 걸었다.
상세조회가 막힌 물건(종결·취하)은 collected_at이 안 올라가서, 그걸로 걸면 같은 물건이
영영 큐 앞에 남는다 — **매각결과 보충에서 이미 겪은 정체를 그대로 다시 밟을 뻔했다.**
`ORDER BY is_active DESC`도 맨 앞에 뒀다. 안 그러면 사라진 물건이 산 물건을 앞지른다.

큐: **11,711건 = 활성 1,512 + 사라진 물건 10,199.** 앞 300건은 전부 활성이다.
종국결과·종국일자는 사건명(G07)과 같은 표라 추출까지 B가 같이 했다 — **A가 할 일이 없다.**

**G10 — 이 문서의 전제도 틀렸다.** *"`에OOO…` 같은 값도 유형 판별에 충분하다"*고
적혀 있었는데, 마스킹이 **첫 글자만 남기고 전부 가린다**(`광OOOOOOO`). 은행/캐피탈/공공을
가르는 건 불가능하다.

**대신 이름이 가려져도 당사자 '구분'은 안 가려진다.** 거기에 신호가 그대로 있다.
`parse_case_parties`로 구성을 세어 `auction.parties`로 싣는다. 채권자는 이름 '모양'으로
개인(13.8%)과 기관이 갈린다.

**다만 교부권자는 변별력이 없다** — 활성 32,619건 중 **30,926건(94.8%)** 에 있다.
조세채권이 통상 한 건은 붙는다는 뜻이라 '있음'을 위험 신호로 쓰면 아무것도 안 걸러진다.
**세션 E(G14)가 이걸 모르고 쓰면 오탐만 는다.** 임차인 47.9%, 가압류권자 48.7%.

이미 받아둔 상세 **61,478건 전부**를 다시 읽어 채웠다(법원 사이트는 안 두드렸다).

**남은 것** — B 몫은 이걸로 끝났다. 전부 세션 D가 화면에 그려야 완성이다(`G13`).
그리고 **데몬은 아직 옛 코드다.** 재시작해야 사라진 물건 훑기가 시작된다.

---

## 2026-09-17 — G07·G02: 이미 아는 사실을 화면이 보는 배열에 넣는다 (세션 B)

사건 화면을 읽는 김에 둘을 더 붙였다. 둘 다 **데이터는 이미 있었는데 화면이 보는
자리에 없어서** 안 보이던 것들이다.

**G07 — 사건명(임의/강제/형식적)을 안 싣고 있었다.** `auction.case_type`으로 싣는다.
활성 전량: 임의 19,618 · 강제 12,117 · 자동차 723 · 공유물분할 523 · 청산 220.

**함정** — `공유물분할을위한경매`는 이름에 '형식적'이 없지만 형식적경매다.
`'형식적경매' in 사건명`으로 거르면 523건을 놓친다. 세 갈래를 다 잡게 했다.

**G02 — 지분매각은 `property.share`와 `screening`이 이미 잡고 있었다.** 그런데 웹이
딱지로 쓰는 배열은 `special_rights` 하나뿐이라 화면에는 안 보였다. 거기 넣었다.

목록(32,424건) 딱지 분포가 이렇게 됐다 — **지분매각 2,118 · 형식적경매 805 ·
재매각 725**(전에는 비고에 '재매각'이 적힌 419건만).

**남은 것** — 세션 D가 이 딱지들을 실제로 그려야 완성이다(`G13`). B 몫으로는
G03(재수집 조건)과 G10(채권자 유형)이 남았다.

---

## 2026-09-17 — G04·G05: 받아만 놓고 안 읽던 사건 화면을 읽는다 (세션 B)

상세를 받을 때 **사건 단위 표가 같이 딸려 오는데 여태 안 읽었다.** 거기 재매각 여부·
보증금·물건상태가 다 있다. 한 번 파싱해 두 격차를 같이 메웠다.

**G01은 손댈 게 없었다.** 집어 보니 `parse_case_schedule` → `backfill_sale_results`가
이미 있고 상세 수집기 안에서 돌고 있다(A가 어젯밤 낙찰가 808건을 건진 그 경로다).
남은 것은 수집이 도는 시간뿐이라 담당을 A로 넘겼다.

**문서의 전제가 틀렸다 — '재매각이면 보증금 20%'가 아니다.**

| 비고에 '재매각'이 있는 활성 물건의 실제 보증금 | 건수 |
|---|---|
| 20% | 302 |
| 10% (안 올랐다) | 78 |
| **30%** | **37** |

27%에서 틀린다. 30%짜리를 20%로 안내하면 그대로 입찰 무효다. **그래서 비율을
추정하지 않고 법원이 적어 둔 금액을 그대로 싣는다**(`auction.deposit`). 재매각 이력은
별개로 싣는다(`auction.resale`). 둘은 겹치지만 같지 않다(활성 462 / 642 중 둘 다 208).

**목록에서 보이게 하려면 컬럼이어야 했다.** 목록 조회는 `detail_json`을 일부러 안
읽는다(763MB). 상세 payload에만 넣으면 정작 목록에서 구별이 안 된다 — 이 격차가
원하는 게 그건데. `save_item_detail`이 저장할 때 컬럼으로 뽑게 하고(좌표·공시가격과
같은 방식), 이미 받아둔 상세는 `scripts/backfill_case_item_fields.py`로 채웠다.
**법원 사이트를 다시 두드리지 않는다** — 손에 있는 JSON만 다시 읽는다.

| | |
|---|---|
| 다시 읽은 상세 | **61,478건** → 55,953건에 값을 채웠다 |
| 활성 재매각 | **462** (대금미납 388 · 매각불허 74) |
| 활성 보증금 20% / 30% | **603 / 39** |

**G05는 새 필드가 필요 없었다.** 과거 낙찰 이력은 이미 `sale_results`로 나가고 있다.
할 일은 활성 물건의 `sold`를 비우는 것뿐이었는데, **그냥 비우면 안 됐다** — 18건 중
6건은 이력 쪽에 금액이 없어 비우는 순간 낙찰가를 잃는다. 이력에 먼저 넣고 비운다.

**남은 것**
- **데몬은 아직 옛 코드다.** 재시작해야 G05 자동 정리(활성 19건)와 새 물건의 재매각
  추출이 돈다.
- `updated_at`을 안 건드렸다. 목록(스냅샷)은 다음 푸시에 새 필드가 실리지만 물건별
  상세 파일은 그 물건이 갱신될 때 따라간다. 전량을 당장 맞추려면 763MB 재업로드다.
- **세션 D가 `deposit.rate > 0.1`을 경고로 띄워야 한다**(`G13`에 적어 뒀다). 이게 이
  격차의 알맹이다 — 데이터는 이제 다 있다.

---

## 2026-09-17 — G12 엉뚱한 좌표: 원인은 읍면 승격이었다, 활성 449건 교정 (세션 B)

**`_extract_building_hint`가 시도명을 돌려주는 건 새는 구멍이었지 원인이 아니었다.**
왜 멀쩡한 지번이 실패하는지를 캤더니 **읍면 승격**이 나왔다.

```
충청북도 음성군 대소면 성본리 577-2  → 브이월드 네 번 모두 '없음' → 폴백이 지어낸 가짜
충청북도 음성군 대소읍 성본리 577-2  → 36.962750, 127.528336 (지번 일치, 가짜와 4.5km)
```

법원은 승격 전 이름을, 지도는 승격 후 이름을 쓴다. 시도(`MERGED_SIDO`)·시군구
(`RENAMED_SIGUNGU`)는 보정하면서 **읍면은 아무도 안 봤다.** 그 면의 토지가 전부
지번 검색에 실패해 폴백으로 샜고, 같은 면 물건이 한 점에 뭉쳤다. 함정 ⑤의 읍면판이다.

**핀을 없애는 쪽으로 가지 않았다**(사용자 지침 — "못 찾는 핀을 자꾸 사라지게 만들면
안 된다"). 못 찾으면 범위를 넓히되 어디까지 맞는 핀인지 밝힌다: 지번 → 건물명 →
본번만(`lot`) → 리·동(`district`) → 그래도 없으면 핀 없음. **읍면 단위 핀은 안 만든다**
— 그게 이 격차의 거짓말이었다. 근사 단계는 PNU를 안 싣는다(옆 필지 PNU를 쓰면
공시가격·건축물대장·실거래가가 남의 땅 것으로 채워진다).

**읍면은 승격만 하는 게 아니라 개명도 한다.** 성주군 금수면 → 금수강산면처럼
이름이 통째로 바뀐 것은 맞바꾸기로 못 잡는다. 읍면 토큰을 빼면 지도가 새 이름으로
찾아 준다(`apply_dropped_eup_myeon`).

**거둔 것 — 활성 449 + 비활성 244, 693건 전부 교정**

| | |
|---|---|
| 지번 정확(`verified`) | **662** — PNU도 662건 새로 붙었다 |
| 옆 필지 / 리 단위 | 12 / 13 |
| 핀 없음 | 6 (활성 3) |
| 옮겨간 거리 | 중앙값 2.65km · 최대 14.23km · **1km 넘게 틀려 있던 것 537건(78%)** |

가짜 좌표는 활성·비활성 모두 0건이다.

좌표를 지웠다 다시 찍지 않고 **제자리에서 갈아끼웠다**(`scripts/g12_refix_coords.py`).
지우면 다음 사이클까지 핀이 사라지고, 그 사이 옛 코드를 돌던 데몬이 다시 가짜를
박을 수 있다. 원본 값은 `data/g12-fake-coords-*.json`에 백업했다.

**밟은 함정 — `mark_coordinate_missing`은 이름과 달리 좌표를 안 지운다.** quality만
바꾼다. 원래 호출자(좌표가 처음부터 없는 물건)에는 맞는 동작인데, 이미 박힌 좌표가
틀린 것으로 밝혀진 경우에 쓰면 **가짜 핀이 그대로 남는다.** 1차 실행에서 9건이 그랬다.
게다가 `geocode_query` 기본값이 빈 문자열이라 무엇이 가짜였는지 증거까지 지워서
검사에서도 사라졌다. 백업으로 되살려 다시 돌렸고, `clear_point=True`를 달았다.

**밟을 뻔한 함정** — C의 재점검 스크립트는 망가진 행을 *지금 코드의*
`_extract_building_hint`로 골라냈다. 그 함수를 고치면 0건으로 보여 "다 고쳐졌다"는
착각을 준다. 판정을 **그때 던진 쿼리**(`geocode_query`)로 옮겼다.

**남은 것**
- **핀 없는 6건**(활성 3)은 **핀을 안 만들기로 했다**(사용자 결정). 면은 멀쩡한데
  그 리·그 도로만 브이월드에 없다. 읍면 단위로 넓히면 한 점에 뭉쳐 이 격차의
  거짓말과 같아진다. DB·payload 모두 '위치 미상'(`missing`/`none`)으로 말한다.
  **세션 D가 목록에 '위치 미상'으로 드러내야 한다**(G13에 적어 뒀다).
- ~~좌표는 있는데 라벨이 `missing`인 22건~~ → 고쳤다. 다시 물어보니 22건 모두
  `verified`가 나왔고 **좌표는 0m 움직였다** — 값은 처음부터 맞았고 라벨만 거짓말이었다.
  웹이 `quality`를 쓰기 시작하면 멀쩡한 핀 16개가 '위치 미상'으로 빠질 뻔했다.
  `--mislabeled` 모드로 돌렸다. **이 모드는 실패해도 점을 지우지 않는다** — 좌표가
  틀렸다는 근거가 없는데 API가 잠깐 죽은 것만으로 멀쩡한 핀을 날릴 수는 없다.
  지금 라벨과 실제가 어긋난 행은 양방향 모두 0건이다.
- **웹이 `coordinate_quality`를 쓰게 해야 한다(세션 D, G13).** 근사 핀 11건이 지금은
  지번 핀과 똑같이 보인다. 사다리를 놓은 이상 더 미룰 수 없다.
- 한 번 박힌 좌표를 다시 안 물어보는 구조는 그대로다. `approximate`는 기한을 두고
  재시도하는 게 맞다.

---

## 2026-09-17 — B 세션 작업대 준비, main 44커밋 푸시 (세션 C)

G12에 착수할 **세션 B의 워크트리를 `../court-auction-B`(브랜치 `g12-geocode`)에 만들었다.**

`origin/main`이 로컬보다 **44커밋 뒤**여서, 지침대로 `origin/main`에서 갈랐으면
C의 G12 진단(`7fc2109`)이 없는 코드에서 시작할 뻔했다. 로컬 `main`에서 갈라 만들고,
곧바로 **main을 푸시해 `origin/main`을 맞췄다**(`597eeab..e72858d`).
이제 다음 워크트리는 지침대로 `origin/main`에서 갈라도 된다.

**남은 것** — B가 그 워크트리에서 G12를 고친다. 그 세션에서는 DB 명령을 치지 않는다
(`data/`가 안 따라와 빈 DB가 조용히 생긴다). DB 조회·좌표 삭제는 원본 폴더 세션 몫이다.

## 2026-09-17 — 밤새 매각결과 보충: 낙찰가 808건(2,025억) 회수 (세션 A)

00:04~08:14, 15패스. 사이트 차단으로 12번 자가 중단됐고 그때마다 냉각 후 이어갔다.

**거둔 것**

| | |
|---|---|
| 보충한 결과행 | **8,853행** (유찰 5,874 / 변경 1,549 / 매각 1,419) |
| 금액이 붙어 온 행 | 1,187 |
| **낙찰 확정된 물건** | **808건 / 합계 2,025억원** |

회수한 기일이 2023.03까지 거슬러 올라간다. 몰려 있는 곳은 **2026.06(291건)과
2026.07(495건, 그중 481건에 금액)** — 결과 수집을 시작한 8/20 이전 구간이다.
**정상 경로로는 영영 못 얻었을 낙찰가다.**

**대기열이 앞으로 안 나가는 문제를 찾아 고쳤다**

남은 대상이 15,982 → 14,948로밖에 안 줄었다. 대상 조건이 '그 물건의 **현재 기일**에
결과행이 없다'인데, 그 기일에 법원이 결과를 아예 안 올리는 물건이 많다(census 기준
93%가 변경·취소라 애초에 경매가 안 열렸다). 그런 물건은 몇 번을 훑어도 안 빠지고
대기열 맨 앞에 그대로 남는다. 그래서 패스마다 같은 사건을 다시 걸었다 — 12패스는
715사건 2,232행, 15패스는 870사건 160행.

`auction_items.result_checked_at`에 훑은 시각을 적고 **한 번도 안 본 것부터** 돌게
했다. 못 채웠어도 적는다(다음 차례는 오되 한 바퀴 뒤다).

**아침 확인 패스는 수확이 없었다.** 대기열 맨 앞은 밤새 이미 훑은 자리라 당연하고,
여덟 시간을 두들긴 직후라 사이트도 식히는 중으로 보인다(사건 조회가 거의 다 실패).
**다음 보충은 몇 시간 쉬었다가 돌릴 것.**

**남은 것**
- 대상 14,948건(사건 12,010개). 7월 7,447 · 8월 5,971이 아직 안 열린 노른자다.
  이제 대기열이 앞으로 나가므로 다음 밤샘이 훨씬 멀리 간다.
- 보충한 유찰·변경 행은 웹에 바로 안 간다. 낙찰 확정분은 `updated_at`이 갱신돼
  푸시 후보가 되지만 나머지는 아니다(CLAUDE.md 함정 ②).
- 상세 수집 데몬을 8시간 내려뒀더니 상세 대기열이 1,791건까지 쌓였다. 복구됐고
  지금 소화 중이다. 다음 밤샘 때도 같은 값을 치른다.

---

## 2026-09-17 — G12 진단: 엉뚱한 좌표의 78%는 코드가 아니라 **낡은 데이터**였다 (세션 C)

세션 B가 바로 일할 수 있게 DB 실측을 떠뒀다. **고치는 순서가 바뀐다.**
`scripts/geocode_recheck.py` (읽기 전용).

**표본 50건을 코드 한 줄 안 고치고 그대로 다시 돌렸더니 48건(96%)이 정상 좌표를 얻었다.**
옮겨간 거리 중앙값 2.13km, 최대 12.18km — 81%가 1km 넘게 틀려 있었다.

**왜냐면 망가진 좌표의 78%(813건)가 2026-07에 찍혔다.** 광주·전남 통합 대응
(`MERGED_SIDO`)은 **2026-08-24 `ebbf640`** 에 들어왔다. 그 전에는 `전라남도 …`를
브이월드가 못 찾아서 건물명 폴백으로 샜고, 폴백이 시도명을 건물명으로 써서 읍면
한 점에 뭉쳤다. **지금은 같은 쿼리가 그냥 통한다.**

**진짜 구조적 문제는 따로 있다 — 한 번 박힌 좌표는 다시 안 물어본다.**
`geocode-missing`은 *좌표가 없는* 물건만 본다(`cli.py:225`). **틀리게 있는 것은 영원히
안 고쳐진다.** 그래서 8월에 코드를 고쳤는데도 7월에 생긴 813건이 두 달째 지도에 남았다.

**버그 자체는 살아 있다.** 통합 대응 이후인 9월에도 52건이 새로 망가졌고, 지금도
`충청북도 음성군 대소면 삼호리 790` 같은 주소는 주소검색·getcoord 네 번 모두 '없음'이라
폴백으로 흘러간다(표본의 4%).

**버린 가설 하나** — `max_queries=4`가 지번 후보를 잘라낸다고 봤는데, 448건 중 **0건**이
잘렸다. 토지 주소는 정규화하면 후보가 1~2개뿐이라 상한에 안 닿는다.

**세션 B에게 — 순서를 지켜야 한다**
1. `_extract_building_hint`가 행정구역명을 못 돌려주게 막는다. **이게 먼저다** —
   안 그러면 재시도에서 실패하는 4%가 다시 가짜 좌표를 받는다.
2. 그다음 1,041건의 좌표를 지워 재시도 대상으로 돌린다(96% 회수). **DB 쓰기는 C에 부탁.**
3. 그래도 못 찾으면 좌표를 안 만든다.
4. **`approximate` 좌표를 기한 두고 재시도하는 구조**를 볼 것. 이번 건의 진짜 교훈이다.

---

## 2026-09-17 — 바깥 기준선을 깔았다: 법원 공식 통계와 대조 (G11, 세션 C)

우리 검증은 전부 '이전 우리 값'과 비교한다. 처음부터 적게 긁고 있었으면 영원히 모른다.
**법원이 주는 공식 매각통계를 바깥 자[尺]로 쓴다.** `scripts/baseline_compare.py`.

**세션 A가 필요 없어졌다.** 원래는 A가 긁고 C가 대조하는 계획이었는데, 화면을 60번
검색할 필요 없이 **JSON API 하나**면 됐다(`selectRletCortDspslStats.on`, 법원 코드는
`selectCortOfcCdLst.on`). curl은 WAF가 막아서 Playwright로 페이지를 한 번 열고 그 안에서
부른다. 60개 법원에 20초. **`crawler.py`도 `store.py`도 안 건드렸다** — 저장은
`data/official_stats.json`.

**정의가 우리와 다른 곳 두 군데**
- `경매건수` = 입찰(유찰 포함) 및 매각된 물건 → 우리 `유찰`+`매각` 기일행과 맞는다.
- `매각건수` = **최종 물건상태** 기준(`매각·매각허가결정·대금납부`). 우리는 **기일 결과**
  기준이라 **대금미납 재매각에서 어긋난다.** 그래서 경매건수로만 대조한다.

**통계는 다음 달 1일에 확정된다.** 응답에 `frstInptDt: 20260901`이 붙는다.

**기준선 숫자를 얻었다 — 온전히 받으면 100~102%다**
202608은 우리가 8/20에 수집을 시작해 달을 처음부터 못 봤다(전국 58.6%). 하지만
**8/13 이후에만 기일이 있던 법원 5곳**은 남양주 100%, 영덕 101%, 강릉 101%, 거창 102%,
남원 98%였다. **95% 아래면 그 법원에서 새고 있는 것이다.**

**깃발 8개는 전부 오탐이었다.** 영동지원 0%는 8월 기일이 8/4·8/11뿐이라 우리 창 밖이었고
(9월엔 110행 정상), 밀양 25%·안동 25%도 같은 이유였다. **부분 달로는 '적게 받은 법원'과
'기일이 일찍 있던 법원'을 못 가른다.** 도구가 부분 달이면 경고를 먼저 띄우게 해뒀다.

**남은 것 — 날짜 하나**
2026년 9월이 우리가 처음부터 끝까지 본 첫 달이고, 그 통계는 **2026-10-01**에 나온다.
그날 아래를 돌리면 G11이 처음으로 제 일을 한다.
```
.venv/bin/python scripts/baseline_compare.py --fetch 202609
.venv/bin/python scripts/baseline_compare.py 202609
```
95% 아래로 떨어지는 법원이 있으면 **그게 우리가 몰랐던 손실이다.**

덧: 두 스크립트 모두 `--check`로 자가 점검이 붙어 있다. 스크립트는 `.venv/bin/python`으로
돌려야 한다(시스템 python3.9에는 playwright가 없다).

---

## 2026-09-17 — 사이클을 줄여도 되는가: 재보니 전제 세 개가 틀렸다 (G17, 세션 C)

묶음 F가 *"하루 8사이클 중 6개가 낭비"* 라고 했고, G17이 *"낮 사이클은 낭비가 아니라
보험"* 이라고 뒤집었다. 어느 쪽인지 **로그 58일·사이클 296회로 재봤다.**
`scripts/cycle_value.py` — 읽기 전용이고 언제든 다시 돌아간다.

**전제 세 개가 틀렸다**
1. **하루 8회가 아니라 5.1회다.** launchd는 3시간마다 깨우는데 한 사이클이 1.5시간
   걸린다. 3+1.5 = 실제 주기 4.5시간.
2. **로그의 `N개 반영`은 물건 수가 아니다.** 긁은 줄 수다. 물건이 없는 화면이 안내 행을
   7~8줄 돌려주는데, 그게 `8개 반영 (신규 0, 변경 0, 동일 0)`으로 찍힌다.
   **0건이 8건으로 보인다.** 그리고 **커버리지 통계가 쓰는 값이 바로 이것이다.**
3. **'흔들림'은 2.1%가 아니라 12.1%다.** 들어간 수로 다시 세면 그렇다.

**제일 큰 것 — 자정을 넘긴 사이클은 진행 수집을 통째로 날린다**
창은 사이클 **시작 때** 정해진다(`current=9/14~9/27`). 자정이 지나면 시작일이 어제가 되고
진행 화면이 빈 화면을 돌려준다. 예정은 창이 두 달이라 멀쩡하다.
23시대 시작 사이클의 진행 놓침률 **23.1%**(다른 시간대 0.8~5%). 진행 법원 절반 이상을
날린 사이클 11회 중 6회가 23:20~23:38 시작이다. **하루 5회 중 1회가 반쪽이다.**

**답: 4회까지는 안전, 2회는 꼬리가 위험**

| K회/일 | 완전손실(평균) | 최악의 날 | 못 본 물건/일 |
|---|---|---|---|
| 2 | 2.1 | 106 | 403 |
| 4 | 0.1 | 25 | 11 |
| 5 (현재) | 0.0 | 16 | 1 |

완주한 사이클 하나면 120개를 다 본다. **한 번이면 충분한데 어느 사이클이 망가질지 몰라서
다섯 번 돈다** — 보험이 맞다. 다만 보험료는 사이클당 1.5시간 + R2 283MB/791초다.
**생명주기는 줄이기를 막지 않는다** — `unseen_future_days=7`은 하루 2회여도 14회 연속
미목격을 요구하고, 한 법원이 하루 통째로 빠질 확률이 1.9%다.

**남은 것 (세션 A 몫)**
- **자정 넘김 수정** — 진행 수집 직전에 창을 오늘 날짜로 다시 잡는다. 줄이는 것보다 크다.
- **경고 기준을 '긁은 줄 수' → '들어간 수'로.** 지금은 0건이 8건으로 기록돼 안 걸린다.
- **경고 판정을 같은 날 다른 사이클과 비교하도록.** (G17 원래 1번)

---

## 2026-09-17 — 놓친 낙찰가를 사건 화면에서 되찾는 길을 깔았다

앞 기록에서 "사건 화면의 기일 내역에 낙찰가가 영구히 남는다"를 확인했고, 이번에
그걸 실제로 쓰는 코드를 넣었다. **7일 창을 놓쳐도 메울 수 있다.**

**무엇을 만들었나**
- **평소 상세 수집이 사건 화면에 들른 김에** 빠진 결과행을 함께 메운다. 추가 조회 0회.
- `collect-details --backfill-results` — 기일이 지났는데 그 기일의 결과행이 없는
  물건만 골라 사건 단위로 도는 전용 패스. 물건 상세는 아예 건드리지 않는다.
  종결 물건은 상세조회 버튼이 막혀 있어 시도해봐야 unavailable만 쌓인다.

**지켜야 했던 선 세 개** (테스트로 묶어 뒀다)
1. **이미 받아둔 행은 덮지 않는다.** 기일내역은 '매각'인데 금액이 빠질 때가 있다
   (실측 15%). 덮어쓰면 결과 화면에서 받아둔 낙찰가를 잃는다. `DO NOTHING`이다.
2. **되살아난 물건을 옛 낙찰로 내리지 않는다.** 대금미납 재매각은 목록 기일이 옛
   낙찰보다 뒤에 있다. 이걸 팔린 걸로 처리하면 입찰 가능한 물건이 목록에서 사라진다.
3. **이레 창이 아직 열린 기일은 대상에서 뺀다.** 정상 경로가 곧 가져간다. 안 빼면
   대기열 맨 앞이 통째로 '곧 들어올 것들'로 차서 같은 걸 두 번 긁는다.

**첫 보충 실적** (대상 1,500건 / 사건 195건까지 돌고 사이트 차단으로 자가 중단)
- **결과행 772행 보충**, 그중 **낙찰가 18건 회수**.
- 회수한 기일에 2026.03.23 · 05.22 · 06.08 · 07.20이 섞여 있다. **몇 달 지난
  낙찰가가 실제로 돌아왔다.** 6건은 낙찰 확정으로 목록에서 내려가 sold 스냅샷에 들어간다.
- 최근 기일은 수확이 적다. 법원이 기일내역에 결과를 늦게 채우기 때문이다
  ('매각'만 먼저 뜨고 금액이 나중에 붙는다). 오래된 것부터가 수확이 좋다.

**한 번 헛디뎠다.** 처음엔 물건 버튼 개수를 먼저 보는 기존 흐름을 그대로 탔는데,
종결 사건은 버튼이 통째로 없어서 9건 중 6건이 실패하고 보충 0행이 나왔다. 결과만
메우는 패스는 버튼을 아예 보면 안 된다.

**남은 것**
- 백로그가 아직 **15,000행 가까이** 남았다(사건 12,000여 개). `--backfill-results
  --loop`로 붙여 두면 차단될 때마다 냉각하고 이어서 돈다. 상세 수집 데몬과 같은
  락을 쓰므로 데몬을 내리고 돌려야 한다.
- 보충한 행이 웹에 바로 안 갈 수 있다. 낙찰 확정분은 `updated_at`이 갱신돼 푸시
  후보가 되지만, 유찰·변경만 늘어난 물건은 `pending_item_pushes()` 기준에 안 걸린다
  (CLAUDE.md 함정 ②). 전량 재업로드가 필요하면 `web_sync`의 item 행을 지운다.

---

## 2026-09-16 — 페이지 첫 물건 유실 수정 + 낙찰 누락률 0.8% + "7일 지나면 끝"은 틀렸다

같은 날 앞 기록의 후속. **고쳐 넣었고, 낙찰 누락률을 처음으로 실측했고, 이 저장소가
오래 믿어온 전제 하나가 틀렸다는 것을 확인했다.**

**① 고침 적용** — `crawler._extract_court_items`에서 셀 없는 행도 rowspan 잔량을
깎게 했다. 실제 화면 확인: 속초지원 11건 중 11건 추출(전 10건). 검사 하나
추가(`tests/test_crawler_extract.py`) — 고치기 전 코드에서 실패하는 것 확인.
수집 데몬 재시작 완료.

**② 낙찰 누락률 ≈ 0.8%** — 기일이 지났는데 결과행이 없는 비활성 물건 3,108건이
미지수였다. 무작위 표본 508건의 사건 화면을 훑어 그날 진짜 무슨 일이 있었는지 봤다.

| | 508건 기준 |
|---|---|
| 변경 (경매 안 열림) | 381 (75.0%) |
| 그 날짜 기일 자체가 없음 | 92 (18.1%) |
| 유찰 (놓친 유찰) | 27 (5.3%) |
| **매각 (놓친 낙찰)** | **8 (1.6%)** |

**93%는 애초에 경매가 안 열린 것이다.** 3,108건에 펼치면 놓친 낙찰 ~49건,
같은 기간 잡은 낙찰 6,352건 대비 **0.8%**.

**③ 전제가 틀렸다 — 낙찰가는 영구히 남는다.** 사건검색의 **'기일 내역' 탭**이
물건번호별로 `매각 (175,900,000원)` 꼴로 **금액까지** 준다. '매각' 기일행의 85%에
금액이 있고, 2025년 기일도 그대로 보이며, 우리가 결과 화면에서 받아둔 낙찰가와
대조해 불일치 0건이었다. **종결돼서 물건상세조회가 막힌 물건도 이 탭은 열린다.**
물건 상세 쪽 기일내역에는 금액이 없어서 없는 줄 알았던 것이다. CLAUDE.md 함정 ⑥을
고쳤다.

**④ 먼저 헛디딘 것** — 종결 물건은 '물건상세조회' 버튼이 비활성이라 물건 상세로는
426건 중 380건(88%)이 조회 불가였다. 답은 물건 쪽이 아니라 사건 쪽에 있었다.

**남은 것**
- **보충 경로를 만들 만하다.** 결과행이 빠진 물건을 사건 단위로 훑으면 한 번 조회로
  그 사건 모든 물건의 기일결과와 낙찰가가 나온다. `_extract_case_shared`가 이미
  `schedule_tables`로 받아오는데 물건 상세에만 얹고 버린다.
- 고침 효과 검증: 일주일 뒤 기일내역 대조로 유찰 누락 1.0%가 얼마나 내려갔는지 본다.
- 매각결과 화면의 총건수 대조는 여전히 사각지대(H3).
- 결과행의 최초 수집 시각(`first_collected_at`)이 없어 결과가 며칠 뒤 올라오는지
  사후 확인이 안 된다.

---

## 2026-09-16 — 매각결과 누락의 범인: 표 헤더 rowspan이 페이지 첫 물건을 먹었다 (조사, 코드 변경 없음)

`SALE-RESULT-GAP.md`가 남긴 숙제를 이어받아 원인을 잡았다. **수집기가 화면에 있는
행을 못 읽고 있었다.** H1(파서가 버린다)이 맞았는데, 버리는 자리가 `store`가 아니라
그 앞 DOM 추출기였다. 코드는 아직 안 고쳤다.

**① 누락률을 정답지로 다시 쟀다 — 2.0%가 아니라 1.0%**
상세의 **기일내역**은 법원이 영구 보관하는 기록이고 '매각/유찰'이 날짜별로 박혀 있다
(금액만 없다). 이걸 분모로 쓰면 되살아난 물건만 보던 코호트 방식보다 정확하다.
8/21~9/14 기일내역이 유찰로 확정한 16,448건 중 결과행 확보 16,278건 = **누락 1.0%**.
기존 2.0%에는 기일변경이 섞여 있었다.

**② 손실은 행 단위다 — 페이지도 법원도 아니다**
누락 170건이 (법원×기일) 93묶음에 흩어져 있고 한 묶음 최대 5건이다. 그중 **49건은
같은 사건의 형제 물건은 잡혔는데 자기만 빠졌다**(예: 서울중앙 2008타경25092 —
물건 2~15는 잡히고 1번만 없음). 페이지가 잘렸다면 40건씩 통째로 사라진다.

**③ 오늘 화면을 직접 열어 재현했다**
서울중앙 매각결과 5페이지: **화면 179건 / 추출 174건 — 페이지마다 정확히 1건**.
속초지원 11건 중 10건. 빠지는 것은 항상 그 페이지의 **첫 물건**이다.

**④ 원인 — `crawler._extract_court_items`의 rowspan 이월**
매각결과 표의 헤더는 `rowspan=3`인데 헤더 블록 셋째 줄이 **셀이 0개인 빈 `<tr>`**이다.
JS가 `if (!cells.length) continue`로 이 줄을 건너뛰면서 rowspan 잔량을 안 깎는다.
남은 잔량이 다음 행(=그 페이지 첫 물건)으로 흘러들어 열이 통째로 밀리고, 물건번호
자리에 '용도'가 들어와 `/^\d+$/` 검사에서 탈락한다. 예외도 안 나고 로그도 안 남는다.

진행 화면은 멀쩡하다(142/142 실측). 매각결과 표에만 있는 헤더 구조다. 게다가
'수집 부족 의심' 경고는 `mode == "current"` 전용이고 임계값도 5%라, 40건당 1건
(2.5%) 손실은 어느 화면에서도 안 걸린다.

**⑤ 왜 2.5%가 아니라 1%로 보이나 — 유찰 행은 화면에서 일찍 사라진다**
속초 9/9 기일: 우리 DB엔 유찰 72·매각 8. 오늘(D+7) 화면에 남은 건 **유찰 3건,
매각 8건 전부**. 유찰은 다음 기일이 잡히는 대로 결과 화면에서 빠진다. 56번 긁어도
유찰 행에 주어지는 기회는 훨씬 적고, 그 짧은 동안 페이지 첫 줄에 걸리면 영구 손실이
된다. 낙찰 행은 7일 내내 남아 회수 기회가 많다.

**⑥ 고침안은 검증까지 해뒀다 (3줄)**
셀 없는 행에서도 pending 잔량을 1씩 깎게 한다. 오늘 받은 실제 페이지로 재생하니
손실 5→0, 1→0이고 진행 화면은 142/142 그대로다.

**남은 것**
- 워크트리에서 고쳐 넣고 `launchctl kickstart -k`. 고치면 결과 화면 수집량이
  페이지당 1건씩 늘어야 한다(서울중앙급이면 사이클당 5건).
- **낙찰 누락률은 아직 못 쟀다.** 잡은 낙찰은 비활성이 되고 상세를 다시 안 받아서
  기일내역으로 대조가 안 된다(8월 낙찰 4,117건 중 기일 뒤 재수집 17건뿐).
  기일이 지났는데 결과행이 없는 비활성 물건 **3,108건**의 상세만 다시 받으면
  '놓친 낙찰'이 몇 건인지 바로 나온다(시간당 ~430건이면 8시간).
- 매각결과 화면의 총건수 대조는 여전히 사각지대다(H3).

---

## 2026-09-16 — 옥션원 대조로 격차 10건, 그중 4건은 법원 화면으로 직접 검증

`DATA-GAPS.md` 신설(G1~G10). **조사만 했고 코드는 안 고쳤다.**

옥션원에 로그인해 우리와 나란히 놓고 봤다. 결론이 하나로 모인다 —
**우리는 거의 다 받아놨고, 안 읽고 있다.**

**법원 화면으로 직접 확인한 것 4개**

1. **낙찰가 창은 7일이 아니다. 그렇다고 영구도 아니다.**
   경매사건검색 유의사항: *"사건이 종국되면 기일관련 정보를 제공하지 않고, 이후 30일이
   지나면 기본정보 만을 제공합니다."* 종국(취하)된 평택지원 2024타경1116을 열어보니
   `기일정보`·`최근입찰결과`가 실제로 공란이었다. **창은 '사건 종국까지'**(보통 2~6개월)다.
   우리 DB의 `종국결과`가 전수 55,345건 전부 `미종국`인 것도 같은 이유다.

2. **법원이 `종국결과`와 `종국일자`를 직접 준다.** 그 평택 건은 `취하 / 2026.09.09`였고,
   우리 `last_seen_at`과 날짜가 정확히 같았다. 우리는 활성 물건만 상세를 받으므로
   **종국된 순간부터 영영 안 본다.** 비활성 직후 한 번만 더 받으면 된다.

3. **`물건상태` 한 필드에 전체 이력이 있다.** 표본의 97.5%가 이미 갖고 있다.
   `매각준비 -> 매각공고 -> 매각 -> 매각허가결정 -> 대금미납` 같은 문자열이다.
   기일내역을 파싱할 필요가 없었다. **법원 오타 `대급납부`를 그대로 받아야 한다**(353건).

4. **인근매각통계가 3,892건 전부 빈 표인 이유** — 화면에 `인근매각통계` 버튼이 따로
   있고 눌러야 채워진다. JS 클릭은 안 먹는다(WebSquare 신뢰된 클릭). 페이저와 같은 문제다.

**옥션원 대조로 안 것**

- 법원 매각결과 화면은 **유찰·매각만** 싣는다. 변경·정지·취하·기각은 안 싣는다.
  옥션원 매각통계 1,274건 기준 **11.5%**. 우리가 잰 '결과행 없음' 13%와 1.5%p 차이인데,
  그 1.5%p가 다른 세션이 찾은 rowspan 버그였다. 숫자가 세 방향에서 만난다.
- 상태 어휘: 우리 2개(`신건`·`유찰 N회`) vs 옥션원 18개.
- 특수물건: 우리 13개 vs 옥션원 40개. **`지분`이 빠져 있다 — 활성 2,497건**(목록 비고에 이미 있다).
- 우리가 갖고도 안 쓰는 것: 사건명(임의/강제·형식적경매), 당사자 내역(100% 보유),
  매각물건명세서 전문(`최선순위 설정`이 있어 대항력 계산이 가능하다).
- 못 따라가는 것: **응찰자 수·낙찰자·차회기일 즉시 확인.** 법정 현장 수집이라 방법이 없다.
- 옥션원은 상세 진입 함수 이름을 로드마다 바꾸고(`AjrRQXBt`→`FpLWcnIB`) 무단수집을
  금지한다. **긁는 것은 선택지가 아니다.** 눈으로 대조하는 용도까지다.

**사고 친 것**
같은 폴더에서 다른 세션이 동시에 작업 중이었고, `SALE-RESULT-GAP.md`를 `cat >`로
덮어썼다. `d3820c4` 버전으로 복구하고 내 몫(옥션원 대조 검증)만 덧붙였다.
**CLAUDE.md 세션 규칙대로 워크트리 격리가 왜 필요한지 실물로 겪었다.**

**남은 것**
- `DATA-GAPS.md`의 고치는 순서표 참고. G5(sold_amount 초기화)와 G2(지분 한 줄)가 제일 싸다.
- G9(기간입찰 0건)은 아직 확인 안 됐다. 법원에 전용 화면이 있는지부터 볼 것.

---

## 2026-09-16 — 수집 낭비를 따지다 격차 16건이 나왔다 (세션 둘이 동시에)

**조사만 했다. 코드는 한 줄도 안 고쳤다.** 결과는 `DATA-GAPS.md`(G1~G16),
`SCREENING-REDESIGN.md`, `SALE-RESULT-GAP.md`에 나눠 담았고 `CLAUDE.md`도 두 군데 고쳤다.

### 어떻게 시작했나

사용자 질문 하나였다. *"신건이 특정 시점에만 올라온다면, 신건 없는 날엔 리소스를
다른 데 쓸 수 있지 않을까? 수집 안 된 것들만 다시 정확하게 수집한다든지."*

가설은 반쯤 맞았다. 신건은 금요일 자정이 아니라 **매각기일 정확히 14일 전**에 올라온다
(8/20 이후 신규 발견 물건의 최빈 간격이 14일, 5,601건). 기일이 월~금에 흩어져 있어
신건도 평일 내내 나온다. 토·일·월이 사실상 0인 것만 맞았다.

**그런데 요일보다 큰 게 나왔다.** 하루 8사이클 중 6개가 매일 신규 0·변경 0~16이다.
낮·저녁 여섯 번이 각각 1.5~3시간씩 120개 화면을 훑고 "바뀐 거 없음"을 확인한다.

그래서 "남는 리소스를 어디에 쓸까"를 찾다가 **"쓸 데가 없다"**는 것을 알았다.
상세 없는 활성 물건 19건, 문서 없는 물건 19건, 좌표 없는 물건 159건. 다 차 있었다.
상세 수집기는 할 일이 없어 같은 34건을 1분마다 재시도하고 있었다.

**답이 뒤집혔다.** 아낀 요청을 다른 수집에 돌리는 게 아니라 **요청 자체를 줄이는** 것이
이득이다. 병목은 우리 시간이 아니라 법원 사이트가 우리를 참아주는 한도이기 때문이다.

### 그러다 진짜 문제로 빠졌다

"수집 안 된 것"을 찾다가 매각결과 누락률 13%를 발견했고, 여기서부터 길이 갈렸다.

**세션이 둘 붙어 있었다.** 다른 세션이 같은 폴더에서 동시에 파고 있었고,
그쪽이 **진짜 원인을 찾았다** — 매각결과 표의 헤더 `rowspan=3` 중 셋째 줄이 셀 0개인
빈 `<tr>`인데, 그 줄을 건너뛰면서 rowspan 잔량을 안 깎아 **페이지마다 첫 물건이 조용히
사라지고 있었다.** 서울중앙 179→174, 속초 11→10. 예외도 로그도 없었다. 고쳐서 main에 넣었다.

나는 바깥에서 같은 질문을 쟀다. 옥션원 매각통계 1,274건을 세니
**법원 매각결과 화면은 유찰·매각만 싣고 변경·정지·취하·기각(11.5%)은 안 싣는다.**
우리가 잰 13%에서 그걸 빼면 1.5%p가 남고, 그게 rowspan 버그가 먹던 양과 같은 자리다.
**독립된 두 측정이 만났다.**

### 사고도 쳤다

내가 `cat >`로 다른 세션의 `SALE-RESULT-GAP.md`를 덮어썼다. `d3820c4`로 복구하고
내 몫(옥션원 대조 검증)만 덧붙였다. **CLAUDE.md 세션 규칙이 왜 워크트리 격리를 말하는지
실물로 겪었다.** 지금도 그쪽이 `crawler.py`를 만지고 있어 위험이 남아 있다.

### 축이 둘이었고, 더 나쁜 건 두 번째에서 나왔다

| | 방법 | 나온 것 |
|---|---|---|
| ① 옥션원 대조 | 그쪽에 있고 우리에 없는 것 | G1~G11 대부분 |
| ② 우리 내부 검증 | 우리 데이터·코드를 직접 뜯기 | **G12·G13·G14** |

**제일 나쁜 것들은 ②에서 나왔다.** 엉뚱한 좌표 449건, 도달 불가능한 위험도 분기.
옥션원과 나란히 놓아서는 안 보이는 것들이다. 비교 대상이 있어야 보이는 격차와,
우리 것만 뜯어야 보이는 결함은 다른 종류다.

### 관통하는 한 줄

**우리는 거의 다 받아놨고, 안 읽고 있다.**

`detail_json['case']` 아래에 사건 화면 세 덩어리(사건내역·문건송달·기일내역)가
들어와 있는데 `sections`/`tables`만 읽고 있었다. 거기에:
- 낙찰가가 `매각(21,000,000원)` 꼴로 들어 있다 (결과 화면과 699건 대조, 불일치 0)
- `물건상태`에 `매각준비 -> 매각공고 -> 매각 -> 매각허가결정 -> 대금미납` 전체 이력이 있다 (97.5% 보유)
- `종국결과`·`종국일자`가 있다 (취하/기각을 알 수 있는 유일한 길)
- `사건명`에 임의/강제/형식적경매가 있다
- `당사자 내역`에 채권자·채무자·임차인이 있다 (100% 보유)

### 그리고 마지막에 제일 큰 게 나왔다

**`CLAUDE.md`의 "공고 전 물건은 홈페이지 어디에도 없다"가 틀렸다.**

`경매공고 > 배당요구종기공고`에 **경매개시결정된 사건**이 뜬다. 매각공고보다 몇 달 이르다.
서울중앙 경매1계 한 계만 봐도 **73건 중 73건(100%)이 우리 DB에 없다.**
2026타경101257은 7/1 개시인데 9/16까지 우리는 존재조차 몰랐다.
법원 60개에 계가 여럿이니 1만 건대로 추정되고, 옥션원과의 4천 건 격차보다 크다.

**이걸 아무도 못 찾은 이유는 단순하다 — 그 화면을 아무도 안 열어봤다.**

### 시행착오 (반복하지 말 것)

1. **분모를 잘못 잡아 13%를 보고했다.** 유찰 물건은 다음 기일이 공고되면 `sale_date`가
   미래로 옮겨가 분모에서 빠진다. 되살아난 물건만 분모로 써야 한다.
2. **"착시였다"고 성급히 닫았다.** 13% 중 11.5%p는 착시가 아니라 실재하는 상태
   (변경·취하·기각)이고 **우리가 못 보는 정보**였다. 착시로 치우면 그걸 영영 안 보게 된다.
3. **"사이트가 원래 안 준다"로 닫으려 했다.** 사용자가 막았고, 실제로는 우리 DOM 파서
   버그였다. **외부 탓으로 닫기 전에 우리 쪽을 다 뒤져야 한다.**
4. **`SPECIAL_RIGHT_KEYWORDS`만 보고 "지분 딱지가 없다"고 단정했다.** 실제 payload를
   만들어보니 `property.share`와 `screening.flags`에 이미 있었다. **코드 한 군데가 아니라
   결과물을 봐야 한다.**
5. **옥션원 상세를 표 제목만 훑었다.** 그래서 "낙찰가를 지우자"는 틀린 제안을 했다.
   옥션원은 `▶본건매각 2026.01.19 / 매각가 130,000,000원 / 1명 입찰 / 대금미납`처럼
   **과거 낙찰을 이력으로 보여준다.** 내가 덤프한 데이터 안에 이미 있었는데 못 봤다.
6. **"핀이 없는 게 틀린 핀보다 낫다"고 했다.** 포기였다. 정상 지번이 왜 실패하는지부터
   봐야 한다.
7. **"링크아웃하면 된다"고 했다.** 사용자 방침은 우리 사이트에서 직접 보는 것이다.
8. **`_extract_building_hint`가 시도명을 반환한다는 걸 코드만 읽고는 못 찾았다.**
   함수를 실제로 호출해보고 나서야 드러났다. **실행이 독해를 이긴다.**

### 조사 막바지에 나온 두 가지 (9/16 늦게)

**낙찰자 실명 출처 — 법원 웹에는 없다(확정).**
옥션원 상세에 `입찰3명, 매수인:서초구 김미자 / 차순위금액 389,100,000원`이 실명으로 있고
URL의 `person_hide` 파라미터로 표시만 토글한다. 법원 웹 6개 화면을 전수 확인했는데
매수인명·차순위·응찰자수가 어디에도 없다. 우리 DB의 `최근입찰결과` 20,000건도
`날짜+결과+금액+허가결정`까지다. 같은 사건을 같은 날 법원에서 열어보니 공란이었다 —
**옥션원이 법원 웹보다 빠르다.** 출처가 현장 청취인지 직접 수급인지는 외부에서 구별 불가라
단정하지 않는다. `차순위금액`은 **2등 입찰가**다(사용자 확인).

**G17 — 낮·저녁 사이클은 '헛도는 것'이 아니라 '보험'이었다.**
에러 로그·테스트·R2를 훑다가 **커버리지 경고를 처음 열었다. 6일에 54번 울렸는데 아무도 안 봤다.**
매 사이클 120개 중 12~17개가 0건인데 대부분은 정상이다(진행 화면이 '오늘~13일' 창이라
그 창에 기일이 없으면 0이 정답이고, 실제로 하루 단위로 통째다).
진짜 신호는 **같은 날 안에서 0↔비0이 흔들리는 것**이고 840개 조합 중 18개(2.1%)다.
광주 478건·목포 264건처럼 큰 법원이 한 사이클만 통째로 빠진다.

**이것이 이 조사의 출발점이었던 "낮 사이클은 낭비"를 뒤집는다.** 메워진 물건은
이미 DB에 있으므로 '동일'로 처리돼 신규·변경 카운트에 안 잡힐 뿐이었다.
**사이클 축소는 이 복구 효과를 계산한 뒤에.**

`지도검색`은 꽁지맵 영역이라 여기서 다루지 않기로 했다(사용자 판단). 미확인 목록에서 뺀다.

### 남은 것과 순서

`DATA-GAPS.md`의 우선순위표를 볼 것. 요지만:

- **G15 배당요구종기공고** — 가장 크지만 새 파이프라인이라 시간이 든다
- **G11 법원 공식 집계와 대조** — 유일하게 '모르는 문제'를 드러내는 장치.
  rowspan 버그가 정확히 그 부재로 오래 살아남았다
- **G12 좌표 449건** — 유일하게 '틀린' 데이터
- **G5·G1·G4(=4·7·9번)** — 전부 같은 데이터에서 나오므로 한 번에 파싱
- **묶음 F(운영 낭비)** — 위를 고친 뒤에 해도 늦지 않다. 다만 **계산은 미리 해둔다.**
  사이클을 줄이려면 `apply_lifecycle`의 전제를 다시 계산해야 한다
  (주석: *"수집이 하루 4~5회 도니 7일이면 30회"* → 하루 2회면 14회)

**세션 분담을 먼저 정해야 한다.** 다른 세션이 `crawler.py`를 만지는 중이고,
오늘 문서에서 겪은 충돌이 코드에서 일어나면 훨씬 비싸다.

---

## 2026-09-16 — 매각결과 '누락 13%'는 착시였다 (조사만, 코드 변경 없음)

수집 낭비를 따지다 "기일이 지났는데 결과행이 없는 물건이 13%"를 발견해 조사했다.
**결론: 실제 누락은 2%다. 13%는 분모를 잘못 잡은 것이다.**

**틀린 계산**
`sale_date`가 지난 물건 수를 분모로 썼다. 그런데 유찰 물건은 다음 기일이 공고되면
(기일 14일 전) `sale_date`가 미래로 옮겨가 분모에서 빠진다. 그래서 분모에 남는 것은
'아직 다음 기일이 안 잡힌 물건'에 쏠려 있고, 그 쪽이 결과행이 없을 확률이 높다.

**바른 계산 — auction_events로 되짚기**
기일이 지나 종결 처리됐다가 **새 기일을 받아 되살아난** 물건은 그날 기일이 실제로
열렸다는 증거다(진행상태 유찰 N→N+1, 최저매각가격 하락으로 확인). 이 집합으로 재니:

| | 건수 |
|---|---|
| 되살아남 + 결과행 있음 | 16,419 |
| 되살아남 + 결과행 없음 | **336 (2.0%)** |

336 중에도 일부는 기일 변경(유찰 횟수·최저가 그대로)이라 진짜 놓침은 더 적다.
8/31 코호트 표본에서는 43건 중 31건만 유찰 확정이었다.

'미상'으로 남는 나머지는 대부분 **아직 다음 기일이 공고 안 된 유찰 물건**이다.
코호트가 늙을수록 줄어든다: 9/08 11.6% → 8/24 5.8% → 8/21 4.9%.

**아니라고 확인한 것들**
- **페이지 잘림 아님.** 통영지원은 31사이클 내내 176 → (9/14 기일 뒤) 207로 고정.
  어떤 법원도 페이지 블록 경계(40×10=400) 근처가 아니다. 최대가 서울남부 611.
  단, `_collect_result_pages`의 '수집 부족 의심' 경고는 `mode == "current"`에서만
  켜진다. **매각결과 화면은 잘려도 경고가 안 뜬다** — 지금은 문제없지만 사각지대다.
- **키 매칭 아님.** 9/4 이후 고아 결과행 0건(그 전 108건은 caseno 수정 전 잔재).
- **법원 통째 누락 아님.** 31사이클 전부 0건이던 해남·여주·남원·거창·강릉은
  그 기간에 **실제로 기일이 없었다**(여주 8/26→9/16, 거창 8/21→10/16, 남원 8/31→10/12).
  법원 기일 달력에 몇 주씩 공백이 있다.

**부수 소득 — 사이클마다 매각결과를 다시 긁을 이유가 없다**
결과는 기일 다음날 오전에 몰려 올라온다(9/15 기일 → 9/16 01:29엔 7,799, 07:38엔 8,226).
그 뒤 사이클들의 증가분은 +10 수준이다. 지금은 7일 창 동안 56번(8회/일 × 7일) 긁는데도
2%를 못 잡는다. **더 자주 긁어서 해결되는 종류가 아니다.** 하루 1~2회로 줄여도 손실이 없다.

**남은 것 → `SALE-RESULT-GAP.md`로 넘겼다 (미해결, 새 세션이 이어받음)**
- 2%도 낙찰가라 놓치면 영영 못 얻는다. 닫지 말 것. 첫 단서는 `record_sale_results`가
  `received - saved`(유효성 검사에서 버린 행 수)를 계산해 놓고 로그에 안 찍는 것.
- 매각된 물건의 누락률은 우리 데이터만으로 검증 불가(결과 화면이 유일한 증거).
  같은 화면·같은 행이므로 유찰과 비슷한 2% 안팎으로 본다.
- 유찰 누락은 사실상 손실이 아니다. 상세의 기일내역에 유찰 이력이 영구히 남는다.
  **7일 시한이 걸린 것은 낙찰 '금액' 하나뿐이다**(기일내역은 '매각/유찰'만, 금액 없음 — 확인함).

---

## 2026-09-15 — 상세 수집기: 브라우저부터 교체 + 조회불가 무한루프

커밋 `2713316`, `250c238`. main에 빨리감기 병합 후 **푸시 완료**.

9/8부터 이어온 상세 수집기 실패 추적의 마지막 두 손질이다.

**① 차단 의심에 냉각 사다리부터 타던 문제 (2713316)**
거버너는 차단 의심이 뜨면 60→120→240→480초로 냉각을 올리며 단일 워커 보수 모드로
버티다가, 워치독이 성공률 급락을 감지해야 비로소 브라우저를 새로 열었다. 효과는 그
반대 순서다 — 9/11~9/15 자가 복구 167회를 재보니 **교체 직전 25% → 냉각만 거친 뒤
48% → 교체 직후 65%**. 이제 첫 차단 의심에서 바로 패스를 접고 새 브라우저로 재개한다.

실측(같은 날 앞뒤 비교):

| | 변경 전 00:05~12:09 (12.1h) | 변경 후 12:09~18:47 (6.6h) |
|---|---|---|
| 차단 의심 | 53회 | 7회 |
| 냉각 단계 | 60·120·240·480·900초 | 60초만 |
| 냉각 합계 | 4.5시간 (전체의 37%) | 0.1시간 (1.5%) |

처리량은 이 날 비교가 불가능하다. 낮에 **백로그가 처음으로 다 비었다**(대상 1,681 →
39). 오후의 낮은 성공률·처리량은 할 일이 없어서지 변경 탓이 아니다. 어제는 하루 종일
109 밑으로 안 내려갔다.

**② 조회 불가 물건이 영원히 재시도 대상으로 돌아오던 문제 (250c238)**
①의 결과로 백로그가 비면서 드러난 묵은 버그다. 패스가 1분마다 같은 사건으로 다시
시작했고 선두 물건은 `detail_fail_count`가 41이었다. `list_detail_targets`의
'목록 갱신' 조건이 상태를 안 가린 탓 — unavailable은 백오프가 아니라
`detail_next_retry_at=NULL`로 표시되는데 그 조건이 NULL을 '지금 대상'으로 읽어서
**시도→조회불가→NULL→즉시 대상**으로 영원히 돌았다. 7건이 걸려 있었다. 그 조건에서
unavailable을 뺐다(재공고는 아래 전용 조건이 `detail_checked_at` 기준으로 잡아준다).

**누적 성과** (9/7 → 9/15)
성공률 52% → 88~90%, 상세 없는 활성 물건 118건 → **11건**, 사진 없는 활성 1,395 → 1,104.

**9/16 실측 — 두 변경 모두 확인됨 (아침 9h / 낮 13.3h 두 차례)**

네트워크 오류 0건, 데몬 18.7시간 무중단. 냉각 사다리가 사라졌다.

| | 9/13 | 9/14 | 9/15 | **9/16 (13.3h)** |
|---|---|---|---|---|
| 냉각 합계 | 17.3h (72%) | 6.1h (25%) | 4.6h (19%) | **0.6h (4.5%)** |
| 냉각 단계 | ~900초까지 | ~900초까지 | ~900초까지 | **60초만 (37회)** |
| 시간당 성공 | 41건 | 317건 | 296건 | **432건** |
| 성공률 | 41% | 86% | 81% | 87% |

성공률은 그대로인데 **속도가 역대 최고**다. 24시간 환산 약 10,400건으로 종전 최고인
9/14의 7,605건을 크게 넘는다. 기다리던 시간이 일하는 시간으로 바뀐 것이 그대로 나온다.

**부하 시험도 통과했다.** 오전에 수집 사이클이 돌며 대기 물량이 30 → 2,762까지 찼는데
냉각은 0.6시간에서 더 늘지 않았고(09:07 34회 → 13:23 37회), 물량은 몇 시간 만에
201까지 빠졌다. 브라우저 교체는 13.3시간에 37회로, 되돌림 기준으로 잡았던
'하루 300회'에 한참 못 미친다.

무한루프도 끝났다. 패스 선두 사건이 매번 바뀌고, 조회 불가 사건은 9시간 로그에 1~3회만
나온다(고치기 전에는 1분마다). `detail_fail_count` 최댓값 403은 고침 이전 누적이며
그 뒤로 건드리지 않았다. 기일 뒤로 밀린 재시도는 61건 → **2건**.

이로써 9/8부터 이어온 상세 수집기 추적을 닫는다. 성공률 52% → 87%,
시간당 성공 약 200건 → 432건, 상세 없는 활성 물건 118건 → 19건.

**남은 것**
- 사이트가 원래 10~15%는 안 준다(새 브라우저로도 재현). 100%는 목표가 아니며
  성공률 80%대가 정상 운전 상태다.
- 재시작 직후 표본으로 판단하지 말 것 — 이 건으로 세 번 틀린 보고를 했다.
- 판단 지표는 성공률이 아니라 **냉각 합계 시간**과 **시간당 성공 건수**다.

---|---|---|---|---|
| 냉각 합계 | 17.3h (72%) | 6.1h (25%) | 4.6h (19%) | **0.6h (6.6%)** |
| 냉각 단계 | ~900초까지 | ~900초까지 | ~900초까지 | **60초만** |
| 시간당 성공 | 41건 | 317건 | 296건 | **362건** |
| 성공률 | 41% | 86% | 81% | 80% |

성공률은 그대로인데 **속도가 역대 최고**다(24시간 환산 약 8,700건, 종전 최고는 9/14의
7,605건). 기다리는 시간을 일하는 시간으로 바꾼 것이 그대로 나타났다.

무한루프도 끝났다. 패스 선두 사건이 매번 바뀌고, 조회 불가 사건은 9시간 로그에 1~3회만
나온다(고치기 전에는 1분마다). `detail_fail_count` 최댓값 403은 고침 이전 누적이며
그 뒤로 건드리지 않았다. 기일 뒤로 밀린 재시도도 61건 → **2건**으로 정리됐다.

**남은 것**
- 사이트가 원래 10~15%는 안 준다(새 브라우저로도 재현). 100%는 목표가 아니며,
  성공률 80% 부근이 정상 운전 상태다.
- 재시작 직후 표본으로 판단하지 말 것 — 이 건으로 세 번 틀린 보고를 했다.
- 판단 지표는 성공률이 아니라 **냉각 합계 시간**과 **시간당 성공 건수**다.

---

## 2026-09-15 — 세션 엉킴 정리

크롤러 저장소에 세션이 5개 붙어 한 작업대(워크트리 하나)를 같이 쓰다 엉켰다. 대화 도중에도 다른 세션이 같은 파일을 커밋할 만큼 실시간으로 부딪히고 있었다.

**한 것**
- 8월 26일 이후 활동이 없던 세션 2개 아카이브 (`프로젝트 폴더 구조 및 목적 확인`, `수집 데이터 검증`) — 크롤러 세션 5개 → 3개
- `CLAUDE.md`에 세션 규칙 추가 (푸시 승인 / 개발기록 / 워크트리 격리)
- 이 파일 신설
- `.claude/worktrees/`를 `.gitignore`에 추가 — 격리 작업대가 `git status`에 잡히지 않게

- 밀려 있던 `chore/launchd-paths-and-log-rotation`(6커밋)을 main에 빨리감기 병합하고 푸시 (승인받음).
  `git switch` 대신 `git fetch . <브랜치>:main`으로 main 딱지만 옮겼다 — 이 폴더에서 수집 데몬이 도는데
  switch를 먼저 하면 파일이 8/26 상태로 잠깐 되돌아가고, 그 순간 새 프로세스가 뜨면 옛 코드로 돈다.
- 다 끝난 브랜치 2개 삭제 (`chore/launchd-paths-and-log-rotation`, `fix/list-pagination-early-stop`).
  원격에 `main` 하나만 남았다.

**격리 워크트리 동작 확인** (한 번 만들어보고 지웠다)
- `origin/main` 최신에서 갈라진다 — main이 밀려 있으면 낡은 코드에서 시작하므로, 병합을 먼저 한 이유다
- `data/`는 실제로 따라오지 않는다 (문서에 적은 함정이 사실임을 확인)
- 격리 세션은 `git -C <원본경로>`로 원본 작업대를 건드리는 것조차 막힌다 — 규칙이 아니라 강제다

**알아낸 함정**
격리된 워크트리에는 `data/`가 따라오지 않는다(gitignore). 거기서 수집 명령을 돌리면 `--db data/auction.sqlite3` 기본값이 cwd 상대경로라 **에러 없이 빈 DB가 새로 생긴다.** 실제 4GB DB는 원본 폴더에만 있다.

---

## 2026-09-15 이전

이 파일이 생기기 전 기록은 `git log`에 있다.

```bash
git log --oneline
```
